---
phase: quick-260528-v61
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/test_async_tools.py
  - tests/staleness/tool_invoker.py
  - src/db/metadata.py
  - tests/unit/test_metadata.py
autonomous: true
requirements: [WR-05]

must_haves:
  truths:
    - "ruff check on tests/unit/test_async_tools.py reports 0 errors"
    - "ruff check on tests/staleness/tool_invoker.py reports 0 errors"
    - "ruff check on src/db/metadata.py reports 0 errors"
    - "get_table_schema FK serialization yields target_schema=None on Databricks/generic dialects when referred_schema is None"
    - "get_table_schema FK serialization preserves target_schema='dbo' on MSSQL when referred_schema is None"
    - "Full pytest suite stays green and coverage stays >= 85%"
  artifacts:
    - path: "src/db/metadata.py"
      provides: "Dialect-aware FK target_schema fallback (WR-05)"
      contains: "self._dialect.default_schema if self._dialect else None"
    - path: "tests/unit/test_metadata.py"
      provides: "Regression test for dialect-aware FK schema fallback"
  key_links:
    - from: "src/db/metadata.py get_table_schema"
      to: "self._dialect.default_schema"
      via: "FK target_schema fallback expression"
      pattern: "referred_schema.*self\\._dialect\\.default_schema"
---

<objective>
v2.1 tech-debt cleanup: three low-risk fixes, each landing as its own atomic commit.

Task A — Hoist E402 mid-file imports in tests/unit/test_async_tools.py into the top import block.
Task B — Resolve three ruff findings (I001, F841, UP017) in tests/staleness/tool_invoker.py.
Task C — Fix WR-05: dialect-aware FK target_schema fallback in src/db/metadata.py so Databricks/generic output no longer stamps the MSSQL default "dbo" when SQLAlchemy returns referred_schema=None. Implemented TDD (failing regression test first).

Purpose: Clear lint debt and correct a dialect-correctness bug that mislabels FK target schemas on non-MSSQL backends.
Output: Clean ruff on all three files, a new regression test, and a full green suite at >= 85% coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Verified during planning. Executor should use these directly. -->

Dialect default_schema values (verified):
- MssqlDialect.default_schema  -> "dbo"   (src/db/dialects/mssql.py:63)
- GenericDialect.default_schema -> None   (src/db/dialects/generic.py:65)
- DatabricksDialect.default_schema -> None (src/db/dialects/databricks.py:105)

MetadataService construction (verified in src/db/metadata.py and tests/unit/test_metadata.py):
- `MetadataService(engine, dialect=<DialectStrategy | None>)`  (metadata.py:45)
- The dialect is stored on `self._dialect`.
- get_table_schema lives at metadata.py:849; the FK serialization block is at lines 913-924.

Current FK block (src/db/metadata.py:913-924):
```
if include_relationships:
    fks = self.get_foreign_keys(table_name, schema_name)
    result["foreign_keys"] = [
        {
            "constraint_name": fk.get("name"),
            "source_columns": fk.get("constrained_columns", []),
            "target_schema": fk.get("referred_schema", "dbo"),   # <- line 919, bug
            "target_table": fk.get("referred_table"),
            "target_columns": fk.get("referred_columns", []),
        }
        for fk in fks
    ]
```

Existing test patterns (tests/unit/test_metadata.py):
- Inspector injection: build `MetadataService(test_engine, dialect=dialect)`, then set
  `service._inspector = mock_insp` where `mock_insp = MagicMock()` and configure
  `mock_insp.get_foreign_keys.return_value = [...]`. (mirrors _build_metadata_service:1231-1240
  and the get_foreign_keys error test at 562-575)
- Databricks get_table_schema additionally calls DESCRIBE EXTENDED — short-circuit with
  `patch.object(service, "_parse_databricks_table_properties", return_value={})` and
  `patch.object(service, "_engine_catalog", return_value="main")` (see test at 1197-1209).
- For dialects, prefer REAL dialect instances (MssqlDialect, GenericDialect/DatabricksDialect)
  over the MagicMock helpers `_make_databricks_dialect`/`_make_generic_dialect` (lines 595-611):
  those mocks do NOT define `default_schema`, so a MagicMock dialect would return a truthy
  MagicMock for `self._dialect.default_schema` and defeat the test. Import real dialects from
  src.db.dialects (mssql / databricks / generic).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task A: Hoist E402 mid-file imports in test_async_tools.py</name>
  <files>tests/unit/test_async_tools.py</files>
  <action>
    Two module-level imports sit mid-file (lines 281-282), directly after the
    "Error classification wiring tests" comment banner:
      `import pytest`
      `from sqlalchemy.exc import SQLAlchemyError`
    Move both into the top-of-file import block (currently lines 9-26). Preserve
    ruff/isort ordering: `import pytest` belongs in the third-party group (it can join
    or sit near the `from datetime import datetime` / stdlib grouping per isort rules —
    let `ruff check --fix` settle final ordering if needed). `from sqlalchemy.exc import
    SQLAlchemyError` belongs with the other third-party `from src.*`/sqlalchemy imports.
    After adding them at the top, DELETE the now-redundant mid-file occurrence (the two
    lines after the comment banner). Leave the comment banner itself intact.
    Do NOT touch src/metrics.py (out of scope).
  </action>
  <verify>
    <automated>uv run ruff check tests/unit/test_async_tools.py</automated>
  </verify>
  <done>ruff check on tests/unit/test_async_tools.py reports 0 errors (E402 resolved); imports appear only once, at top of file. Commit atomically: `style(quick-v61): hoist E402 imports in test_async_tools.py`</done>
</task>

<task type="auto">
  <name>Task B: Ruff cleanup in tests/staleness/tool_invoker.py</name>
  <files>tests/staleness/tool_invoker.py</files>
  <action>
    Three ruff findings:
      - I001: unsorted import block (top of file, ~line 8). ruff --fix handles this.
      - UP017: `datetime.timezone.utc` -> `datetime.UTC` at line 288
        (`sampled_at=datetime(2026, 1, 1, tzinfo=timezone.utc)`). ruff --fix handles this;
        note the file imports `from datetime import datetime, timezone` at line 9 — after
        the autofix, ruff will rewrite the usage and may adjust the import. Let ruff settle it.
      - F841: unused binding `mock_engine` at line 122:
        `patch.object(get_connection_manager(), "get_engine") as mock_engine,`
        Manual fix: drop the `as mock_engine` clause ONLY. The `patch.object(...)` call MUST
        remain inside the `with (...)` block so get_engine stays mocked — just remove the
        unused name binding.
    Approach: run `uv run ruff check --fix tests/staleness/tool_invoker.py` first (clears
    I001 + UP017), then manually remove the `as mock_engine` binding for F841.
    Do NOT remove the patch.object call itself. Do NOT touch src/metrics.py.
  </action>
  <verify>
    <automated>uv run ruff check tests/staleness/tool_invoker.py</automated>
  </verify>
  <done>ruff check on tests/staleness/tool_invoker.py reports 0 errors (I001, F841, UP017 all resolved); get_engine remains patched (patch.object call intact, only the binding removed). Commit atomically: `style(quick-v61): resolve I001/F841/UP017 in tool_invoker.py`</done>
</task>

<task type="auto" tdd="true">
  <name>Task C: WR-05 dialect-aware FK schema fallback (TDD)</name>
  <files>src/db/metadata.py, tests/unit/test_metadata.py</files>
  <behavior>
    - Test 1 (Databricks/generic): get_table_schema with include_relationships=True and a
      mocked inspector returning one FK with referred_schema=None, using a REAL
      DatabricksDialect (or GenericDialect) instance, must produce
      foreign_keys[0]["target_schema"] == None (NOT "dbo").
    - Test 2 (MSSQL regression preserved): same setup with a REAL MssqlDialect instance and
      referred_schema=None must produce foreign_keys[0]["target_schema"] == "dbo".
  </behavior>
  <action>
    RED first: add the regression test(s) to tests/unit/test_metadata.py BEFORE editing
    metadata.py. Mirror existing patterns:
      - Construct `service = MetadataService(test_engine, dialect=<real dialect>)`.
      - Inject a mock inspector: `mock_insp = MagicMock()`,
        `mock_insp.get_foreign_keys.return_value = [{"name": "fk1",
          "constrained_columns": ["other_id"], "referred_schema": None,
          "referred_table": "other", "referred_columns": ["id"]}]`,
        plus minimal `get_columns`/`get_pk_constraint` returns and (for databricks)
        the DESCRIBE EXTENDED short-circuit patches noted in <interfaces>.
        Set `service._inspector = mock_insp`.
      - Call `service.get_table_schema("t", "main", ... )` with include_relationships=True
        (confirm the param name/signature at metadata.py:849 during execution).
      - Assert target_schema per the two cases above.
      Use REAL dialect classes imported from src.db.dialects (mssql/databricks/generic) — the
      existing MagicMock dialect helpers do not define default_schema and would invalidate the test.
    Run the new test(s) and confirm they FAIL (red) against current code.

    GREEN: change src/db/metadata.py line 919 from
      `"target_schema": fk.get("referred_schema", "dbo"),`
    to
      `"target_schema": fk.get("referred_schema") or (self._dialect.default_schema if self._dialect else None),`
    Confirm the attribute name on the instance is `self._dialect` (verified at metadata.py:45).
    Re-run the new test(s) — must pass (green). Do NOT touch src/metrics.py.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_metadata.py -q && uv run ruff check src/db/metadata.py</automated>
  </verify>
  <done>New regression test fails before the fix and passes after; target_schema is None on Databricks/generic and "dbo" on MSSQL when referred_schema is None; ruff clean on src/db/metadata.py. Commit atomically: `fix(quick-v61): dialect-aware FK target_schema fallback (WR-05)`</done>
</task>

</tasks>

<verification>
Full-suite gate after all three tasks (run once at end):
- `uv run pytest -q` — green, 0 failures.
- Coverage stays >= 85% (no regression).
- `uv run ruff check tests/unit/test_async_tools.py tests/staleness/tool_invoker.py src/db/metadata.py` — 0 errors across all three.
</verification>

<success_criteria>
- Three atomic commits land (A: style hoist, B: style ruff cleanup, C: fix WR-05).
- ruff clean on all three modified files.
- New FK-schema regression test present and green; MSSQL "dbo" behavior preserved.
- Full pytest suite green at >= 85% coverage.
- src/metrics.py untouched (pre-existing Generator warning out of scope).
</success_criteria>

<output>
Create `.planning/quick/260528-v61-v2-1-tech-debt-cleanup-lint-fixes-and-di/260528-v61-SUMMARY.md` when done.
</output>
