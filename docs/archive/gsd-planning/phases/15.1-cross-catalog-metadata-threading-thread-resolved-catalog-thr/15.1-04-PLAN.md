---
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
plan: 04
type: tdd
wave: 2
depends_on: ["15.1-01"]
files_modified:
  - src/analysis/column_stats.py
  - tests/unit/test_column_stats.py
autonomous: true
requirements: [IDENT-08]
must_haves:
  truths:
    - "ColumnStatsCollector accepts an optional catalog arg; aggregate stat queries target that catalog via a 3-part _qualified_table."
    - "The DESCRIBE EXTENDED fast path (_try_describe_extended_stats) is 3-part when catalog is set (Pitfall 5) — not 2-part."
    - "column_exists / get_columns_by_pattern reflect from the requested catalog on Databricks."
    - "Default-catalog path unchanged; no USE CATALOG emitted."
  artifacts:
    - path: "src/analysis/column_stats.py"
      provides: "Catalog-aware column statistics (aggregate + DESCRIBE EXTENDED fast path)"
      contains: "catalog"
    - path: "tests/unit/test_column_stats.py"
      provides: "Cross-catalog aggregate + fast-path tests (catalog-discriminating fake)"
      contains: "cross_catalog"
  key_links:
    - from: "src/analysis/column_stats.py _qualified_table"
      to: "transpile_query"
      via: "3-part TSQL bracket name transpiled to Databricks backticks"
      pattern: "_qualified_table"
    - from: "src/analysis/column_stats.py _try_describe_extended_stats"
      to: "dialect.quote_identifier (3-part native backticks)"
      via: "catalog-scoped DESCRIBE EXTENDED fast path"
      pattern: "_try_describe_extended_stats"
---

<objective>
Thread `resolved.catalog` into `ColumnStatsCollector`. Column stats has BOTH an aggregate-SQL
path (transpiled, fixed by a 3-part `_qualified_table`) AND a DESCRIBE-EXTENDED fast path
(native backticks, currently 2-part — Pitfall 5). Both must be catalog-scoped, plus the
existence/pattern column reads.

Purpose: Eliminate CR-02 for the get_column_info column-statistics path.
Output: catalog-aware ColumnStatsCollector + catalog-discriminating tests for both paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-PATTERNS.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-01-SUMMARY.md

<interfaces>
<!-- From src/analysis/_sql.py (Plan 01): CatalogAwareReflector + transpile_query (see Plan 02). -->
<!-- Current ColumnStatsCollector.__init__ (lines 58-81): -->
<!--   (connection, schema_name, table_name, dialect=None, inspector=None) -->
<!--   self._qualified_table = f"[{schema_name}].[{table_name}]"  (line 81, 2-part TSQL) -->
<!-- Aggregate queries interpolate _qualified_table: lines 214, 242, 285, 291, 324, 341. -->
<!-- Catalog-blind reads: column_exists line 86; get_columns_by_pattern lines 119/154/512. -->
<!-- DESCRIBE EXTENDED fast path (Pitfall 5) _try_describe_extended_stats line 367: -->
<!--   qualified_table = f"{qi(schema)}.{qi(table)}"  (2-part, native backticks, NOT transpiled). -->
<!--   This path bypasses transpile_query (consistent with get_sample_data 130-139). -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — cross-catalog aggregate + DESCRIBE-EXTENDED-fast-path + no-USE-CATALOG tests</name>
  <read_first>
    - src/analysis/column_stats.py (full — constructor 58-81; aggregate query sites 214/242/285/291/324/341; column_exists 86; get_columns_by_pattern 119/154/512; _try_describe_extended_stats around line 367)
    - tests/unit/test_column_stats.py (existing mock patterns — catalog-blind)
    - tests/conftest.py lines 49-69 (real `dialect` fixture)
    - tests/unit/test_metadata.py lines 595-669 (catalog-asserting pattern)
    - src/db/query.py lines 123-139 (get_sample_data — 3-part native-backtick reference for the fast path)
    - src/analysis/_sql.py CatalogAwareReflector (Plan 01)
  </read_first>
  <behavior>
    - test_cross_catalog_aggregate_targets_catalog: ColumnStatsCollector(..., catalog="cerner_src"); a catalog-discriminating fake returns aggregate rows ONLY when the SQL carries `cerner_src`.`dbo`.`orders`; assert stats populated (not the empty/default-catalog result).
    - test_qualified_table_three_part: _qualified_table == "[cerner_src].[dbo].[orders]" and sqlglot transpiles it to 3-part backticks.
    - test_describe_extended_fast_path_three_part (Pitfall 5): the DESCRIBE EXTENDED SQL emitted by _try_describe_extended_stats contains `cerner_src`.`dbo`.`orders` (3-part native backticks), NOT a 2-part name.
    - test_column_exists_cross_catalog: column_exists reflects from the requested catalog.
    - test_no_use_catalog: no "USE CATALOG" in any executed SQL.
    - test_default_path_unchanged: catalog=None keeps 2-part _qualified_table + Inspector column_exists + 2-part fast path.
  </behavior>
  <action>
    Extend tests/unit/test_column_stats.py with a TestCrossCatalogColumnStats class using a catalog-discriminating fake connection (asserts no USE CATALOG; returns rows only for the resolved-catalog 3-part name, for both the transpiled aggregate form and the native-backtick fast-path form). Use real `dialect` fixture. Tests MUST fail (no catalog kwarg). Do not implement.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_column_stats.py -k "cross_catalog or qualified_table or describe_extended_fast_path or column_exists_cross or no_use_catalog" -x -q 2>&1 | grep -qE "(TypeError|unexpected keyword|FAILED|error)" &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>New cross-catalog column-stats tests exist and fail because ColumnStatsCollector has no catalog kwarg (RED).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — thread catalog through ColumnStatsCollector (both paths)</name>
  <read_first>
    - src/analysis/column_stats.py (file being modified)
    - tests/unit/test_column_stats.py (RED tests — contract)
    - src/db/query.py lines 123-139 (3-part native-backtick fast-path reference)
    - src/analysis/_sql.py CatalogAwareReflector (Plan 01)
  </read_first>
  <action>
    Add `catalog: str | None = None` to ColumnStatsCollector.__init__; store `self._catalog`. Build `self._qualified_table = f"[{catalog}].[{schema_name}].[{table_name}]"` when catalog set, else existing 2-part (TSQL brackets — fixes all aggregate sites at 214/242/285/291/324/341 via transpile_query). In _try_describe_extended_stats (line 367), build a 3-part native-backtick name `f"{qi(self._catalog)}.{qi(self.schema_name)}.{qi(self.table_name)}"` when self._catalog is set, else the existing 2-part (this path is native Databricks SQL, NOT transpiled — Pitfall 5; mirror get_sample_data). On the cross-catalog Databricks branch, re-route column_exists / get_columns_by_pattern Inspector reads (86/119/154/512) to CatalogAwareReflector.reflect_columns. Default-catalog branch unchanged. Never emit USE CATALOG.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_column_stats.py -x -q</automated>
  </verify>
  <done>All test_column_stats.py tests pass (new + regression). Both aggregate and DESCRIBE-EXTENDED paths 3-part on cross-catalog. `grep -v '^#' src/analysis/column_stats.py | grep -c "USE CATALOG"` == 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool args -> raw SQL | catalog/schema/table -> transpiled aggregate SQL + native-backtick DESCRIBE EXTENDED. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-09 | Tampering (SQLi) | DESCRIBE EXTENDED fast path (native backticks, no bound params) | mitigate | Every segment quoted via dialect.quote_identifier (T-mwr-02); fast-path test asserts quoted 3-part form. |
| T-15.1-10 | Tampering (SQLi) | aggregate SQL _qualified_table | mitigate | TSQL brackets transpiled via sqlglot; segments are identifiers not concatenated raw user SQL. |
| T-15.1-11 | Tampering / Info Disclosure | pooled connection catalog state | mitigate | No USE CATALOG on either path (stateless 3-part); test_no_use_catalog + grep gate. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_column_stats.py -q` green.
- `uv run ruff check src/analysis/column_stats.py` clean.
- `grep -v '^#' src/analysis/column_stats.py | grep -c "USE CATALOG"` == 0.
</verification>

<success_criteria>
get_column_info column-statistics with a non-default Databricks catalog targets that catalog
on BOTH the aggregate and DESCRIBE-EXTENDED fast paths; default path unchanged; no USE CATALOG.
</success_criteria>

<output>
Create `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-04-SUMMARY.md` when done.
</output>
