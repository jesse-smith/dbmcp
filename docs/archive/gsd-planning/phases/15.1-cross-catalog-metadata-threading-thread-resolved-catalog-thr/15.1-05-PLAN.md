---
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
plan: 05
type: tdd
wave: 3
depends_on: ["15.1-02", "15.1-03", "15.1-04"]
files_modified:
  - src/mcp_server/analysis_tools.py
  - tests/unit/test_async_tools.py
autonomous: true
requirements: [IDENT-08]
must_haves:
  truths:
    - "find_pk_candidates and find_fk_candidates compute the cross_catalog flag and pass catalog=resolved.catalog into their analysis class when cross-catalog Databricks; get_column_info threads catalog into ColumnStatsCollector."
    - "find_pk_candidates and find_fk_candidates use the catalog-aware MetadataService.table_exists existence check on the cross-catalog branch (mirroring get_column_info)."
    - "Default path and MSSQL/generic gate unchanged — resolver still rejects catalog on non-Databricks; no tool passes catalog on the default branch."
  artifacts:
    - path: "src/mcp_server/analysis_tools.py"
      provides: "Cross-catalog wiring for all three analysis tools at the MCP boundary"
      contains: "cross_catalog"
    - path: "tests/unit/test_async_tools.py"
      provides: "Tool-boundary tests asserting catalog threads into each analysis class + cross-catalog existence check"
      contains: "catalog"
  key_links:
    - from: "src/mcp_server/analysis_tools.py find_pk_candidates"
      to: "PKDiscovery(catalog=...)"
      via: "catalog passed on cross-catalog branch"
      pattern: "catalog=resolved.catalog"
    - from: "src/mcp_server/analysis_tools.py find_fk_candidates"
      to: "FKCandidateSearch(catalog=...)"
      via: "catalog passed on cross-catalog branch"
      pattern: "catalog="
    - from: "src/mcp_server/analysis_tools.py find_pk_candidates/find_fk_candidates"
      to: "MetadataService.table_exists(catalog=...)"
      via: "catalog-aware existence check on cross-catalog branch"
      pattern: "table_exists"
---

<objective>
Wire the catalog through the three tool entry points so `resolved.catalog` actually reaches
the analysis classes. `get_column_info` already has the catalog-aware existence check
(lines 119-131) — extend it to pass catalog into `ColumnStatsCollector`. `find_pk_candidates`
and `find_fk_candidates` need BOTH the cross-catalog existence-check branch (their existence
checks at lines 259-266 / 400-410 are Inspector-only, catalog-blind) AND the catalog passed
into their analysis class.

Purpose: Connect the Wave 2 catalog-aware analysis classes to the MCP boundary so the
explicit `catalog` arg has end-to-end effect (closes CR-02 at the integration seam).
Output: catalog-wired analysis_tools.py + boundary tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-PATTERNS.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-02-SUMMARY.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-03-SUMMARY.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-04-SUMMARY.md

<interfaces>
<!-- Analysis classes now accept catalog= (Plans 02/03/04): -->
<!--   PKDiscovery(connection, schema_name, table_name, dialect, inspector, catalog=None) -->
<!--   FKCandidateSearch(connection, source_schema, source_table, source_column, -->
<!--                     source_data_type, dialect, inspector, catalog=None) -->
<!--   ColumnStatsCollector(connection, schema_name, table_name, dialect, inspector, catalog=None) -->
<!-- Existing working template — get_column_info._sync_work lines 119-131: -->
<!--   if resolved.catalog and dialect is not None and dialect.name == "databricks": -->
<!--       MetadataService(engine, dialect=dialect).table_exists(table, schema, catalog=resolved.catalog) -->
<!--   else: Inspector existence check. -->
<!-- find_pk_candidates._sync_work: existence check 259-266 (Inspector-only); PKDiscovery 268-274. -->
<!-- find_fk_candidates._sync_work: existence check 400-410 (Inspector + source-col read 411); -->
<!--   FKCandidateSearch construction ~423. -->
<!-- resolve_identifier (src/db/identifiers.py) GATES catalog on MSSQL/generic — UNCHANGED. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — boundary tests assert catalog threads into each tool</name>
  <read_first>
    - src/mcp_server/analysis_tools.py (full _sync_work for get_column_info ~100-163, find_pk_candidates ~242-279, find_fk_candidates ~360-431)
    - tests/unit/test_async_tools.py lines 22-24 (imports of the three tools) and the existing tool-call test fixtures/mock-connection-manager pattern
    - tests/conftest.py lines 49-69 (real dialect fixture)
  </read_first>
  <behavior>
    - test_find_pk_candidates_threads_catalog: call find_pk_candidates with catalog="cerner_src" on a databricks connection; assert PKDiscovery was constructed with catalog="cerner_src" (spy/patch) AND MetadataService.table_exists was called with catalog="cerner_src" (not the Inspector existence path).
    - test_find_fk_candidates_threads_catalog: same for FKCandidateSearch + catalog-aware existence check.
    - test_get_column_info_threads_catalog: assert ColumnStatsCollector constructed with catalog="cerner_src".
    - test_default_path_no_catalog: with catalog=None, all three construct their class with catalog=None and use the Inspector existence path (no MetadataService.table_exists).
    - test_mssql_catalog_rejected_unchanged: catalog on MSSQL still returns an error response (resolver gate untouched) for all three tools.
  </behavior>
  <action>
    Extend tests/unit/test_async_tools.py with cross-catalog boundary tests for all three tools. Patch PKDiscovery / FKCandidateSearch / ColumnStatsCollector and MetadataService.table_exists to capture constructor/call kwargs. Reuse the existing connection-manager mocking pattern in the file. Tests MUST fail now because find_pk_candidates/find_fk_candidates do not yet pass catalog or use the cross-catalog existence check. Do not implement.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_async_tools.py -k "threads_catalog or default_path_no_catalog or mssql_catalog_rejected" -x -q 2>&1 | grep -qE "(FAILED|AssertionError|error)" &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>New boundary tests exist and fail because catalog is not yet threaded at the tool boundary (RED).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — wire catalog + cross-catalog existence check into all three tools</name>
  <read_first>
    - src/mcp_server/analysis_tools.py (file being modified)
    - tests/unit/test_async_tools.py (RED tests — contract)
  </read_first>
  <action>
    In each of the three _sync_work functions, after resolve_identifier compute once:
    `cross_catalog = bool(resolved.catalog) and dialect is not None and dialect.name == "databricks"`.
    get_column_info: pass `catalog=resolved.catalog if cross_catalog else None` into ColumnStatsCollector (line ~143); existence check already correct.
    find_pk_candidates: wrap the existence check (259-266) so when cross_catalog, use MetadataService(engine, dialect=dialect).table_exists(resolved_table, resolved_schema, catalog=resolved.catalog) (mirror get_column_info 119-131) else the existing Inspector path; pass `catalog=resolved.catalog if cross_catalog else None` into PKDiscovery (268-274).
    find_fk_candidates: same existence-check wrap (400-410); on the cross_catalog branch the source-column type read at line 411 must use the catalog-aware reflection (CatalogAwareReflector or FKCandidateSearch's internal path) rather than inspector.get_columns; pass `catalog=resolved.catalog if cross_catalog else None` into FKCandidateSearch (~423).
    Do NOT touch resolve_identifier or the MSSQL/generic gate. Default branch behavior is byte-for-byte unchanged. Update the stale docstring notes in find_pk_candidates/find_fk_candidates that claim the Inspector binds to the default catalog (they now support cross-catalog).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_async_tools.py -x -q</automated>
  </verify>
  <done>All test_async_tools.py tests pass. Catalog threads into all three analysis classes on the cross-catalog branch; cross-catalog existence check used for pk/fk; default + MSSQL-gate paths unchanged. `grep -v '^#' src/mcp_server/analysis_tools.py | grep -c "USE CATALOG"` == 0.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + coverage gate</name>
  <read_first>
    - .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md (Validation Architecture — Sampling Rate / Phase gate)
  </read_first>
  <action>
    Run the full unit suite and coverage to confirm no regression across the three analysis classes + tool boundary + the rest of the project, and that the 85% coverage floor holds (currently 91%). Run ruff over all modified source. Fix any regression before marking done. No watch mode.
  </action>
  <verify>
    <automated>uv run pytest --tb=short -q &amp;&amp; uv run pytest --cov=src --cov-fail-under=85 -q &amp;&amp; uv run ruff check src/analysis/ src/mcp_server/analysis_tools.py src/analysis/_sql.py</automated>
  </verify>
  <done>Full suite green; coverage >= 85%; ruff clean on all modified source. No MSSQL/generic regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client -> tool -> resolver -> analysis class | catalog arg crosses from untrusted client through the resolver gate into catalog-scoped SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-12 | Elevation / Access Control | catalog gate bypass on MSSQL/generic | mitigate | resolve_identifier still gates catalog on non-Databricks (untouched); test_mssql_catalog_rejected_unchanged asserts the error response persists for all three tools. |
| T-15.1-13 | Information Disclosure | cross-catalog reach via tool boundary | accept | Existence + reflection run as connected principal; Unity Catalog ACLs enforce; no new exposure beyond get_column_info's existing cross-catalog table_exists. |
| T-15.1-14 | Tampering | pooled connection catalog state | mitigate | Delegated to stateless analysis-class SQL (Plans 02-04); grep gate confirms no USE CATALOG at the boundary. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_async_tools.py -q` green.
- Full suite `uv run pytest --tb=short -q` green; `--cov-fail-under=85` passes.
- `uv run ruff check src/analysis/ src/mcp_server/analysis_tools.py` clean.
</verification>

<success_criteria>
All three tools thread the resolved catalog end-to-end on the Databricks cross-catalog branch;
pk/fk use the catalog-aware existence check; default path + MSSQL/generic gate unchanged;
full suite green at >= 85% coverage.
</success_criteria>

<output>
Create `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-05-SUMMARY.md` when done.
</output>
