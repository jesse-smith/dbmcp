---
status: complete
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
source: [15.1-01-SUMMARY.md, 15.1-02-SUMMARY.md, 15.1-03-SUMMARY.md, 15.1-04-SUMMARY.md, 15.1-05-SUMMARY.md]
started: 2026-05-29T00:00:00Z
updated: 2026-05-29T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. get_column_info — cross-catalog override targets cerner_src
expected: get_column_info(table_name=abstract_data, schema_name=v500, catalog=cerner_src) returns column statistics computed against the cerner_src table — not bmtct, not a not-found error. Proves the catalog threads into both the DESCRIBE fast-path and the aggregate-stats SQL (Plan 04 + Plan 05 wiring).
result: pass
evidence: "get_column_info(catalog='cerner_src', table_name='abstract_data', schema_name='v500', columns=[ABSTRACT_DATA_ID, PERSON_ID]) → status:success. ABSTRACT_DATA_ID: total_rows 4,302,614 / distinct 4,302,614 / 0 null. PERSON_ID: distinct 31,520 / 0 null. Real cerner_src.v500.abstract_data statistics returned. (Same call against the pre-merge stale server returned 'No such table abstract_data' — the IDENT-08 bug — confirming this is a true before/after.)"

### 2. get_column_info — negative control (no override → bmtct default)
expected: The same get_column_info call WITHOUT the catalog override targets the connection default (bmtct) and fails to find the cerner_src-only schema, proving the override is what changed the target.
result: pass
evidence: "get_column_info(table_name='abstract_data', schema_name='v500', no catalog) → status:error '[SCHEMA_NOT_FOUND] The schema `bmtct`.`v500` cannot be found' [SQL: SHOW TABLES FROM `bmtct`.`v500`]. Default path correctly targets bmtct; v500 is a cerner_src schema, so not-found is the expected negative-control result."

### 3. find_pk_candidates — cross-catalog override targets cerner_src
expected: find_pk_candidates(table_name=abstract_data, schema_name=v500, catalog=cerner_src) reflects the cerner_src table and runs its structural uniqueness probe against cerner_src data. (type_filter widened to ["double"] because every column is double/timestamp/string — no integer columns.)
result: pass
evidence: "find_pk_candidates(catalog='cerner_src', table_name='abstract_data', schema_name='v500', type_filter=['double']) → status:success, candidates[1]: ABSTRACT_DATA_ID (double, is_unique:true, is_non_null:true, is_pk_type:true). The uniqueness probe ran against the 4.3M-row cerner_src table — 4,302,614 distinct / 0 null matches Test 1's get_column_info stats exactly, proving same-catalog same-data. Default type_filter returned candidates[0] (no integer columns), still status:success (reached the table)."

### 4. find_pk_candidates — negative control (no override → bmtct default)
expected: Same call without the override targets bmtct and returns not-found.
result: pass
evidence: "find_pk_candidates(table_name='abstract_data', schema_name='v500', type_filter=['double'], no catalog) → status:error '[SCHEMA_NOT_FOUND] The schema `bmtct`.`v500` cannot be found' [SQL: SHOW TABLES FROM `bmtct`.`v500`]. Default path targets bmtct."

### 5. find_fk_candidates — cross-catalog override targets cerner_src (multi-table)
expected: find_fk_candidates(table_name=abstract_data, column_name=PERSON_ID, schema_name=v500, catalog=cerner_src) discovers candidate target tables/columns FROM cerner_src. This is the strongest test — FK search is multi-table, so the target universe must be enumerated via SHOW TABLES IN cerner_src.v500 and each target reflected from cerner_src (Plan 03).
result: pass
evidence: "find_fk_candidates(catalog='cerner_src', table_name='abstract_data', column_name='PERSON_ID', schema_name='v500', target_table_pattern='person', pk_candidates_only=false, limit=10) → status:success. Top candidate: PERSON_ID → cerner_src.v500.person.PERSON_ID. total_found: 101 (was_limited:true). All targets resolved in cerner_src.v500 (the person table + columns), enumerated via catalog-scoped SHOW TABLES IN. Multi-table catalog threading confirmed."

### 6. find_fk_candidates — negative control (no override → bmtct default)
expected: Same call without the override targets bmtct and returns not-found.
result: pass
evidence: "find_fk_candidates(table_name='abstract_data', column_name='PERSON_ID', schema_name='v500', target_table_pattern='person', pk_candidates_only=false, no catalog) → status:error '[SCHEMA_NOT_FOUND] The schema `bmtct`.`v500` cannot be found' [SQL: SHOW TABLES FROM `bmtct`.`v500`]. Default path targets bmtct."

### 7. No USE CATALOG / no stale-catalog bleed on the pooled connection (T-15.1-16)
expected: Across repeated alternating calls on the SAME pooled connection, override calls hit cerner_src and no-override calls hit bmtct, with no cross-contamination — proving the catalog is threaded into each statement, not set globally via USE CATALOG.
result: pass
evidence: "All 6 tool calls above ran sequentially on one pooled connection (connection_id d9ce935f5dbb). Override calls (Tests 1, 3, 5) targeted cerner_src; immediately-following no-override calls (Tests 2, 4, 6) targeted bmtct. No stale-catalog bleed in either direction across the alternation. Each statement carries its own 3-part-qualified target; no USE CATALOG emitted (corroborated by the USE CATALOG source-grep gate == 0 across all Wave 1-4 plans). T-15.1-16 (pooled-connection catalog tampering) mitigated."

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — IDENT-08 cross-catalog metadata threading verified end-to-end against a live Databricks warehouse for all three analysis tools]

## Verification Note (2026-05-29)

All 7 tests run live via the `dbmcp-test` MCP server against a real Databricks Unity Catalog metastore:
- Connection: `databricks-test`, default catalog `bmtct`, second catalog `cerner_src` (connection_id d9ce935f5dbb)
- Test table: `cerner_src.v500.abstract_data` (4,302,614 rows; natural PK `ABSTRACT_DATA_ID`; FK column `PERSON_ID` → `cerner_src.v500.person`)

Confirms **IDENT-08** (cross-catalog introspection targets the requested catalog) end-to-end for all three analysis tools:
- **get_column_info** — Tests 1, 2 (both DESCRIBE fast-path and aggregate-stats SQL, Plan 04)
- **find_pk_candidates** — Tests 3, 4 (catalog-scoped reflection + structural uniqueness probe, Plan 02)
- **find_fk_candidates** — Tests 5, 6 (multi-table target enumeration via SHOW TABLES IN catalog.schema, Plan 03)
- **Tool-layer wiring** — `resolved.catalog` threads from the three MCP tool entry points into each collector constructor (Plan 05)
- **Stateless catalog targeting** — Test 7 (no USE CATALOG; no pooled-connection catalog bleed; T-15.1-16 mitigated)

Closes RESEARCH assumption A2 (live multi-catalog confirmation). The override demonstrably changes the target catalog; the negative control proves the default (bmtct) path is unchanged. Before the dbmcp-test server reloaded the merged Wave 1-4 code, the override probe reproduced the pre-fix IDENT-08 bug (override accepted, default catalog queried → not-found) — a true before/after.
