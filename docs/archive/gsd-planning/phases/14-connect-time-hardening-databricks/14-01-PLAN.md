---
phase: 14-connect-time-hardening-databricks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/dialects/databricks.py
autonomous: true
requirements:
  - IDENT-01
must_haves:
  truths:
    - "DatabricksDialect.create_engine raises ValueError('Databricks catalog is required') when catalog kwarg is missing or empty"
    - "The 'main' string no longer appears as a default fallback anywhere in src/db/dialects/databricks.py"
    - "DatabricksDialect.list_catalogs(engine) returns the list of catalog names from SHOW CATALOGS"
  artifacts:
    - path: "src/db/dialects/databricks.py"
      provides: "Catalog-required guard in create_engine, removal of 'main' defaults, and new list_catalogs method"
      contains: "def list_catalogs"
  key_links:
    - from: "src/db/dialects/databricks.py::create_engine"
      to: "ValueError('Databricks catalog is required')"
      via: "empty-string check on catalog kwarg"
      pattern: "raise ValueError.*Databricks catalog is required"
    - from: "src/db/dialects/databricks.py::list_catalogs"
      to: "SHOW CATALOGS SQL text"
      via: "sqlalchemy.text execution"
      pattern: "SHOW CATALOGS"
---

<objective>
Harden `DatabricksDialect` at the dialect layer: remove both hidden `"main"` catalog fallbacks
(D-02), add a `ValueError` guard for missing/empty catalog in `create_engine` (D-01/D-03), and
add a new `list_catalogs(engine) -> list[str]` method for the connect-layer helper to call
(D-08). Keep `create_engine` IO-free — the probe/SHOW CATALOGS execution runs only inside the
new `list_catalogs` method.

Purpose: Satisfies the dialect-tier portion of IDENT-01. After this plan, any Databricks
engine constructed without a catalog fails fast with a deterministic `ValueError`; the
connect-layer helper (Plan 02) catches that error and enriches it.

Output: Updated `src/db/dialects/databricks.py` with two `"main"` defaults removed, one guard
added, and one new public method.
</objective>

<execution_context>
@/Users/jsmith79/.claude/plugins/cache/gsd-plugin/gsd/2.40.1/workflows/execute-plan.md
@/Users/jsmith79/.claude/plugins/cache/gsd-plugin/gsd/2.40.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-connect-time-hardening-databricks/14-CONTEXT.md
@.planning/phases/14-connect-time-hardening-databricks/14-RESEARCH.md
@src/db/dialects/databricks.py

<interfaces>
Current relevant shapes in src/db/dialects/databricks.py (verify on read):

- `_kwargs_from_url(url)` at ~line 104-180: parses `databricks://` URL. Line 149 currently:
  `catalog = query.get("catalog", "main")` — REMOVE the "main" default.
- `create_engine(**kwargs) -> Engine` at ~line 182-265. Existing pattern at lines ~233-236
  validates host/http_path presence with ValueError. Line 239 currently:
  `catalog: str = kwargs.get("catalog", "main")` — REMOVE the "main" default and add guard.
- No `list_catalogs` method exists yet. Add it as a new public method on `DatabricksDialect`.

SQL execution pattern (already used elsewhere in codebase):
```python
from sqlalchemy import text
with engine.connect() as conn:
    rows = conn.execute(text("SHOW CATALOGS")).fetchall()
return [row[0] for row in rows]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove "main" fallbacks and add catalog guard in create_engine</name>
  <files>src/db/dialects/databricks.py</files>
  <read_first>
    - src/db/dialects/databricks.py (full file — focus lines 100-270)
    - .planning/phases/14-connect-time-hardening-databricks/14-CONTEXT.md (decisions D-01, D-02, D-03)
  </read_first>
  <behavior>
    - Given `create_engine(host=..., http_path=..., token=..., catalog="", schema="default")`
      then: raises `ValueError("Databricks catalog is required")`.
    - Given `create_engine(...)` with `catalog` kwarg absent: raises the same ValueError.
    - Given `create_engine(...)` with `catalog=None`: raises the same ValueError (None is falsy).
    - Given `create_engine(...)` with `catalog="main"` explicitly passed: succeeds (no
      implicit rejection of "main" — only missing/empty is rejected).
    - Given `_kwargs_from_url("databricks://host/?http_path=/x&token=t")` (no `?catalog=`):
      returned kwargs dict has `catalog=""` (empty string, NOT `"main"`).
    - Given `_kwargs_from_url("databricks://host/?http_path=/x&token=t&catalog=my_cat")`:
      returned kwargs dict has `catalog="my_cat"`.
  </behavior>
  <action>
Implement per D-01, D-02, D-03:

1. At line 149 in `_kwargs_from_url`, change:
   ```python
   catalog = query.get("catalog", "main")
   ```
   to:
   ```python
   catalog = query.get("catalog", "")
   ```

2. At line 239 in `create_engine`, change:
   ```python
   catalog: str = kwargs.get("catalog", "main")
   ```
   to:
   ```python
   catalog: str = kwargs.get("catalog", "") or ""
   if not catalog:
       raise ValueError("Databricks catalog is required")
   ```
   (The `or ""` guards against an explicit `catalog=None` that would otherwise bypass the
   empty-string check only.)

3. Do NOT touch the `schema` default `"default"` at line ~240 — schema default is Databricks-
   meaningful and out of scope (per D-02 discussion in CONTEXT).

4. Do NOT touch the existing `host` / `http_path` / `token` validation — those stay as-is;
   the new `catalog` guard follows the same ValueError style for consistency.

5. Write tests in `tests/unit/test_databricks_dialect.py` (create if missing) covering the
   six behaviors above. Use exact error string match `"Databricks catalog is required"`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_databricks_dialect.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"main"' src/db/dialects/databricks.py` returns ZERO matches (no more "main"
      string literals in the file).
    - `grep -n 'Databricks catalog is required' src/db/dialects/databricks.py` returns exactly
      1 match inside `create_engine`.
    - `grep -n 'query.get("catalog"' src/db/dialects/databricks.py` shows only the
      `query.get("catalog", "")` form.
    - `grep -n 'kwargs.get("catalog"' src/db/dialects/databricks.py` shows only the
      `kwargs.get("catalog", "")` form.
    - New test file `tests/unit/test_databricks_dialect.py` exists and all test cases pass.
  </acceptance_criteria>
  <done>
    Two "main" defaults removed. ValueError guard present in create_engine. Dialect-level
    tests for empty/missing/None catalog all pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DatabricksDialect.list_catalogs method</name>
  <files>src/db/dialects/databricks.py, tests/unit/test_databricks_dialect.py</files>
  <read_first>
    - src/db/dialects/databricks.py (review DatabricksDialect class shape after Task 1 edits)
    - .planning/phases/14-connect-time-hardening-databricks/14-CONTEXT.md (decision D-08)
    - .planning/phases/14-connect-time-hardening-databricks/14-RESEARCH.md (§"SHOW CATALOGS — command shape and failure modes" and §"D-08: list_catalogs dialect method")
  </read_first>
  <behavior>
    - Given an engine whose `conn.execute(text("SHOW CATALOGS")).fetchall()` returns
      `[("main",), ("hive_metastore",), ("samples",)]`, `list_catalogs(engine)` returns
      `["main", "hive_metastore", "samples"]` (list of strings, order preserved).
    - Given `fetchall()` returns `[]`, `list_catalogs(engine)` returns `[]`.
    - Given `conn.execute` raises `SQLAlchemyError("boom")`, `list_catalogs` lets the
      SQLAlchemyError propagate (the connect-layer helper in Plan 02 catches it — dialect is
      NOT responsible for wrapping).
  </behavior>
  <action>
Implement per D-08 and the code example in 14-RESEARCH.md. Add a new public method to the
`DatabricksDialect` class:

```python
def list_catalogs(self, engine: Engine) -> list[str]:
    """Return catalog names visible to the connected principal via SHOW CATALOGS.

    Used by the connect-time helper that enriches the catalog-required error
    (IDENT-01), and by future DISC-01 tooling. Lets SQLAlchemyError propagate —
    callers decide how to wrap.
    """
    from sqlalchemy import text

    with engine.connect() as conn:
        rows = conn.execute(text("SHOW CATALOGS")).fetchall()
    return [row[0] for row in rows]
```

Place the method alongside other dialect-specific query helpers in the class (typically
after `create_engine` or near other `text()`-using helpers — follow local file order).

Import `Engine` from `sqlalchemy.engine` at the top of the file if not already imported
(typing only — it is already used elsewhere in the file; likely already imported).

Add three test cases to `tests/unit/test_databricks_dialect.py`:
1. `test_list_catalogs_returns_row_zero_values` — MagicMock engine with `conn.execute`
   returning `fetchall=[("main",), ("hive_metastore",)]`, assert result is
   `["main", "hive_metastore"]`.
2. `test_list_catalogs_returns_empty_list_when_no_rows` — `fetchall=[]`, assert `[]`.
3. `test_list_catalogs_propagates_sqlalchemy_error` — `conn.execute.side_effect =
   SQLAlchemyError("boom")`, assert `pytest.raises(SQLAlchemyError)`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_databricks_dialect.py::test_list_catalogs_returns_row_zero_values tests/unit/test_databricks_dialect.py::test_list_catalogs_returns_empty_list_when_no_rows tests/unit/test_databricks_dialect.py::test_list_catalogs_propagates_sqlalchemy_error -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def list_catalogs" src/db/dialects/databricks.py` returns exactly 1 match
      inside the `DatabricksDialect` class.
    - `grep -n "SHOW CATALOGS" src/db/dialects/databricks.py` returns exactly 1 match (inside
      the new method).
    - The method signature is `def list_catalogs(self, engine: Engine) -> list[str]:` (exact).
    - All three new tests pass.
    - No try/except around `conn.execute` inside `list_catalogs` (errors propagate per D-08
      intent).
  </acceptance_criteria>
  <done>
    `DatabricksDialect.list_catalogs(engine) -> list[str]` exists, executes `SHOW CATALOGS`
    via `text()`, returns `row[0]` list, lets SQLAlchemyError propagate. Three tests pass.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `uv run pytest tests/unit/test_databricks_dialect.py -x -v` — all pass
- `uv run pytest tests/ -x -m "not integration"` — no regressions in existing dialect tests
- `grep -c '"main"' src/db/dialects/databricks.py` — returns 0
</verification>

<success_criteria>
- Dialect-layer IDENT-01 guard in place: empty/missing catalog → ValueError
- Both "main" fallbacks (lines 149, 239) removed
- `list_catalogs` method exists with contract specified in D-08
- No regressions in existing tests
</success_criteria>

<output>
Create `.planning/phases/14-connect-time-hardening-databricks/14-01-SUMMARY.md` describing:
- Exact lines edited (149, 239, plus new method location)
- Exact ValueError string used
- Signature of new `list_catalogs` method
- Test file created / extended with test names
- Any discretionary decisions (placement of new method, import organization)
</output>
