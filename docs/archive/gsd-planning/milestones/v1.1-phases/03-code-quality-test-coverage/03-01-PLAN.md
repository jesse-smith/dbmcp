---
phase: 03-code-quality-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/metrics.py
  - src/models/schema.py
  - src/db/query.py
  - pyproject.toml
autonomous: true
requirements: [QUAL-01, QUAL-03]

must_haves:
  truths:
    - "src/metrics.py does not exist on disk"
    - "No Python file in src/ or tests/ imports from src.metrics"
    - "query.py has zero type: ignore comments"
    - "pyright reports no errors on src/db/query.py"
  artifacts:
    - path: "src/models/schema.py"
      provides: "Query dataclass with columns, rows, total_rows_available fields"
      contains: "total_rows_available"
    - path: "src/db/query.py"
      provides: "Query result population without type: ignore"
  key_links:
    - from: "src/db/query.py"
      to: "src/models/schema.py"
      via: "Query dataclass fields"
      pattern: "query\\.columns|query\\.rows|query\\.total_rows_available"
---

<objective>
Remove dead code and fix type suppressions to make the codebase honest.

Purpose: QUAL-01 eliminates unused metrics.py (0% coverage drag). QUAL-03 replaces monkey-patched attributes on the Query dataclass with proper fields, enabling type checker validation.
Output: Cleaner codebase with no dead imports and zero type: ignore in query.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-quality-test-coverage/03-RESEARCH.md

@src/metrics.py
@src/models/schema.py
@src/db/query.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete metrics.py and clean up references</name>
  <files>src/metrics.py</files>
  <action>
1. Delete `src/metrics.py` entirely. No archiving, no skeleton -- per user decision, the module is disposable.
2. Grep the entire project for any remaining references to `metrics` as a module import (e.g., `from src.metrics`, `import src.metrics`, `from src import metrics`). The research found zero imports outside metrics.py itself, but verify.
3. Check `tests/performance/__init__.py` which mentions "Performance metrics logging" in comments -- update or remove the comment if it references the deleted module. Do NOT delete performance test files (they test NFRs, not the metrics module).
4. Run `uv run pytest -m "not integration" -x -q` to confirm nothing breaks.
5. Run `uv run ruff check src/` to confirm no lint issues.
  </action>
  <verify>
    <automated>test -f src/metrics.py && echo "FAIL: metrics.py still exists" || echo "PASS: metrics.py deleted" && grep -rn "from src.metrics\|import src.metrics\|from src import metrics" src/ tests/ && echo "FAIL: references remain" || echo "PASS: no references" && uv run pytest -m "not integration" -x -q</automated>
  </verify>
  <done>src/metrics.py deleted, no import references anywhere in codebase, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix type: ignore suppressions in query.py</name>
  <files>src/models/schema.py, src/db/query.py, pyproject.toml</files>
  <behavior>
    - Test: Query dataclass accepts columns, rows, total_rows_available as constructor args with defaults
    - Test: Query().columns returns empty list by default
    - Test: Query().rows returns empty list by default
    - Test: Query().total_rows_available returns None by default
    - Test: Existing Query construction (without new fields) still works
    - Test: execute_query populates query.columns, query.rows, query.total_rows_available
    - Test: get_query_results reads from query.columns, query.rows, query.total_rows_available
  </behavior>
  <action>
1. Add pyright to dev dependencies: `uv add --group dev pyright`
2. In `src/models/schema.py`, add three new fields to the `Query` dataclass AFTER existing fields (all with defaults so existing construction is not broken):
   ```python
   columns: list[str] = field(default_factory=list)
   rows: list[dict[str, Any]] = field(default_factory=list)
   total_rows_available: int | None = None
   ```
3. In `src/db/query.py`, replace the three `# type: ignore` lines (546-548):
   - `query._columns = columns` -> `query.columns = columns`
   - `query._rows = rows` -> `query.rows = rows`
   - `query._total_rows_available = total_rows_available` -> `query.total_rows_available = total_rows_available`
4. In `src/db/query.py`, update `get_query_results()` (around lines 716-718) to read from new field names:
   - `getattr(query, '_columns', [])` -> `query.columns`
   - `getattr(query, '_rows', [])` -> `query.rows`
   - `getattr(query, '_total_rows_available', None)` -> `query.total_rows_available`
5. Run `uv run pyright src/db/query.py` to confirm zero errors/suppressions.
6. Run full unit test suite: `uv run pytest -m "not integration" -x -q`
7. Run `uv run ruff check src/`

PITFALL: New dataclass fields MUST have defaults (field(default_factory=list) or None) and MUST come AFTER existing fields with defaults. Otherwise existing Query() construction breaks with TypeError.
  </action>
  <verify>
    <automated>grep -n "type: ignore" src/db/query.py && echo "FAIL: type ignores remain" || echo "PASS: no type ignores" && uv run pyright src/db/query.py && uv run pytest -m "not integration" -x -q</automated>
  </verify>
  <done>Zero type: ignore comments in query.py, pyright reports no errors on query.py, all existing tests pass</done>
</task>

</tasks>

<verification>
- `src/metrics.py` does not exist
- `grep -rn "from src.metrics\|import src.metrics" src/ tests/` returns nothing
- `grep -n "type: ignore" src/db/query.py` returns nothing
- `uv run pyright src/db/query.py` exits 0
- `uv run pytest -m "not integration" -x -q` all pass
- `uv run ruff check src/` clean
</verification>

<success_criteria>
- metrics.py deleted with no orphaned references
- Query dataclass has proper typed fields for columns, rows, total_rows_available
- pyright validates query.py with zero suppressions
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-test-coverage/03-01-SUMMARY.md`
</output>
