---
phase: 03-code-quality-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/metadata.py
  - src/db/query.py
  - src/db/connection.py
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "No except Exception: blocks remain in src/db/ or src/analysis/"
    - "MCP tool safety nets (9 blocks in src/mcp_server/) are unchanged"
    - "Error messages include exception type context for debugging"
    - "All existing tests still pass after narrowing"
  artifacts:
    - path: "src/db/metadata.py"
      provides: "12 narrowed exception handlers using SQLAlchemyError"
      contains: "SQLAlchemyError"
    - path: "src/db/query.py"
      provides: "3 narrowed exception handlers using SQLAlchemyError"
      contains: "SQLAlchemyError"
    - path: "src/db/connection.py"
      provides: "1 narrowed exception handler using SQLAlchemyError"
      contains: "SQLAlchemyError"
  key_links:
    - from: "src/db/metadata.py"
      to: "sqlalchemy.exc"
      via: "import SQLAlchemyError"
      pattern: "from sqlalchemy.exc import SQLAlchemyError"
    - from: "src/db/query.py"
      to: "sqlalchemy.exc"
      via: "import SQLAlchemyError"
      pattern: "from sqlalchemy.exc import SQLAlchemyError"
---

<objective>
Narrow all broad exception catches in service/db layer to specific SQLAlchemy exception types.

Purpose: QUAL-02 makes error handling honest -- catching only database-related exceptions rather than swallowing everything. If a non-database error (e.g., TypeError, KeyError) occurs, it should propagate rather than be silently caught.
Output: 16 except blocks narrowed from Exception to SQLAlchemyError (or more specific types where appropriate).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-quality-test-coverage/03-RESEARCH.md

@src/db/metadata.py
@src/db/query.py
@src/db/connection.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow exception handlers in metadata.py (12 blocks)</name>
  <files>src/db/metadata.py</files>
  <action>
Add `from sqlalchemy.exc import SQLAlchemyError` to metadata.py imports.

Narrow all 12 `except Exception` blocks to `except SQLAlchemyError`. Per user decision: SQLAlchemy hierarchy only, do NOT also catch pyodbc (SQLAlchemy wraps pyodbc errors; if raw pyodbc leaks, that is worth knowing about). No new custom exception classes.

**Category 1 -- Inspector operations (6 blocks):**
- Line 130: `except Exception:` -> `except SQLAlchemyError:` (get_schema_names fallback)
- Line 145: `except Exception:` -> `except SQLAlchemyError:` (get_schema_names MSSQL fallback)
- Line 290: `except Exception:` -> `except SQLAlchemyError:` (get_table_comment fallback)
- Line 314: `except Exception:` -> `except SQLAlchemyError:` (inspector.get_columns fallback)
- Line 330: `except Exception:` -> `except SQLAlchemyError:` (column data_type extraction)
- Line 712: `except Exception:` -> `except SQLAlchemyError:` (table_exists fallback)

**Category 2 -- Data/query operations (6 blocks):**
- Line 372: `except Exception as e:` -> `except SQLAlchemyError as e:` (row count). Also add `{type(e).__name__}: ` prefix to the debug log message for better diagnostics.
- Line 555: `except Exception as e:` -> `except SQLAlchemyError as e:` (get_indexes). Add `{type(e).__name__}: ` to log message.
- Line 591: `except Exception as e:` -> `except SQLAlchemyError as e:` (get_indexes secondary). Add type name to log.
- Line 608: `except Exception as e:` -> `except SQLAlchemyError as e:` (get_primary_key). Add type name to log.
- Line 624: `except Exception as e:` -> `except SQLAlchemyError as e:` (get_primary_key secondary). Add type name to log.

For each block, improve error messages per user decision: include `{type(e).__name__}` where there is an `as e` clause to provide exception type context for debugging. Where blocks just use `pass` or return a fallback, the narrowing alone is sufficient.

Run `uv run pytest -m "not integration" -x -q` after changes. If any test fails, investigate -- tests that mock with `Exception` may need updating to use `SQLAlchemyError`.
  </action>
  <verify>
    <automated>grep -n "except Exception" src/db/metadata.py && echo "FAIL: broad exceptions remain" || echo "PASS: all narrowed" && uv run pytest -m "not integration" -x -q</automated>
  </verify>
  <done>All 12 except Exception blocks in metadata.py replaced with SQLAlchemyError, error messages include type context, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Narrow exception handlers in query.py (3 blocks) and connection.py (1 block)</name>
  <files>src/db/query.py, src/db/connection.py</files>
  <action>
**query.py -- 3 blocks:**

Add `from sqlalchemy.exc import SQLAlchemyError` to query.py imports (if not already present).

- Line 130: `except Exception as e:` -> `except SQLAlchemyError as e:` (get_sample_data). Update log message to include `{type(e).__name__}`.
- Line 616: `except Exception as e:` -> `except SQLAlchemyError as e:` (_run_query). The error message already includes type name -- verify and keep.
- Line 686: `except Exception:` -> `except SQLAlchemyError:` (_get_total_row_count silent catch).

NOTE: There is also `except Exception` in `src/metrics.py` line 166, but Plan 01 deletes that file. If Plan 01 runs first it will be gone. If not, ignore it -- it will be deleted.

**connection.py -- 1 block:**

Add `from sqlalchemy.exc import SQLAlchemyError` to connection.py imports (if not already present).

- Line 145: `except Exception as e:` -> `except SQLAlchemyError as e:`. The existing error handling already wraps into ConnectionError with context -- keep that pattern but add `{type(e).__name__}` to the raised message if not already present.

**MCP tool safety nets -- DO NOT TOUCH:**
- analysis_tools.py: lines 136, 236, 399 (keep as-is)
- query_tools.py: lines 110, 194 (keep as-is)
- schema_tools.py: lines 172, 230, 335, 421 (keep as-is)
Per user decision, only the 9 MCP `@mcp.tool()` entry points keep `except Exception:` as safety nets.

**Verification after changes:**
Run `uv run pytest -m "not integration" -x -q`. If tests mock exceptions as `Exception` and now the handler only catches `SQLAlchemyError`, update test mocks to use `SQLAlchemyError` (or a subclass like `OperationalError`).

Run `uv run ruff check src/` to confirm no unused imports or lint issues.

Final grep check: `grep -rn "except Exception" src/ | grep -v mcp_server/` should return ZERO lines (after metrics.py deletion by Plan 01; if metrics.py still exists, it will show 1 line which is acceptable).
  </action>
  <verify>
    <automated>grep -n "except Exception" src/db/query.py src/db/connection.py && echo "FAIL: broad exceptions remain" || echo "PASS: all narrowed" && uv run pytest -m "not integration" -x -q && uv run ruff check src/</automated>
  </verify>
  <done>All 4 except Exception blocks in query.py and connection.py replaced with SQLAlchemyError, MCP tool safety nets untouched, all tests pass, ruff clean</done>
</task>

</tasks>

<verification>
- `grep -rn "except Exception" src/ | grep -v mcp_server/` returns zero lines (excluding metrics.py if still present)
- `grep -c "except Exception" src/mcp_server/*.py` shows exactly 9 (safety nets preserved)
- `uv run pytest -m "not integration" -x -q` all pass
- `uv run ruff check src/` clean
</verification>

<success_criteria>
- Zero broad except Exception: blocks outside MCP tool safety nets
- 9 MCP tool safety nets preserved unchanged
- Error messages include exception type context where applicable
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-test-coverage/03-02-SUMMARY.md`
</output>
