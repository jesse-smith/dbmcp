---
phase: 07-wire-orphaned-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/query.py
  - tests/unit/test_query.py
autonomous: true
requirements:
  - INFRA-02
must_haves:
  truths:
    - "Setting text_truncation_limit=500 in config causes query result strings longer than 500 chars to be truncated"
    - "Default text_truncation_limit=1000 preserves strings shorter than 1000 chars"
    - "Config value is read at call time, not cached at module load"
  artifacts:
    - path: "src/db/query.py"
      provides: "Config-driven truncation at both call sites"
      contains: "get_config().defaults.text_truncation_limit"
    - path: "tests/unit/test_query.py"
      provides: "Truncation config tests"
      contains: "text_truncation_limit"
  key_links:
    - from: "src/db/query.py"
      to: "src/config.py"
      via: "get_config().defaults.text_truncation_limit inline call"
      pattern: "get_config\\(\\)\\.defaults\\.text_truncation_limit"
---

<objective>
Plumb text_truncation_limit from TOML config into query.py truncation call sites

Purpose: The config field `text_truncation_limit` exists in AppConfig (Phase 6) but query.py still hardcodes `1000`. This plan completes the INFRA-02 circuit so the config value actually controls truncation behavior.

Output: query.py reads truncation limit from config at two call sites; unit tests verify config flows through.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/db/query.py
@src/config.py
@src/type_registry.py
@tests/unit/test_query.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/config.py:
```python
def get_config() -> AppConfig:  # line 280, singleton accessor

@dataclass(frozen=True)
class Defaults:
    text_truncation_limit: int = 1000  # line 47
```

From src/type_registry.py:
```python
def convert(value: object, truncation_limit: int) -> tuple[object, bool]:  # line 120
```

From src/db/query.py (current hardcoded sites):
```python
# Line 333 in _process_result_rows:
truncated_value, was_truncated = convert(value, 1000)

# Line 667 in _process_sp_results:
truncated_value, _ = convert(value, 1000)
```

Established pattern (from query_tools.py):
```python
config = get_config()
sample_size = config.defaults.sample_size
row_limit = config.defaults.row_limit
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add truncation config tests to test_query.py</name>
  <files>tests/unit/test_query.py</files>
  <behavior>
    - Test 1: With mocked get_config returning text_truncation_limit=500, a 700-char string value in query results is truncated (was_truncated=True, length <= 500+suffix)
    - Test 2: With mocked get_config returning text_truncation_limit=1000, a 700-char string value in query results is NOT truncated (was_truncated=False, full length preserved)
  </behavior>
  <action>
    Add two test functions to tests/unit/test_query.py that test _process_result_rows truncation behavior:

    1. `test_truncation_uses_config_limit_500`: Patch `src.db.query.get_config` to return a mock with `defaults.text_truncation_limit = 500`. Create a QueryService instance (mocked engine/metadata_service), call `_process_result_rows` with a row containing a 700-char string. Assert the value is truncated.

    2. `test_truncation_default_limit_preserves_short_strings`: Same setup but with limit=1000. Assert the 700-char string is NOT truncated.

    Note: `get_config` import does not yet exist in query.py -- tests will fail (RED phase). That's expected; Task 2 adds the import and wiring.

    Use `unittest.mock.patch` targeting `src.db.query.get_config` (the module where it will be imported, not src.config).
  </action>
  <verify>
    <automated>cd /Users/jsmith79/Documents/Projects/Ongoing/dbmcp && uv run pytest tests/unit/test_query.py -x -k "truncation_config or truncation_limit" -q 2>&1 | tail -5</automated>
  </verify>
  <done>Two truncation config tests exist. They will fail until Task 2 wires the config import (RED phase of TDD).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded 1000 with config-driven truncation limit</name>
  <files>src/db/query.py</files>
  <action>
    In src/db/query.py:

    1. Add import: `from src.config import get_config` (near top, with other src imports)
    2. Line 333 in `_process_result_rows`: Replace `convert(value, 1000)` with `convert(value, get_config().defaults.text_truncation_limit)`
    3. Line 667 in `_process_sp_results`: Same replacement -- `convert(value, 1000)` becomes `convert(value, get_config().defaults.text_truncation_limit)`

    No other changes needed. No API changes to QueryService. The `get_config()` call is inline at each site (not cached) so runtime config changes take effect immediately. This matches the established pattern in query_tools.py.

    IMPORTANT: Do NOT cache get_config() at module level or in __init__. Call it inline at each truncation site.
  </action>
  <verify>
    <automated>cd /Users/jsmith79/Documents/Projects/Ongoing/dbmcp && uv run pytest tests/unit/test_query.py -x -k "truncation_config or truncation_limit" -q && uv run pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Both hardcoded `1000` values replaced with `get_config().defaults.text_truncation_limit`. Truncation config tests pass (GREEN). Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `grep -n "convert(value, 1000)" src/db/query.py` returns no results (hardcoded values gone)
2. `grep -n "get_config().defaults.text_truncation_limit" src/db/query.py` returns 2 matches (lines ~333 and ~667)
3. `uv run pytest tests/unit/test_query.py -x -k "truncation" -q` passes
4. `uv run pytest tests/ -x -q` full suite passes
</verification>

<success_criteria>
- query.py reads text_truncation_limit from config at both truncation call sites
- No hardcoded `1000` remains for truncation in query.py
- Unit tests verify that changing the config value changes truncation behavior
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/07-wire-orphaned-exports/07-01-SUMMARY.md`
</output>
