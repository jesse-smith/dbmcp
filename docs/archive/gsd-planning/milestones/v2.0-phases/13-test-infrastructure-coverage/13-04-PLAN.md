---
phase: 13
plan: 04
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - pyproject.toml
autonomous: true
requirements: [TEST-03]
tags: [coverage, pyproject, ratchet]

must_haves:
  truths:
    - "pyproject.toml `[tool.coverage.report].fail_under` equals `85` (previously `70`)."
    - "`uv run pytest --cov=src` exits 0 with total coverage ≥ 85% (measured baseline 89.11%)."
    - "This change lands AFTER all Wave 2 migrations are merged and passing (strict ordering to avoid transient coverage drops)."
  artifacts:
    - path: "pyproject.toml"
      provides: "Coverage floor ratchet from 70 to 85"
      contains: "fail_under = 85"
  key_links:
    - from: "pyproject.toml [tool.coverage.report]"
      to: "pytest-cov enforcement"
      via: "fail_under threshold check at end of coverage run"
      pattern: "fail_under = 85"
---

<objective>
Raise `[tool.coverage.report].fail_under` in pyproject.toml from `70` to `85`. Sequenced strictly last — after Plans 02 and 03 migrate tests and pass — to prevent transient coverage drops during the refactor from tripping the floor mid-wave.

Purpose: Close the TEST-03 gap with a meaningful ratchet. Current measured coverage is 89.11%; 85% leaves ~4 points of headroom for normal churn while blocking a ~19-point silent regression that the current 70% floor permits. Per D-11 / D-12 / D-13: no per-module floors, no CI integration — single global knob.

Output: One-line edit to pyproject.toml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-test-infrastructure-coverage/13-CONTEXT.md
@.planning/phases/13-test-infrastructure-coverage/13-RESEARCH.md
@.planning/phases/13-test-infrastructure-coverage/13-PATTERNS.md
@.planning/phases/13-test-infrastructure-coverage/13-02-SUMMARY.md
@.planning/phases/13-test-infrastructure-coverage/13-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- No new interfaces. Single pyproject.toml edit. -->

Existing pyproject.toml section (line 101 per PATTERNS.md):
```toml
[tool.coverage.report]
fail_under = 70
```

Target state:
```toml
[tool.coverage.report]
fail_under = 85
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify current coverage is ≥ 85%, then raise fail_under from 70 to 85 in pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current [tool.coverage.report] block — line 101 per PATTERNS.md)
    - .planning/phases/13-test-infrastructure-coverage/13-RESEARCH.md §6 (Coverage Ratchet rationale), Risk #5 (must land LAST)
    - .planning/phases/13-test-infrastructure-coverage/13-CONTEXT.md D-11, D-12, D-13
    - .planning/phases/13-02-SUMMARY.md and 13-03-SUMMARY.md (confirm Plan 02 and Plan 03 completed and passed)
  </read_first>
  <action>
1. PRE-FLIGHT: Confirm Plans 02 and 03 completed successfully. Check for `.planning/phases/13-test-infrastructure-coverage/13-02-SUMMARY.md` and `13-03-SUMMARY.md` existence. If either is missing, STOP — the plan cannot land before its dependencies.

2. Measure current coverage BEFORE editing pyproject.toml:
   ```bash
   uv run pytest --cov=src --cov-report=term 2>&1 | tail -20
   ```
   Capture the "TOTAL" coverage percentage from the output. If it is < 85%, STOP — do not raise the floor below the current measurement. Report the actual percentage and halt for operator review.

3. If coverage ≥ 85%, edit pyproject.toml:
   - Find the line containing exactly `fail_under = 70` inside the `[tool.coverage.report]` section.
   - Replace with `fail_under = 85`.
   - Preserve all surrounding formatting, whitespace, and other lines in the section verbatim.

4. Do NOT add per-module floors (D-12). Do NOT add CI config (D-13). Do NOT modify any other pyproject.toml section.

5. Verify POST-EDIT:
   ```bash
   uv run pytest --cov=src 2>&1 | tail -3
   ```
   Must exit 0 (total coverage ≥ 85%).

Scope reminder:
- NO changes to src/
- NO changes to tests/
- NO changes to any pyproject.toml section other than `[tool.coverage.report].fail_under`
- NO new dependencies
- NO new files
  </action>
  <verify>
    <automated>grep -c "fail_under = 85" pyproject.toml; grep -c "fail_under = 70" pyproject.toml; uv run pytest --cov=src 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fail_under = 85" pyproject.toml` returns `1`
    - `grep -c "fail_under = 70" pyproject.toml` returns `0`
    - `uv run pytest --cov=src` exits 0 (total coverage ≥ 85%)
    - `git diff pyproject.toml` shows exactly one line changed: `fail_under = 70` → `fail_under = 85`
    - No other pyproject.toml sections modified
    - `.planning/phases/13-test-infrastructure-coverage/13-02-SUMMARY.md` and `13-03-SUMMARY.md` exist (pre-flight check passed)
  </acceptance_criteria>
  <done>
    - Coverage floor raised from 70 to 85 in pyproject.toml.
    - Full pytest+cov run passes with the new floor.
    - Single-line change; no other file modifications.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — configuration change to test coverage threshold only. No user input, no network I/O, no production code paths modified, no new runtime dependencies, no secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | N/A | coverage config | accept | Test infrastructure, no production code paths modified; no new external I/O; no new dependencies. |
</threat_model>

<verification>
- `uv run pytest --cov=src` exits 0.
- `grep "fail_under" pyproject.toml` shows exactly `fail_under = 85`.
- Coverage measurement after the edit is ≥ 85% (expected ~89%+ given baseline).
</verification>

<success_criteria>
- pyproject.toml `[tool.coverage.report].fail_under` is `85`.
- Full pytest+cov run passes (exit 0).
- Plan landed strictly after Plans 02 and 03 (enforced by `depends_on`).
- No other file changes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-infrastructure-coverage/13-04-SUMMARY.md`.
</output>
