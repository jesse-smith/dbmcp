---
phase: 01-atomic-toon-migration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/serialization.py
  - tests/helpers.py
  - tests/unit/test_serialization.py
  - tests/unit/test_helpers.py
autonomous: true
requirements:
  - SRLZ-01
  - SRLZ-02
  - SRLZ-04
  - TEST-01

must_haves:
  truths:
    - "toon-format library is installed and importable"
    - "encode_response() converts a dict to a TOON string"
    - "encode_response() pre-serializes datetime to isoformat strings"
    - "encode_response() pre-serializes StrEnum to string values"
    - "encode_response() raises TypeError on unrecognized types"
    - "parse_tool_response() decodes a TOON string back to a dict"
  artifacts:
    - path: "pyproject.toml"
      provides: "toon-format git dependency"
      contains: "toon-format"
    - path: "src/serialization.py"
      provides: "TOON encoding wrapper with pre-serialization"
      exports: ["encode_response"]
      min_lines: 25
    - path: "tests/helpers.py"
      provides: "Test deserialization helper"
      exports: ["parse_tool_response"]
      min_lines: 8
    - path: "tests/unit/test_serialization.py"
      provides: "Unit tests for serialization wrapper"
      min_lines: 40
    - path: "tests/unit/test_helpers.py"
      provides: "Unit tests for test helper"
      min_lines: 15
  key_links:
    - from: "src/serialization.py"
      to: "toon_format.encode"
      via: "import and call"
      pattern: "from toon_format import encode"
    - from: "tests/helpers.py"
      to: "toon_format.decode"
      via: "import and call"
      pattern: "from toon_format import decode"
    - from: "src/serialization.py"
      to: "_pre_serialize"
      via: "internal function call"
      pattern: "encode\\(_pre_serialize"
---

<objective>
Install the toon-format dependency, build the serialization wrapper module with pre-serialization, and create the test helper -- all TDD.

Purpose: These are the foundational building blocks that Plan 02 (the atomic tool swap) depends on. Without encode_response() and parse_tool_response(), the tool migration cannot proceed.

Output: `src/serialization.py` (encode_response), `tests/helpers.py` (parse_tool_response), both with full unit test coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-atomic-toon-migration/01-CONTEXT.md
@.planning/phases/01-atomic-toon-migration/01-RESEARCH.md

@pyproject.toml
@src/models/schema.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install toon-format and build serialization wrapper (TDD)</name>
  <files>pyproject.toml, src/serialization.py, tests/unit/test_serialization.py</files>
  <behavior>
    - test_toon_import: `import toon_format` succeeds, `toon_format.encode` and `toon_format.decode` are callable
    - TestPreSerialize:
      - Primitives (None, bool, int, float, str) pass through unchanged
      - dict values are recursively pre-serialized
      - list values are recursively pre-serialized
      - tuple values are converted to lists (preserving order, not sorted)
      - datetime -> .isoformat() string
      - date -> .isoformat() string
      - StrEnum -> str(value) (e.g., AuthenticationMethod.SQL_LOGIN -> "sql")
      - Unknown type (e.g., a custom class instance) -> raises TypeError with type name in message
      - Nested structure: dict containing list of dicts with datetime and StrEnum values
    - TestEncodeResponse:
      - Simple dict {"status": "success"} returns a non-empty string
      - Returned string is NOT valid JSON (does not start with '{')
      - Roundtrip: toon_format.decode(encode_response(data)) == data (for primitive-only dict)
      - Roundtrip with pre-serialization: dict containing datetime produces decodable TOON with isoformat string
      - Roundtrip with StrEnum: dict containing StrEnum value produces decodable TOON with string value
      - TypeError propagates: dict containing unknown type raises TypeError
  </behavior>
  <action>
    RED phase:
    1. Add toon-format git dependency to pyproject.toml: `"toon-format @ git+https://github.com/toon-format/toon-python.git@v0.9.0-beta.1"` in the dependencies list
    2. Run `uv sync` to install
    3. Create `tests/unit/test_serialization.py` with all tests from behavior spec above. Import from `src.serialization import encode_response, _pre_serialize`. Tests MUST fail (module doesn't exist yet).
    4. Run tests -- confirm RED.

    GREEN phase:
    5. Create `src/serialization.py` with:
       - `_pre_serialize(value)`: recursive walker handling None/bool/int/float/str passthrough, dict recursion, list/tuple recursion (tuple -> list), datetime/date -> .isoformat(), StrEnum -> str(value), Decimal -> float (defensive), unknown -> raise TypeError
       - `encode_response(data: dict) -> str`: calls `encode(_pre_serialize(data))`
       - Import: `from toon_format import encode`
       - Import: `from datetime import datetime, date` and `from enum import StrEnum` and `from decimal import Decimal`
    6. Run tests -- confirm GREEN.

    REFACTOR phase (if needed):
    7. Clean up, ensure `uv run ruff check src/serialization.py` passes.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_serialization.py -x -v</automated>
  </verify>
  <done>
    - toon-format is importable via `uv run python -c "import toon_format"`
    - encode_response() produces TOON strings from dicts
    - _pre_serialize() converts datetime, date, StrEnum, Decimal; raises TypeError on unknowns
    - All test cases green, ruff clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build test helper parse_tool_response (TDD)</name>
  <files>tests/helpers.py, tests/unit/test_helpers.py</files>
  <behavior>
    - test_parse_simple_dict: parse_tool_response(encode_response({"status": "success"})) == {"status": "success"}
    - test_parse_nested_dict: roundtrip with nested structures preserves shape
    - test_parse_non_dict_raises: if TOON string decodes to a non-dict (e.g., a list), raises ValueError
    - test_parse_invalid_toon_raises: malformed TOON string raises an exception (from toon_format.decode)
  </behavior>
  <action>
    RED phase:
    1. Create `tests/unit/test_helpers.py` with tests from behavior spec. Import `from tests.helpers import parse_tool_response` and `from src.serialization import encode_response`. Tests MUST fail (helpers.py doesn't exist yet).
    2. Run tests -- confirm RED.

    GREEN phase:
    3. Create `tests/helpers.py` with:
       - `from toon_format import decode`
       - `def parse_tool_response(response: str) -> dict:` -- calls decode(response), checks isinstance(result, dict), raises ValueError if not, returns result
    4. Run tests -- confirm GREEN.

    Run full test suite to confirm no regressions:
    5. `uv run pytest tests/ -x -q` -- all 385+ existing tests must still pass (they don't use helpers.py yet)
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_helpers.py tests/unit/test_serialization.py -x -v && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    - parse_tool_response() decodes TOON strings to dicts
    - ValueError on non-dict TOON payloads
    - Full existing test suite passes (no regressions)
    - ruff clean on tests/helpers.py
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_serialization.py tests/unit/test_helpers.py -v` -- all new unit tests green
- `uv run pytest tests/ -x -q` -- full suite green (no regressions)
- `uv run ruff check src/serialization.py tests/helpers.py` -- clean
- `uv run python -c "from src.serialization import encode_response; print(encode_response({'status': 'success'}))"` -- prints TOON string
</verification>

<success_criteria>
- toon-format installed and importable
- src/serialization.py exports encode_response() with recursive pre-serialization
- tests/helpers.py exports parse_tool_response()
- Both have comprehensive unit tests
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-toon-migration/01-01-SUMMARY.md`
</output>
