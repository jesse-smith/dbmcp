---
phase: quick-260505-mr3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md
autonomous: true
requirements:
  - AUDIT-DBX-TESTS-Q1  # Is there integration-style test covering connect_with_config → dialect.create_engine for Databricks?
  - AUDIT-DBX-TESTS-Q2  # If yes, why did it not catch the signature mismatch (commit 4fdaa90)?
  - AUDIT-DBX-TESTS-Q3  # Env-var substitution tests for Databricks host/http_path?

must_haves:
  truths:
    - "Reader can see, per audit question, a yes/no answer with file:line citations"
    - "Reader can identify which Databricks code paths are covered end-to-end vs mock-only"
    - "Reader can decide whether the coverage gap is a blocker for further Databricks work"
    - "Proposed integration-test names/locations exist for each identified gap"
  artifacts:
    - path: ".planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md"
      provides: "Audit findings with citations, gap list, verdict, follow-up recommendation"
      contains: "## Verdict"
  key_links:
    - from: "AUDIT.md § Q1/Q2"
      to: "tests/unit/**/*databricks*"
      via: "file:line citations"
      pattern: "tests/.*:[0-9]+"
    - from: "AUDIT.md § Gaps"
      to: "src/db/connection.py:376-433 + src/db/dialects/databricks.py:60-107"
      via: "call-path trace: connect_with_config → connect_with_url → dialect.create_engine(sqlalchemy_url=…)"
      pattern: "connect_with_config|connect_with_url|create_engine"
---

<objective>
Audit Databricks test coverage to explain why the `DatabricksDialect.create_engine`
signature drift (fixed in commit 4fdaa90, quick-260428-jr7) was not caught by the
607-test suite at Phase 11 merge time. Produce a single deliverable audit note
answering the three questions from the source todo with file:line citations,
and issue a blocker-or-not verdict for subsequent Databricks work.

Purpose: Close the todo at `.planning/todos/pending/2026-04-28-audit-databricks-test-coverage.md`
with a defensible written answer — not a code change. Any test additions are
explicitly optional and scope-gated in Task 3.

Output: `.planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/2026-04-28-audit-databricks-test-coverage.md
@./CLAUDE.md

<!-- Source code under audit (read ONLY these ranges; do not re-read whole files) -->
@src/db/connection.py  (lines 376-470 — connect_with_config Databricks branch)
@src/db/dialects/databricks.py  (lines 60-120 — create_engine signature)

<interfaces>
<!-- Key call-path contract. Executor should trace this during the audit. -->
<!-- Phase 11 bug: connect_with_config (Databricks branch) called connect_with_url(url, …), -->
<!-- which calls dialect.create_engine(sqlalchemy_url=url). But DatabricksDialect.create_engine -->
<!-- reads kwargs["host"] / kwargs["http_path"] → ValueError. Fixed in 4fdaa90. -->

Live call path (post-fix):
  connect_database (MCP tool)
    → ConnectionManager.connect_with_config(config: DatabricksConnectionConfig, dialect)
      → connect_with_url(url, dialect, ...)
        → dialect.create_engine(sqlalchemy_url=url)   # must accept this kwarg
          → _register_engine(engine, dialect, ...)

Env-var substitution:
  src/db/connection.py:425 — only `token` is passed through resolve_env_vars.
  `host` and `http_path` are NOT — that is itself a suspected coverage hole.
</interfaces>

<relevant_history>
- quick-260428-jr7 (commit 4fdaa90): fixed DatabricksDialect.create_engine to
  accept sqlalchemy_url. This audit asks WHY the test suite didn't catch the
  drift in the first place.
- Phase 11 shipped with 607 tests, all green. The bug was found in UAT Test 7.
</relevant_history>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Survey Databricks tests and trace call paths</name>
  <files>
    (read-only investigation — no files modified in this task)
    tests/unit/**/*.py (grep scope)
    tests/integration/**/*.py (grep scope, if dir exists)
    src/db/connection.py:376-470
    src/db/dialects/databricks.py:60-120
  </files>
  <action>
    Use Serena symbolic tools + ripgrep to inventory Databricks test coverage. Do NOT read whole test files — target symbols and matching lines only.

    Step 1 — Inventory (ripgrep, capture file:line for each hit):
      rg -n -i 'databricks|DatabricksConnectionConfig|DatabricksDialect' tests/ --type py
      rg -n 'connect_with_config' tests/ --type py
      rg -n 'connect_with_url'    tests/ --type py
      rg -n 'create_engine'       tests/ --type py | rg -i 'databricks'
      rg -n 'resolve_env_vars|DATABRICKS_|http_path|workspace' tests/ --type py

    Step 2 — For each hit, classify the test into exactly ONE bucket and record file:line:
      A. End-to-end: drives connect_with_config OR the MCP connect_database tool with a real DatabricksConnectionConfig AND allows the real DatabricksDialect.create_engine to execute (no dialect mock, no connect_with_url mock).
      B. Mock-heavy: exercises connect_with_config but mocks dialect.create_engine, connect_with_url, or the dialect object itself — so signature drift is invisible.
      C. Unit-only on create_engine: calls DatabricksDialect.create_engine directly with kwargs (host=, http_path=) — exercises the old kwargs signature, NOT the sqlalchemy_url path the live caller uses.
      D. Adjacent: Databricks config parsing / dialect property tests that don't touch the connection call path.

    Step 3 — Answer the three audit questions with citations:
      Q1: Any bucket-A tests? (yes/no + citations)
      Q2: For bucket-B and bucket-C tests, identify the specific mock/signature shape that hid the drift. Quote the mock call or the direct-kwargs invocation (file:line).
      Q3: Env-var substitution — search for tests that set DATABRICKS_HOST / DATABRICKS_HTTP_PATH env vars and verify substitution reaches create_engine. Compare to the existing MSSQL password substitution test pattern (find it with: rg -n 'resolve_env_vars|MSSQL_PASSWORD' tests/).

    Step 4 — Record findings in scratch notes (in your working context, not a file) structured as:
      - Bucket A count + citations
      - Bucket B count + citations + mock-shape quote per test
      - Bucket C count + citations
      - Bucket D count (just count, no need to cite)
      - Q1 answer (1 sentence + citations)
      - Q2 answer (1 paragraph + citations)
      - Q3 answer (1 sentence + citations, or "no such tests")
      - List of proposed missing tests (name + target file path + what it would assert)

    Do NOT modify any files in this task. This is pure investigation.
  </action>
  <verify>
    <automated>test -d tests/unit && rg -c -i 'databricks' tests/ --type py | head -5</automated>
  </verify>
  <done>
    Executor has, in working context: bucket-classified citations for every Databricks-touching test, explicit answers to Q1/Q2/Q3 with file:line refs, and a gap list with proposed test names. Ready to write Task 2 deliverable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT.md deliverable</name>
  <files>.planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md</files>
  <action>
    Write the audit note using the findings from Task 1. Use this exact structure (section headings are required so the verdict is easy to locate):

    ```markdown
    # Databricks connect_with_config Test Coverage Audit

    **Date:** 2026-05-05
    **Source todo:** .planning/todos/pending/2026-04-28-audit-databricks-test-coverage.md
    **Context:** Phase 11 shipped with 607 tests; DatabricksDialect.create_engine signature drift
    still slipped through and was fixed reactively in commit 4fdaa90 (quick-260428-jr7).

    ## Summary
    <one-paragraph TL;DR including the verdict — blocker or not — up front>

    ## Audit Questions

    ### Q1 — Is there an integration-style test covering connect_with_config → dialect.create_engine for Databricks?
    <yes/no + citations>

    ### Q2 — If yes, why did it not catch the signature mismatch?
    <mock-shape analysis with file:line quotes, OR explanation that no such test exists>

    ### Q3 — Env-var substitution for Databricks host/http_path?
    <yes/no + citations, with comparison to MSSQL password substitution pattern>

    ## Coverage Map

    | Code path | Covered? | Test type | Citations |
    |---|---|---|---|
    | connect_with_config (Databricks branch, connection.py:415-470) | … | A/B/C/D/none | file:line |
    | connect_with_url → dialect.create_engine(sqlalchemy_url=…) | … | … | … |
    | DatabricksDialect.create_engine (kwargs signature, databricks.py:70+) | … | … | … |
    | resolve_env_vars for token (connection.py:425) | … | … | … |
    | resolve_env_vars for host/http_path | … | … | … |

    ## Gaps and Proposed Tests

    For each gap, list:
    - **Gap:** <what's uncovered>
    - **Proposed test:** `tests/.../<name>.py::<test_func>`
    - **What it asserts:** <one sentence>
    - **Effort:** small (fits in quick task) / structural (needs follow-up)

    ## Verdict

    - **Blocker for further Databricks work?** YES / NO
    - **Rationale:** <2-3 sentences>
    - **Recommended follow-up:**
      - If small gaps → propose adding them as Task 3 of this plan.
      - If structural (new fixtures, mocking reshape) → create a follow-up todo at
        `.planning/todos/pending/<date>-add-databricks-integration-tests.md` and leave
        this audit todo closable.

    ## Closure
    Move the source todo `2026-04-28-audit-databricks-test-coverage.md` from
    `pending/` to `done/` once this AUDIT is committed. (Do NOT perform the move
    in this task — leave it for the user or a follow-up commit.)
    ```

    Keep the whole file ≤ 250 lines. Every claim that references a test or source
    location MUST cite file:line — no unlocated assertions.
  </action>
  <verify>
    <automated>test -s .planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md && grep -q '^## Verdict' .planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md && grep -q 'Blocker for further Databricks work' .planning/quick/260505-mr3-audit-test-coverage-for-databricks-conne/260505-mr3-AUDIT.md</automated>
  </verify>
  <done>
    AUDIT.md exists with: Summary (with verdict up front), Q1/Q2/Q3 answers with
    citations, Coverage Map table, Gaps list with proposed test names and
    small/structural classification, Verdict section with explicit blocker yes/no
    and recommended follow-up path.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3 gate: add tests now, or defer via follow-up todo?</name>
  <decision>
    Based on the AUDIT.md Gaps section from Task 2, decide whether to add 1-3
    integration tests in this quick task or defer all test work to a follow-up todo.
  </decision>
  <context>
    Quick-task scope is ~30% context and is primarily investigation. Adding tests
    is allowed ONLY if the gap fix is small and self-contained (no new fixtures,
    no reshape of existing mocks). Structural gaps — e.g., needing a real or
    faked DatabricksDialect that accepts sqlalchemy_url end-to-end without
    spinning up a real workspace — almost always require a dedicated plan.

    The planner's explicit instruction from the task brief:
      "If the gap is structural (requires new test fixtures, mocking reshape,
      etc.), defer to a follow-up todo instead of adding it to this quick task."
  </context>
  <options>
    <option id="add-tests-now">
      <name>Add 1-3 tests inline</name>
      <pros>Closes the audit AND the worst gap in one pass; no context loss between audit and fix.</pros>
      <cons>Only viable if gaps are all "small" per AUDIT.md classification. Risk of scope creep if a test turns out to need a real Databricks engine fake.</cons>
    </option>
    <option id="defer-via-todo">
      <name>Close audit, create follow-up todo for test additions</name>
      <pros>Keeps quick-task scope honest; gives user a clean decision point on whether the work is worth prioritizing.</pros>
      <cons>Gap stays open until the follow-up todo is picked up.</cons>
    </option>
    <option id="partial">
      <name>Add 1 trivial test, defer the rest</name>
      <pros>Captures the quickest-win gap (e.g., a direct assertion that connect_with_url → DatabricksDialect.create_engine accepts sqlalchemy_url). Follow-up todo for anything structural.</pros>
      <cons>Executor judgment call on what counts as "trivial".</cons>
    </option>
  </options>
  <resume-signal>
    Reply with one of: `add-tests-now`, `defer-via-todo`, `partial`, or give explicit
    direction. If `defer-via-todo` or `partial`, the executor will create/update the
    follow-up todo and the plan is complete after that. If `add-tests-now` or
    `partial` with additions, executor proceeds to implement the agreed test(s),
    runs `uv run pytest tests/` to confirm green, and commits.
  </resume-signal>
</task>

</tasks>

<verification>
- AUDIT.md exists at the declared path with all required sections
- Every test-reference claim in AUDIT.md has a file:line citation
- Q1, Q2, Q3 each have an explicit yes/no (or "no such test") answer
- Verdict section states blocker yes/no and recommends a concrete follow-up path
- If Task 3 decision added tests: `uv run pytest tests/` passes with zero new failures and zero new warnings
- If Task 3 decision deferred: a follow-up todo exists at `.planning/todos/pending/<date>-add-databricks-integration-tests.md`
</verification>

<success_criteria>
- User can read AUDIT.md and decide in <2 minutes whether to block or proceed with further Databricks work
- Source todo `2026-04-28-audit-databricks-test-coverage.md` is closable (user or follow-up commit moves it to `done/`)
- No silent test additions: any code change is gated through the Task 3 checkpoint
</success_criteria>

<output>
After completion, no SUMMARY.md needed (quick-task convention). The AUDIT.md
itself is the deliverable. The user handles:
  - Moving the source todo pending/ → done/
  - Committing the AUDIT (and any optional tests from Task 3)
</output>
