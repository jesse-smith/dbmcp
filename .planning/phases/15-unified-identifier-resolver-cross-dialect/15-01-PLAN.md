---
phase: 15-unified-identifier-resolver-cross-dialect
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/dialects/protocol.py
  - src/db/dialects/mssql.py
  - src/db/dialects/databricks.py
  - src/db/dialects/generic.py
  - tests/unit/test_mssql_dialect.py
  - tests/unit/test_databricks_dialect.py
  - tests/unit/test_generic_dialect.py
autonomous: true
requirements: [IDENT-03, IDENT-07]

must_haves:
  truths:
    - "Each dialect advertises its own default_schema (MSSQL='dbo', Databricks=None, generic=None)"
    - "Each dialect advertises max_identifier_depth (Databricks=3, MSSQL=2, generic=1)"
    - "The resolver (Plan 03) can read both properties off any DialectStrategy instance"
  artifacts:
    - path: "src/db/dialects/protocol.py"
      provides: "default_schema + max_identifier_depth on the DialectStrategy Protocol"
      contains: "def default_schema"
    - path: "src/db/dialects/mssql.py"
      provides: "default_schema='dbo', max_identifier_depth=2"
      contains: "def max_identifier_depth"
    - path: "src/db/dialects/databricks.py"
      provides: "default_schema=None, max_identifier_depth=3"
      contains: "def max_identifier_depth"
    - path: "src/db/dialects/generic.py"
      provides: "default_schema=None, max_identifier_depth=1"
      contains: "def max_identifier_depth"
  key_links:
    - from: "src/db/identifiers.py (Plan 03)"
      to: "dialect.default_schema / dialect.max_identifier_depth"
      via: "@property read"
      pattern: "dialect\\.(default_schema|max_identifier_depth)"
---

<objective>
Add two dialect-owned capability properties — `default_schema` and `max_identifier_depth` — to the `DialectStrategy` Protocol and all three implementations (MSSQL, Databricks, generic). These are the dialect-specific facts the cross-dialect resolver (Plan 03) consumes for depth validation (IDENT-03), the catalog gate (D-07), and the `dbo`-replacement default schema (IDENT-07/D-08/D-09).

Purpose: Move the hardcoded `'dbo'` knowledge and per-dialect depth limits out of scattered signatures and onto the dialect, following the established dialect-owned-behavior pattern. No resolver or tool wiring here — pure capability advertisement.
Output: 4 modified source files (protocol + 3 impls), 3 extended dialect test files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-CONTEXT.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing @property pattern on the Protocol the executor extends. Use this exact shape. -->

From src/db/dialects/protocol.py (lines 30-48):
- `@property name(self) -> str` — dialect id string
- `@property sqlglot_dialect(self) -> str | None` — declared with docstring + `...` body
- `@property supports_indexes(self) -> bool`
- `@property has_fast_row_counts(self) -> bool`
- Class docstring (lines 23-28) carries a `Properties:` block enumerating each property.

From src/db/dialects/mssql.py / databricks.py / generic.py:
- Each impl declares `sqlglot_dialect` as a one-line `return` property (mssql ~47-50, databricks ~89-92, generic ~49-52).
- generic.py threads `sqlglot_dialect` through `self._sqlglot_dialect` (instance attr); the two NEW properties are constant per dialect → return literals directly, do NOT thread through `__init__`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default_schema + max_identifier_depth to the Protocol and dialect tests (RED)</name>
  <read_first>
    - src/db/dialects/protocol.py (lines 16-48 — class docstring Properties block + existing @property declarations)
    - tests/unit/test_mssql_dialect.py (the `test_sqlglot_dialect_is_tsql` test ~line 28 — `assert dialect.sqlglot_dialect == "tsql"` is the exact assertion shape to copy)
    - tests/unit/test_databricks_dialect.py and tests/unit/test_generic_dialect.py (analogous property assertions)
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-PATTERNS.md (section "protocol.py + 3 impls" and "Dialect-property tests (IDENT-07)")
    - .planning/codebase/CONVENTIONS.md (docstring + property style)
  </read_first>
  <behavior>
    - test_default_schema_is_dbo (mssql): `MssqlDialect().default_schema == "dbo"`
    - test_default_schema_is_none (databricks): `DatabricksDialect().default_schema is None`
    - test_default_schema_is_none (generic): `GenericDialect().default_schema is None`
    - test_max_identifier_depth (mssql): `MssqlDialect().max_identifier_depth == 2`
    - test_max_identifier_depth (databricks): `DatabricksDialect().max_identifier_depth == 3`
    - test_max_identifier_depth (generic): `GenericDialect().max_identifier_depth == 1`
  </behavior>
  <action>
    Add two `@property` declarations to the `DialectStrategy` Protocol in `src/db/dialects/protocol.py`, matching the existing `sqlglot_dialect` declaration shape (docstring + `...` body): `default_schema(self) -> str | None` ("Default schema for this dialect, or None if the engine/connection decides.") and `max_identifier_depth(self) -> int` ("Max number of dotted identifier parts: Databricks=3, MSSQL=2, generic=1."). Extend the class-level docstring `Properties:` block (lines 23-28) to enumerate both new properties so the file's documentation convention stays consistent.

    Write the failing dialect-property tests FIRST in the three existing dialect test files: add `test_default_schema_*` and `test_max_identifier_depth_*` to `tests/unit/test_mssql_dialect.py`, `tests/unit/test_databricks_dialect.py`, `tests/unit/test_generic_dialect.py`. Model each assertion on the existing `test_sqlglot_dialect_*` tests (construct the real dialect instance, assert the property value). These tests MUST fail (AttributeError / property not yet implemented on the impls) before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mssql_dialect.py tests/unit/test_databricks_dialect.py tests/unit/test_generic_dialect.py -k "default_schema or max_identifier_depth" 2>&1 | grep -q -E "(failed|error)"</automated>
  </verify>
  <done>Protocol declares both properties with docstrings; 6 new dialect-property tests exist and FAIL because the impls do not yet define the properties.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the two properties on all three dialect impls (GREEN)</name>
  <read_first>
    - src/db/dialects/mssql.py (lines 47-60 — existing one-line `return` property shape for sqlglot_dialect/supports_indexes)
    - src/db/dialects/databricks.py (lines 89-102 — same)
    - src/db/dialects/generic.py (lines 49-62 — same; note `self._sqlglot_dialect` instance attr — do NOT mimic that for the constant properties)
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-RESEARCH.md ("Dialect property additions" code block, lines 283-313 — exact return values)
  </read_first>
  <action>
    Implement the two properties as one-line `return` properties (matching each file's existing `sqlglot_dialect` property style, with a one-line docstring):
    - `src/db/dialects/mssql.py`: `default_schema` returns `"dbo"`; `max_identifier_depth` returns `2`.
    - `src/db/dialects/databricks.py`: `default_schema` returns `None` (do NOT hardcode `'main'` — Phase 14 anti-pattern, RESEARCH.md line 190); `max_identifier_depth` returns `3`.
    - `src/db/dialects/generic.py`: `default_schema` returns `None`; `max_identifier_depth` returns `1`. Return literals directly — do NOT thread through `__init__`/`self._sqlglot_dialect`.
    Run `uv run ruff check src/db/dialects/` and resolve any new warnings (pre-existing src/metrics.py Generator import warning is out of scope).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_mssql_dialect.py tests/unit/test_databricks_dialect.py tests/unit/test_generic_dialect.py tests/unit/test_dialect_protocol.py -k "default_schema or max_identifier_depth or protocol" -x</automated>
  </verify>
  <done>All 6 new dialect-property tests pass; `uv run ruff check src/db/dialects/` reports no new warnings; the Protocol's runtime-checkable conformance test (test_dialect_protocol.py) still passes for all three impls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds constant capability properties; no untrusted input crosses any new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `default_schema` value used downstream in SQL | accept | Property returns a constant literal per dialect (`'dbo'`/`None`); no user input flows into it. SQL safety remains `dialect.quote_identifier` (query.py:119), unchanged by this plan. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — `sqlglot` already pinned (RESEARCH.md: audit not applicable). No `[ASSUMED]`/`[SUS]` packages introduced. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_mssql_dialect.py tests/unit/test_databricks_dialect.py tests/unit/test_generic_dialect.py tests/unit/test_dialect_protocol.py` passes.
- `grep -v '^#' src/db/dialects/databricks.py | grep -c "'main'"` returns 0 (no hardcoded Databricks default schema).
- `uv run ruff check src/db/dialects/` reports no new warnings.
</verification>

<success_criteria>
- Both `default_schema` and `max_identifier_depth` declared on the `DialectStrategy` Protocol with docstrings.
- All three impls return the correct constant values (MSSQL: dbo/2, Databricks: None/3, generic: None/1).
- 6 new dialect-property tests pass.
- No new ruff warnings; Protocol conformance intact.
</success_criteria>

<output>
Create `.planning/phases/15-unified-identifier-resolver-cross-dialect/15-01-SUMMARY.md` when done.
</output>
