---
phase: 15-unified-identifier-resolver-cross-dialect
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/metadata.py
  - src/db/query.py
  - tests/unit/test_query.py
autonomous: true
requirements: [IDENT-07]

must_haves:
  truths:
    - "No MetadataService or QueryService method signature carries schema_name='dbo'"
    - "When a caller passes schema_name=None, MSSQL still resolves to a usable schema via the existing inspector default (no synthetic 'dbo' injection)"
    - "Existing callers passing an explicit schema_name continue to work unchanged"
  artifacts:
    - path: "src/db/query.py"
      provides: "get_sample_data with schema_name: str | None = None (no dbo default)"
      contains: "schema_name: str | None = None"
    - path: "src/db/metadata.py"
      provides: "get_columns/get_indexes/get_foreign_keys/get_primary_key/(+ 2 more) with schema_name: str | None = None"
      contains: "schema_name: str | None = None"
  key_links:
    - from: "src/mcp_server tools (Plan 04)"
      to: "metadata/query methods"
      via: "resolved schema (already filled from default_schema) passed positionally"
      pattern: "schema_name="
---

<objective>
Sweep the hardcoded `schema_name="dbo"` defaults out of the `MetadataService` and `QueryService` method SIGNATURES (D-11). After this plan, these service methods stop defaulting to `'dbo'`; the resolver/`default_schema` (Plans 03/04) becomes the single source of the default. This is the service-layer half of SC4 — the tool-signature half lands in Plan 04.

Purpose: Kill the scattered `'dbo'` so SQLite/Databricks never silently inherit a SQL-Server schema name. Defaults become `None`; `None` flows to the SQLAlchemy inspector which applies the connection's default schema (D-10, no synthetic fallback).
Output: 2 modified source files (6 sites in metadata.py, 1 in query.py), extended query tests.

IMPORTANT — out-of-scope flag to record (do NOT change): `src/db/metadata.py:919` `fk.get("referred_schema", "dbo")` is a RESULT-mapping default (FK target display), NOT a signature default. RESEARCH.md Pitfall 4 / A2 says leave it; note it in the SUMMARY for a future dbo audit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-CONTEXT.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Exact dbo-default sites to edit. Verified by grep 2026-05-28. -->

src/db/metadata.py — `schema_name: str = "dbo"` at lines: 731 (get_columns), 781 (get_indexes), 817 (get_foreign_keys), 833 (get_primary_key), 852, 1126 (table_exists).
src/db/query.py:81 — `schema_name: str = "dbo"` in get_sample_data.

DO NOT TOUCH: src/db/metadata.py:919 `fk.get("referred_schema", "dbo")` (result-mapping default, out of SC4 scope).

Inspector-default contract (D-10): SQLAlchemy `inspector.get_table_names(schema=None)` / `get_columns(table, schema=None)` use the connection's default schema. No synthetic fallback needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dbo signature defaults in metadata.py and query.py</name>
  <read_first>
    - src/db/metadata.py lines 725-860 and 1123-1162 (the 6 method signatures + docstrings carrying `="dbo"`)
    - src/db/metadata.py lines 915-925 (the line 919 result-mapping default — read to confirm you are NOT editing it)
    - src/db/query.py lines 78-128 (get_sample_data signature + the quote_identifier SQL-build at :119)
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-PATTERNS.md (sections "src/db/metadata.py" and "src/db/query.py")
  </read_first>
  <action>
    Change `schema_name: str = "dbo"` to `schema_name: str | None = None` at all six metadata.py sites (731, 781, 817, 833, 852, 1126) and the one query.py site (81). Update each affected docstring: replace `Schema name (default: 'dbo')` with `Schema name (None = connection default schema)`. Leave the method bodies' SQL logic unchanged — `query.py:119` `quote_identifier(schema_name)` stays the SQL-safety boundary (resolver does NOT sanitize, RESEARCH §Security). Do NOT modify `metadata.py:919` `referred_schema` default — it is a result-mapping default, out of SC4 scope (note it in the SUMMARY).

    Note: where a method body assumed a non-None `schema_name`, verify the `None` path reaches `inspector.get_*(schema=None)` (D-10 inspector-default). If any body would break on `None` for MSSQL specifically, that breakage is expected to be covered by the resolver filling the schema upstream in Plan 04 — but the service must not re-inject `'dbo'`.
  </action>
  <verify>
    <automated>grep -nE 'schema_name: str = "dbo"' src/db/metadata.py src/db/query.py | grep -c . | grep -q '^0$'</automated>
  </verify>
  <done>Zero `schema_name: str = "dbo"` signature defaults remain in metadata.py or query.py; line 919 result-mapping default is untouched; docstrings updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update/extend service tests for the None-default behavior</name>
  <read_first>
    - tests/unit/test_query.py (existing get_sample_data tests — find tests that rely on the implicit 'dbo' default)
    - src/db/query.py lines 78-128 (get_sample_data after Task 1 edit)
    - .planning/codebase/TESTING.md (test placement, 85% coverage floor)
  </read_first>
  <action>
    Find any existing test in `tests/unit/test_query.py` (and `tests/unit/test_metadata.py` if present) that relies on the implicit `schema_name="dbo"` default by calling these methods WITHOUT a schema argument. Update those calls to pass an explicit `schema_name` (since the default is now `None`), OR add a test asserting the new `None` default behavior: calling `get_sample_data(table_name=..., schema_name=None)` against a generic/SQLite engine builds an unqualified table reference (the `self._dialect is None` branch at query.py:117 already handles no-prefix). Add one assertion that `inspect.signature(QueryService.get_sample_data).parameters['schema_name'].default is None`.
    Run `uv run ruff check src/db/ tests/unit/test_query.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_query.py -x -q</automated>
  </verify>
  <done>tests/unit/test_query.py passes with the new None default; a signature-assertion test confirms `get_sample_data` default is `None`; no new ruff warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → service method (`schema_name`) | `schema_name` (now `str | None`) flows into SQL via `quote_identifier`. Unchanged boundary — only the default changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | `schema_name` → SQL in query.py get_sample_data | mitigate | `query.py:119` `dialect.quote_identifier(schema_name)` remains the injection defense and is NOT modified by this plan. The `None` path routes through the no-prefix branch (query.py:117) for dialectless engines. |
| T-15-03 | Information Disclosure | SQLite/Databricks no longer silently get `'dbo'` | mitigate | Removing the `'dbo'` default prevents querying a wrong/nonexistent schema; `None` → connection default schema (D-10) returns dialect-correct results. |
| T-15-SC | Tampering | pip installs | accept | No package installs; `sqlglot` already pinned. |
</threat_model>

<verification>
- `grep -nE 'schema_name: str = "dbo"' src/db/metadata.py src/db/query.py` returns nothing.
- `grep -n 'referred_schema' src/db/metadata.py` still shows the line 919 default unchanged (out-of-scope confirmation).
- `uv run pytest tests/unit/test_query.py` passes.
</verification>

<success_criteria>
- No `schema_name: str = "dbo"` signature default remains in `src/db/metadata.py` or `src/db/query.py`.
- `metadata.py:919` result-mapping default left intact and noted for future audit.
- Service tests green; no new ruff warnings.
</success_criteria>

<output>
Create `.planning/phases/15-unified-identifier-resolver-cross-dialect/15-02-SUMMARY.md` when done. Record the `metadata.py:919` out-of-scope flag in the SUMMARY.
</output>
