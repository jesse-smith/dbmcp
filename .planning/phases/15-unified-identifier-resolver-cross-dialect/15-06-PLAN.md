---
phase: 15-unified-identifier-resolver-cross-dialect
plan: 06
type: execute
wave: 5
depends_on: ["15-02", "15-03", "15-05"]
files_modified:
  - src/mcp_server/analysis_tools.py
  - tests/unit/test_async_tools.py
autonomous: true
requirements: [IDENT-03, IDENT-04, IDENT-07]

must_haves:
  truths:
    - "find_pk_candidates and find_fk_candidates each route table_name/schema_name/catalog through resolve_identifier at the @mcp.tool boundary (D-14)"
    - "Neither tool signature carries schema_name='dbo' (categorical SC4 satisfied across all 7 tools)"
    - "Each gains a catalog param (Databricks-only); passing catalog on MSSQL/generic returns a status=error response"
    - "A conflicting table_name/explicit-param or over-depth identifier returns a status=error response (not a crash)"
    - "The D-12 shared boundary test matrix asserts all 7 tools (not 5) route through the resolver and surface ValueError as an error response"
  artifacts:
    - path: "src/mcp_server/analysis_tools.py"
      provides: "find_pk_candidates + find_fk_candidates routing through resolver; no dbo defaults; catalog param"
      contains: "resolve_identifier"
  key_links:
    - from: "src/mcp_server/analysis_tools.py find_pk_candidates/find_fk_candidates _sync_work"
      to: "src.db.identifiers.resolve_identifier"
      via: "call inside _sync_work before inspector/metadata calls"
      pattern: "resolve_identifier\\("
    - from: "resolver ValueError"
      to: "except ValueError -> error_message"
      via: "existing tool boundary (analysis_tools.py)"
      pattern: "except ValueError"
---

<objective>
Make `find_pk_candidates` and `find_fk_candidates` full namespace-aware tools (D-14, user scope decision 2026-05-28), bringing the total from 5 to 7 resolver-routed tools. ROADMAP SC4 ("No tool signature in `src/mcp_server/` carries `schema_name='dbo'`") is categorical, and both tools currently carry the exact `schema_name="dbo"` default IDENT-07 removes — the Databricks/SQLite bug. This plan sweeps that default, routes each tool's `table_name` through `resolve_identifier`, adds the D-07 catalog gate, and adds a Databricks-only `catalog` param.

Purpose: Close the categorical-SC4 gap and unify all seven tools behind one resolver. Supersedes the original D-11 "5 tool" enumeration; the spirit (one resolver, no hardcoded dbo) governs (D-14).
Output: 1 modified tool file (2 tools), extended boundary tests (D-12 matrix → 7 tools).

Both tools use the SQLAlchemy inspector directly (`inspector.get_table_names(schema=...)`, `inspector.get_columns(...)`) — same pattern as get_column_info — so their dbo defaults live ONLY in their own signatures (analysis_tools.py:155, :260); no extra metadata.py sweep is needed beyond Plan 02. Both already have an `except ValueError -> error_message` boundary (analysis_tools.py:~239/~243 for find_pk, ~243 for find_fk per grep) so resolver ValueErrors surface with no new plumbing.

Wave/overlap: this plan edits `analysis_tools.py` and `test_async_tools.py`, both also edited by Plan 05. It therefore runs AFTER Plan 05 (depends_on includes 15-05) to avoid same-wave file conflict.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-CONTEXT.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-PATTERNS.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- D-14 scope. find_pk/fk verified to mirror get_column_info (inspector-direct). -->

From src.db.identifiers (Plan 03):
- resolve_identifier(table_name, schema_name, catalog, dialect) -> ResolvedIdentifier(catalog, schema, table); raises ValueError on conflict/over-depth/catalog-on-shallow-dialect/malformed.
  (Both tools HAVE a table_name, so use resolve_identifier directly — not the bare _assert_catalog_allowed gate.)

analysis_tools.py find_pk_candidates (verified):
- signature: schema_name: str = "dbo" (~:155); NO catalog param.
- _sync_work (~:200): inspector.get_table_names(schema=schema_name); get_primary_key-style work passing schema_name=schema_name; existence-not-found error message references schema_name.
- `except ValueError -> error_message` boundary present.

analysis_tools.py find_fk_candidates (verified):
- signature: schema_name: str = "dbo" (~:260); NO catalog param.
- _sync_work (~:327): inspector.get_table_names(schema=schema_name); inspector.get_columns(table_name, schema=schema_name); source_schema=schema_name passed down.
- `except ValueError -> error_message` boundary present.

Canonical param-shape analog: get_table_schema (schema_tools.py:413-419) — `catalog: str | None = None` at the END; and get_column_info AFTER Plan 05 (same file) is the closest in-file analog for the resolver call inside _sync_work.

Existing parametrized tool list: tests/unit/test_async_tools.py `_TOOL_PARAMS` (~lines 300-332) ALREADY includes find_pk_candidates + find_fk_candidates (used by the safety-net tests). The D-12 boundary matrix (catalog-gate / conflict) currently added in Plans 04/05 covers 5 tools; extend it to all 7 by adding find_pk/fk to the catalog-gate + conflict parametrization.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep dbo + add catalog + resolver routing to find_pk_candidates and find_fk_candidates</name>
  <read_first>
    - src/mcp_server/analysis_tools.py lines 152-250 (find_pk_candidates) and 256-395 (find_fk_candidates) — full signatures, _sync_work, ValueError boundaries
    - src/mcp_server/analysis_tools.py lines 26-150 (get_column_info AFTER Plan 05 — the in-file resolver-routing analog to copy)
    - src/db/identifiers.py (Plan 03 — resolve_identifier)
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-CONTEXT.md (D-14 — full scope)
  </read_first>
  <action>
    For BOTH find_pk_candidates and find_fk_candidates:
    (a) Change `schema_name: str = "dbo"` to `schema_name: str | None = None` (D-11 extended).
    (b) Add `catalog: str | None = None` at the end of the signature (copy get_table_schema's param shape + docstring "Optional Databricks catalog name; rejected on non-Databricks dialects").
    (c) Inside each `_sync_work` (after `dialect = conn_manager.get_dialect(connection_id)`, before the inspector calls), call `resolved = resolve_identifier(table_name, schema_name, catalog, dialect)` and use `resolved.schema` / `resolved.table` for the inspector calls (`get_table_names(schema=resolved.schema)`, `get_columns(resolved.table, schema=resolved.schema)`) and any downstream `source_schema=` / `schema_name=` kwargs. The D-07 catalog gate is enforced by resolve_identifier itself (raises on catalog + max_identifier_depth < 3).
    (d) Update the not-found / column-not-found error messages to use `resolved.schema`/`resolved.table` so they report the resolved values.
    For Databricks cross-catalog: same KISS posture as get_column_info (Plan 05) — the inspector binds to the engine default catalog (Phase 14 IDENT-01); the resolver GATES catalog (rejects on non-Databricks) which satisfies the D-14 gate requirement. Note any cross-catalog inspector limitation in the SUMMARY (consistent with Plan 05).
    Import `resolve_identifier` from `src.db.identifiers` (already imported in analysis_tools.py by Plan 05 for get_column_info — reuse the import; do not duplicate).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_async_tools.py -x -q && grep -nE 'schema_name: str = "dbo"' src/mcp_server/analysis_tools.py | grep -c . | grep -q '^0$'</automated>
  </verify>
  <done>find_pk_candidates + find_fk_candidates each expose `catalog: str | None = None`, route through resolve_identifier in _sync_work, and have no `schema_name: str = "dbo"` default; analysis_tools.py has ZERO dbo signature defaults; async tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the D-12 shared boundary matrix to all 7 tools</name>
  <read_first>
    - tests/unit/test_async_tools.py (the catalog-gate + conflict boundary tests added by Plans 04/05; the `_TOOL_PARAMS` list ~300-332; mock-dialect fixtures ~114-263)
    - src/mcp_server/analysis_tools.py (post-Task-1 find_pk/fk state)
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-CONTEXT.md (D-12 — extend matrix from 5 to 7)
  </read_first>
  <action>
    Extend the parametrized D-12 boundary tests in `tests/unit/test_async_tools.py` so the catalog-gate and conflict assertions cover all 7 tools (add find_pk_candidates + find_fk_candidates to the parametrization that Plans 04/05 used for the other 5):
    - catalog gate: each of the 2 tools with `catalog="x"` on a mocked MSSQL/generic dialect returns `status: error` mentioning catalog not supported.
    - conflict: `table_name="sales.orders"` + `schema_name="hr"` returns `status: error` (conflict) for both tools.
    - happy path: Databricks dialect + valid table_name resolves and reaches the mocked inspector/metadata layer with the expected resolved schema/table (assert via mock kwargs).
    Prefer extending the existing parametrized test (single source of truth across all 7 tools) over adding a separate copy. Reuse existing mock fixtures.
    Run `uv run ruff check src/mcp_server/analysis_tools.py tests/unit/test_async_tools.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_async_tools.py -k "catalog or conflict or resolv" -x -q</automated>
  </verify>
  <done>D-12 boundary matrix asserts the catalog-gate + conflict + happy-path for find_pk_candidates and find_fk_candidates (matrix now spans all 7 tools); full async-tools suite green; no new ruff warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → find_pk_candidates / find_fk_candidates (table_name/schema_name/catalog) | Untrusted client identifier input enters here; resolver validates structure; resolved parts reach the SQLAlchemy inspector. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-14 | Tampering | resolved schema/table → inspector calls in find_pk/fk | mitigate | The SQLAlchemy inspector parameterizes schema/table names; resolver decomposes but does NOT sanitize — inspector + (where raw SQL is used) `quote_identifier` remain the defense. No user input is string-concatenated into SQL by these tools. |
| T-15-15 | Spoofing/Elevation | catalog newly accepted on these 2 tools | mitigate | catalog is GATED by resolve_identifier (rejected on non-Databricks). Tested in Task 2. On Databricks the inspector uses the engine default catalog (IDENT-01). |
| T-15-16 | Denial of Service | resolver ValueError crashing the tool | mitigate | Resolver call placed inside `_sync_work`; existing `except ValueError -> error_message` catches it. |
| T-15-SC | Tampering | pip installs | accept | No package installs; `sqlglot` already pinned. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_async_tools.py` passes.
- `grep -nE 'schema_name: str = "dbo"' src/mcp_server/analysis_tools.py` returns nothing (categorical SC4 across all 7 tools).
- `grep -nE 'schema_name: str = "dbo"' src/mcp_server/` (recursive) returns nothing across the whole tool surface.
- `grep -c 'resolve_identifier' src/mcp_server/analysis_tools.py` >= 1 (shared with get_column_info).
- `uv run ruff check src/mcp_server/analysis_tools.py` — no new warnings.
</verification>

<success_criteria>
- find_pk_candidates + find_fk_candidates route through resolve_identifier (D-14); both gain a gated catalog param.
- ZERO `schema_name: str = "dbo"` signature defaults remain anywhere in `src/mcp_server/` (categorical SC4 met for all 7 tools).
- D-12 boundary matrix covers all 7 tools.
- Suite green; no new ruff warnings.
</success_criteria>

<output>
Create `.planning/phases/15-unified-identifier-resolver-cross-dialect/15-06-SUMMARY.md` when done. Note any find_pk/fk cross-catalog inspector limitation.
</output>
