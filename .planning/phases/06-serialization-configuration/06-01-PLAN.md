---
phase: 06-serialization-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/type_registry.py
  - src/serialization.py
  - src/db/query.py
  - tests/unit/test_type_registry.py
  - tests/unit/test_serialization.py
  - tests/unit/test_query.py
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "All Python types previously handled by _pre_serialize and _truncate_value are handled by the unified registry"
    - "Subclass ordering is correct: bool before int, StrEnum before str, datetime before date"
    - "Unknown types raise TypeError (strict mode, no silent str() fallback)"
    - "encode_response still works identically for all existing callers"
    - "_process_rows still tracks truncation per column"
  artifacts:
    - path: "src/type_registry.py"
      provides: "Unified type handler registry with convert() function"
      exports: ["convert", "DEFAULT_TRUNCATION_LIMIT"]
    - path: "src/serialization.py"
      provides: "encode_response using registry instead of _pre_serialize"
      contains: "from src.type_registry import convert"
    - path: "tests/unit/test_type_registry.py"
      provides: "Comprehensive registry tests including subclass ordering and all type handlers"
      min_lines: 80
  key_links:
    - from: "src/serialization.py"
      to: "src/type_registry.py"
      via: "import convert, call with trunc_limit=sys.maxsize for no truncation"
      pattern: "convert\\(.*sys\\.maxsize"
    - from: "src/db/query.py"
      to: "src/type_registry.py"
      via: "import convert, call with configurable trunc_limit"
      pattern: "from src\\.type_registry import convert"
---

<objective>
Unify the duplicate type-conversion pipelines (_truncate_value in query.py and _pre_serialize in serialization.py) into a single type handler registry.

Purpose: Eliminate duplicated type handling logic spread across two modules, ensuring consistent conversion behavior and a single place to add new type support.
Output: src/type_registry.py with convert() function, updated callers in serialization.py and query.py, comprehensive tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-serialization-configuration/06-CONTEXT.md
@.planning/phases/06-serialization-configuration/06-RESEARCH.md

@src/serialization.py
@src/db/query.py

<interfaces>
<!-- Current _pre_serialize (serialization.py) handles: None, bool, int, float, str, StrEnum, dict, list, tuple, datetime, date, Decimal. Unknown -> TypeError -->
<!-- Current _truncate_value (query.py:340-380) handles: None, bytes, datetime, date, time, Decimal, str (truncation). Unknown -> passthrough -->
<!-- Unified registry must handle the UNION of both: None, bool, int, float, str, bytes, datetime, date, time, Decimal, StrEnum, dict, list, tuple. Unknown -> TypeError -->

From src/serialization.py:
```python
def _pre_serialize(value: Any) -> Any: ...
def encode_response(data: dict) -> str: ...
```

From src/db/query.py (QueryService methods):
```python
def _truncate_value(self, value: Any, column_name: str) -> tuple[Any, bool]: ...
def _process_rows(self, result, rows: list, truncated_columns: list) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create type_registry.py with convert() and comprehensive tests</name>
  <files>src/type_registry.py, tests/unit/test_type_registry.py</files>
  <behavior>
    - None -> (None, False)
    - True/False -> (True/False, False) — NOT (1/0, False)
    - int/float -> (value, False) passthrough
    - str short -> (value, False); str > limit -> (truncated + "... (N chars total)", True)
    - bytes <= 32 -> ("<binary: hex (N bytes)>", True); bytes > 32 -> ("<binary: hex... (N bytes)>", True)
    - datetime -> (isoformat, False); date -> (isoformat, False); time -> (isoformat, False)
    - Decimal -> (float(value), False)
    - StrEnum member -> (str(member.value), False)
    - dict -> recurse values, aggregate was_truncated
    - list -> recurse items, aggregate was_truncated
    - tuple -> convert to list, recurse
    - Unknown type (e.g., object()) -> TypeError raised
    - Subclass ordering: bool checked before int, StrEnum before str, datetime before date
  </behavior>
  <action>
    Write tests first in tests/unit/test_type_registry.py covering every type listed in behavior plus subclass ordering edge cases. Then create src/type_registry.py with:
    - Module-level ordered handler chain `_HANDLER_CHAIN: list[tuple[type, Callable]]` — order: bool, StrEnum, int, float, str, bytes, datetime, date, time, Decimal, dict, list, tuple
    - Individual handler functions: `_handle_X(value, trunc_limit) -> tuple[Any, bool]`
    - Public `convert(value, trunc_limit=DEFAULT_TRUNCATION_LIMIT) -> tuple[Any, bool]` that iterates chain, raises TypeError for unmatched types
    - `DEFAULT_TRUNCATION_LIMIT = 1000` constant exported
    - Follow the exact pattern from RESEARCH.md Pattern 1, adjusted for test results
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_type_registry.py -x -v</automated>
  </verify>
  <done>All type handlers pass, subclass ordering verified, TypeError on unknown types confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Replace _pre_serialize and _truncate_value with registry calls</name>
  <files>src/serialization.py, src/db/query.py, tests/unit/test_serialization.py, tests/unit/test_query.py</files>
  <action>
    Update src/serialization.py:
    - Remove `_pre_serialize()` function entirely
    - Import `convert` from `src.type_registry`
    - In `encode_response()`, call `convert(data, trunc_limit=sys.maxsize)` (effectively no truncation for the serialization path) and use only the first element of the returned tuple: `converted, _ = convert(data, trunc_limit=sys.maxsize)`
    - Import `sys` for `sys.maxsize`
    - Keep the module docstring updated

    Update src/db/query.py:
    - Remove `_truncate_value()` method from `QueryService` class
    - Import `convert` from `src.type_registry`
    - In `_process_rows()`, replace `self._truncate_value(value, column_name)` with `convert(value, 1000)` — the 1000 is the current hardcoded text truncation limit (will become configurable in plan 02)
    - Remove now-unused imports from query.py that were only needed by _truncate_value (datetime, date, dt_time, Decimal) IF they are not used elsewhere in the file — check before removing

    Run existing test suites to verify no regressions:
    - tests/unit/test_serialization.py must all pass (encode_response behavior unchanged)
    - tests/unit/test_query.py must all pass (_process_rows behavior unchanged)
    - Full test suite must pass
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_serialization.py tests/unit/test_query.py tests/unit/test_type_registry.py -x -v && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>_pre_serialize and _truncate_value are deleted; both serialization.py and query.py use the registry; all existing tests pass with no behavior changes</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x -q` — full suite green
- `uv run ruff check src/` — no lint warnings
- `grep -r "_pre_serialize\|_truncate_value" src/` returns no matches (both functions removed)
- `grep "from src.type_registry import" src/serialization.py src/db/query.py` confirms both callers use registry
</verification>

<success_criteria>
- src/type_registry.py exists with convert() handling all 13 types (None + 12 isinstance types)
- _pre_serialize() removed from serialization.py
- _truncate_value() removed from query.py
- encode_response() uses registry with no-truncation limit
- _process_rows() uses registry with 1000-char truncation limit
- All existing tests pass without modification (behavior-preserving refactor)
- New test_type_registry.py covers every handler and subclass edge case
</success_criteria>

<output>
After completion, create `.planning/phases/06-serialization-configuration/06-01-SUMMARY.md`
</output>
