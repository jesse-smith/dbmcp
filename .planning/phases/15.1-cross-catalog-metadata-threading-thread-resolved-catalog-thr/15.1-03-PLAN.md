---
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
plan: 03
type: tdd
wave: 2
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - src/analysis/fk_candidates.py
  - tests/unit/test_fk_candidates.py
autonomous: true
requirements: [IDENT-08]
must_haves:
  truths:
    - "FKCandidateSearch accepts an optional catalog arg; source-column reads target that catalog on Databricks."
    - "FK target-table enumeration is scoped to the resolved catalog (Pitfall 3) — targets are NOT searched in the connection default catalog."
    - "The nested PKDiscovery used for target columns is constructed WITH catalog=self._catalog."
    - "Default-catalog path unchanged; no USE CATALOG emitted."
  artifacts:
    - path: "src/analysis/fk_candidates.py"
      provides: "Catalog-aware FK candidate search (source + target scoped to resolved catalog)"
      contains: "catalog"
    - path: "tests/unit/test_fk_candidates.py"
      provides: "Cross-catalog source + target-scope tests (catalog-discriminating fake)"
      contains: "cross_catalog"
  key_links:
    - from: "src/analysis/fk_candidates.py target enumeration"
      to: "src/analysis/_sql.py CatalogAwareReflector.list_tables"
      via: "SHOW TABLES IN catalog.schema on the cross-catalog branch"
      pattern: "list_tables"
    - from: "src/analysis/fk_candidates.py get_candidate_columns"
      to: "PKDiscovery(catalog=...)"
      via: "nested PK discovery scoped to resolved catalog"
      pattern: "catalog=self._catalog"
---

<objective>
Thread `resolved.catalog` into `FKCandidateSearch`. FK search is multi-table, so catalog
threading must cover BOTH the source-column reads AND the target-table universe (RESEARCH
Pitfall 3). Open Q1 decision: scope cross-catalog FK target search to `resolved.catalog`
ONLY (KISS, matches "target the requested catalog"); cross-catalog FK targets across
different catalogs are a deferred idea, not in this phase.

Purpose: Eliminate CR-02 for find_fk_candidates without searching targets in the wrong catalog.
Output: catalog-aware FKCandidateSearch + catalog-discriminating source/target tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-PATTERNS.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-01-SUMMARY.md

<interfaces>
<!-- From src/analysis/_sql.py (Plan 01): CatalogAwareReflector(connection, dialect) -->
<!--   .reflect_columns(catalog, schema, table) -> list[{"name","data_type"}] -->
<!--   .list_tables(catalog, schema) -> list[str] -->
<!-- Current FKCandidateSearch.__init__ (lines 41-57): -->
<!--   (connection, source_schema, source_table, source_column, source_data_type, -->
<!--    dialect=None, inspector=None)  -- no catalog, no _qualified_table -->
<!-- Catalog-blind reads to re-route (cross-catalog branch only): -->
<!--   _get_target_tables_inspector line 156: inspector.get_table_names(schema=) (Pitfall 3) -->
<!--   get_candidate_columns lines 197-203: constructs PKDiscovery WITHOUT catalog -->
<!--   line 216: inspector.get_columns; line 340 get_pk_constraint; line 343 get_unique_constraints -->
<!-- PKDiscovery now accepts catalog= (Plan 15.1-02 — same wave; thread it through). -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — cross-catalog source + target-scope + no-USE-CATALOG tests</name>
  <read_first>
    - src/analysis/fk_candidates.py (full — constructor 41-57; _get_target_tables_inspector line 156; get_candidate_columns 197-203; column reads 216/340/343)
    - tests/unit/test_fk_candidates.py (existing mock patterns — catalog-blind; identify which to upgrade)
    - tests/conftest.py lines 49-69 (real `dialect` fixture)
    - tests/unit/test_metadata.py lines 595-669 (catalog-asserting call_args_list / SHOW TABLES IN pattern)
    - src/analysis/_sql.py CatalogAwareReflector (Plan 01)
    - src/db/metadata.py lines 345-368 (_list_tables_databricks — SHOW TABLES IN, row[1])
  </read_first>
  <behavior>
    - test_cross_catalog_source_targets_requested_catalog: FKCandidateSearch(..., catalog="cerner_src") with a catalog-discriminating fake; source-column read SQL carries `cerner_src`.`...`; assert non-empty.
    - test_target_tables_scoped_to_catalog (Pitfall 3): target enumeration emits SHOW TABLES IN `cerner_src`.`<schema>` (not the default catalog); assert the executed SQL contains the resolved catalog and NOT a bare 2-part default-catalog enumeration.
    - test_nested_pkdiscovery_gets_catalog: assert the PKDiscovery constructed for target columns receives catalog="cerner_src" (patch/spy on PKDiscovery construction).
    - test_no_use_catalog: no "USE CATALOG" in any executed SQL.
    - test_default_path_unchanged: catalog=None still uses inspector.get_table_names / get_columns (no reflector, no SHOW TABLES IN).
  </behavior>
  <action>
    Extend tests/unit/test_fk_candidates.py with a TestCrossCatalogFK class. Use a catalog-discriminating fake connection (function execute.side_effect, asserts no USE CATALOG, returns rows only for the resolved-catalog 3-part name / SHOW TABLES IN resolved catalog). Use real `dialect` fixture. Spy on the nested PKDiscovery to assert catalog is threaded. Tests MUST fail (no catalog kwarg). Do not implement.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fk_candidates.py -k "cross_catalog or target_tables_scoped or nested_pkdiscovery or no_use_catalog" -x -q 2>&1 | grep -qE "(TypeError|unexpected keyword|FAILED|error)" &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>New cross-catalog FK tests exist and fail because FKCandidateSearch has no catalog kwarg (RED).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — thread catalog through FKCandidateSearch (source + targets)</name>
  <read_first>
    - src/analysis/fk_candidates.py (file being modified)
    - tests/unit/test_fk_candidates.py (RED tests — contract)
    - src/analysis/_sql.py CatalogAwareReflector (Plan 01)
    - src/analysis/pk_discovery.py constructor (Plan 02 — now accepts catalog=)
    - src/db/metadata.py lines 345-368 (SHOW TABLES IN reference)
  </read_first>
  <action>
    Add `catalog: str | None = None` to FKCandidateSearch.__init__; store `self._catalog`; compute `self._cross_catalog = bool(catalog) and dialect is not None and dialect.name == "databricks"`. On the cross-catalog branch: (1) replace _get_target_tables_inspector's inspector.get_table_names (line 156) with CatalogAwareReflector(self.connection, self._dialect).list_tables(self._catalog, schema) — scope targets to resolved catalog (Open Q1: KISS); (2) thread catalog=self._catalog into the nested PKDiscovery construction at lines 197-203; (3) re-route the source/target column reads at 216/340/343 to CatalogAwareReflector.reflect_columns + catalog-scoped information_schema constraint queries (mirror Plan 02). Default-catalog branch keeps existing Inspector calls verbatim. Never emit USE CATALOG.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_fk_candidates.py -x -q</automated>
  </verify>
  <done>All test_fk_candidates.py tests pass (new + regression). Target enumeration scoped to resolved catalog. `grep -v '^#' src/analysis/fk_candidates.py | grep -c "USE CATALOG"` == 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool args -> raw SQL | catalog/schema/table -> DESCRIBE TABLE, SHOW TABLES IN, information_schema 3-part SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-06 | Tampering (SQLi) | FK source/target reflection + SHOW TABLES IN | mitigate | Segments quoted via dialect.quote_identifier (delegated to CatalogAwareReflector); injection covered by Plan 01. |
| T-15.1-07 | Tampering / Info Disclosure | pooled connection catalog state | mitigate | No USE CATALOG (stateless 3-part); test_no_use_catalog + grep gate. |
| T-15.1-08 | Information Disclosure | cross-catalog target enumeration reach | accept | Targets scoped to resolved catalog (Open Q1); Unity Catalog ACLs enforce server-side; no new exposure beyond list_tables. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_fk_candidates.py -q` green.
- `uv run ruff check src/analysis/fk_candidates.py` clean.
- `grep -v '^#' src/analysis/fk_candidates.py | grep -c "USE CATALOG"` == 0.
</verification>

<success_criteria>
find_fk_candidates with a non-default Databricks catalog reflects the source column from that
catalog AND searches FK targets within that catalog only; default path unchanged; no USE CATALOG.
</success_criteria>

<output>
Create `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-03-SUMMARY.md` when done.
</output>
