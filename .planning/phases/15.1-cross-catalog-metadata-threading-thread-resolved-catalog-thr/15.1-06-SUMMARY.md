# 15.1-06 SUMMARY — Live cross-catalog UAT gate

**Plan:** 15.1-06 (checkpoint, autonomous: false)
**Tasks:** 3/3
**Requirement:** IDENT-08

## What this verified

Phase gate before /gsd:verify-work: proved the Wave 1–4 cross-catalog fix works against a
REAL Databricks Unity Catalog warehouse via the `dbmcp-test` MCP server — not just against
catalog-discriminating mocks. Closes RESEARCH assumption A2 (live multi-catalog).

## Method

- Connection: `databricks-test`, default catalog `bmtct`, second catalog `cerner_src` (connection_id d9ce935f5dbb).
- Test table: `cerner_src.v500.abstract_data` (4,302,614 rows; PK `ABSTRACT_DATA_ID`; FK column `PERSON_ID` → `cerner_src.v500.person`).
- For each of the three analysis tools: ran the call WITH `catalog=cerner_src` (expect cerner_src data) and WITHOUT (negative control, expect bmtct/not-found).

## Result — PASS (7/7)

| Tool | Override → cerner_src | Negative control → bmtct |
|------|----------------------|--------------------------|
| get_column_info | ✅ 4,302,614 rows, ABSTRACT_DATA_ID fully distinct | ✅ SCHEMA_NOT_FOUND bmtct.v500 |
| find_pk_candidates | ✅ ABSTRACT_DATA_ID unique+non-null (probe on 4.3M rows) | ✅ SCHEMA_NOT_FOUND bmtct.v500 |
| find_fk_candidates | ✅ PERSON_ID → cerner_src.v500.person, total_found 101 | ✅ SCHEMA_NOT_FOUND bmtct.v500 |

Plus T-15.1-16: 6 alternating calls on one pooled connection showed no stale-catalog bleed
(no USE CATALOG; per-statement 3-part qualification).

## Key finding — stale MCP server (true before/after)

The `dbmcp-test` server process started before Waves 1–4 merged, so its first probe reproduced
the **pre-fix IDENT-08 bug**: `get_column_info(catalog=cerner_src)` accepted the override but
queried the default bmtct → "No such table abstract_data". After the operator reloaded the
server (picking up merged code), the identical call returned real cerner_src statistics. This
gives a genuine before/after demonstration that the fix — not test scaffolding — changed behavior.

## Human checkpoint

Operator reviewed the captured TOON evidence and approved (all three tools cross-catalog-correct
under the cerner_src override; negative control confirms the bmtct default path; no USE CATALOG
behavior). UAT recorded in `15.1-UAT.md`.

## Artifacts

- `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md` — 7 tests, all pass, IDENT-08 + cerner_src + bmtct referenced.

## Self-Check: PASSED
- [x] All 3 tasks executed (live probes, human checkpoint, UAT recorded)
- [x] 15.1-UAT.md exists, references cerner_src + bmtct + IDENT-08
- [x] Human checkpoint approved
- [x] RESEARCH A2 closed
