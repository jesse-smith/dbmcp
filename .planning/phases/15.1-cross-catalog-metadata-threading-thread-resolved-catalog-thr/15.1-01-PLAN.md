---
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/analysis/_sql.py
  - tests/unit/test_analysis_sql.py
autonomous: true
requirements: [IDENT-08]
must_haves:
  truths:
    - "A single catalog-aware reflection helper exists; the three analysis classes call it instead of copy-pasting a 4th DESCRIBE TABLE / SHOW TABLES block."
    - "The helper builds 3-part backtick-quoted names via dialect.quote_identifier per segment (injection-safe)."
    - "The helper NEVER emits USE CATALOG."
  artifacts:
    - path: "src/analysis/_sql.py"
      provides: "CatalogAwareReflector (catalog-scoped DESCRIBE TABLE columns + SHOW TABLES IN table enumeration over a live connection)"
      contains: "class CatalogAwareReflector"
    - path: "tests/unit/test_analysis_sql.py"
      provides: "Catalog-discriminating unit tests for the reflector"
      contains: "def test_"
  key_links:
    - from: "src/analysis/_sql.py CatalogAwareReflector"
      to: "dialect.quote_identifier"
      via: "per-segment quoting of catalog/schema/table"
      pattern: "quote_identifier"
---

<objective>
Extract the catalog-aware raw-SQL reflection pattern (currently duplicated across
`MetadataService._get_databricks_columns` and `_list_tables_databricks`) into a single
shared helper the three analysis classes will consume. RESEARCH Open Q2: this is the 4th
occurrence of the pattern — Rule of Three is crossed, so extract rather than copy-paste.

Purpose: Give pk_discovery / fk_candidates / column_stats one injection-safe, stateless,
catalog-scoped reflection surface that operates over the live `self.connection` they each
hold (NOT a fresh `engine.connect()` — that is the key difference from MetadataService).
Output: `CatalogAwareReflector` in `src/analysis/_sql.py` plus catalog-discriminating tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-PATTERNS.md

<interfaces>
<!-- Reference analog (the canonical working pattern this helper consolidates). -->
<!-- src/db/metadata.py::_get_databricks_columns (lines 945-996): -->
<!--   builds qualified = f"{qi(catalog)}.{qi(schema)}.{qi(table)}" then -->
<!--   conn.execute(text(f"DESCRIBE TABLE {qualified}")); parses rows[i][0]=col_name, -->
<!--   rows[i][1]=data_type; STOPS at blank col_name or col_name.startswith("#"). -->
<!-- src/db/metadata.py::_list_tables_databricks (lines 352-362): -->
<!--   conn.execute(text(f"SHOW TABLES IN {qi(catalog)}.{qi(schema)}")); -->
<!--   SHOW TABLES returns rows of (database, tableName, isTemporary) -> tableName = row[1]. -->
<!-- src/db/dialects/databricks.py::quote_identifier (lines 124-126): returns f"`{identifier}`". -->
<!-- Existing transpile_query in this same file (lines 11-28) is the structural-SQL helper; -->
<!-- the new reflector is the raw-SQL (non-transpiled, native Databricks) sibling. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — catalog-discriminating tests for CatalogAwareReflector</name>
  <read_first>
    - src/analysis/_sql.py (the file being extended; understand transpile_query's existing shape and TYPE_CHECKING import of DialectStrategy)
    - src/db/metadata.py lines 945-996 (_get_databricks_columns — DESCRIBE TABLE parse contract: stop at blank/"#")
    - src/db/metadata.py lines 345-368 (_list_tables_databricks — SHOW TABLES IN, row[1]=tableName)
    - tests/conftest.py lines 49-69 (the real-dialect `dialect` fixture: @pytest.mark.dialects('databricks') yields a real DatabricksDialect whose quote_identifier emits real backticks)
    - tests/unit/test_metadata.py lines 595-669 (TestCatalogListSchemas — the closest existing catalog-asserting fake: call_args_list SQL inspection + backtick assertion)
  </read_first>
  <behavior>
    - Test: reflect_columns(catalog="cerner_src", schema="dbo", table="orders") executes a DESCRIBE TABLE whose SQL string contains `cerner_src`.`dbo`.`orders` (all three segments backtick-quoted).
    - Test (catalog-discriminating): a fake connection.execute that returns column rows ONLY when the SQL carries the expected 3-part name, else []. Assert reflect_columns returns non-empty for the right catalog -> proves the catalog reached the SQL.
    - Test: reflect_columns parses DESCRIBE output identically to _get_databricks_columns — col_name=row[0], data_type=row[1], STOP at blank col_name or one starting with "#".
    - Test: list_tables(catalog="cerner_src", schema="dbo") executes SHOW TABLES IN `cerner_src`.`dbo` and returns tableName from row[1].
    - Test (statelessness guard): assert "USE CATALOG" not in any executed SQL string.
    - Test (injection): a catalog/schema/table containing a backtick or dot is passed through dialect.quote_identifier (assert the emitted SQL is the quoted form, not raw concatenation).
  </behavior>
  <action>
    Create tests/unit/test_analysis_sql.py. Use the real `dialect` fixture from conftest.py with @pytest.mark.dialects('databricks') so quote_identifier emits real backticks. Build a catalog-discriminating fake `connection` (MagicMock) whose `execute.side_effect` is a function inspecting `str(stmt)` and returning a `_mock_result`-style rows object only when the expected 3-part backtick name is present (mirror the call_args_list assertion style of test_metadata.py TestCatalogListSchemas). Tests MUST fail now because CatalogAwareReflector does not yet exist (import error / AttributeError is the RED signal). Do NOT write the implementation in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_analysis_sql.py -x -q 2>&1 | grep -qE "(ImportError|AttributeError|cannot import|no attribute|collected .* error)" &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>tests/unit/test_analysis_sql.py exists with the six behaviors above and fails because CatalogAwareReflector is not implemented (RED).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement CatalogAwareReflector</name>
  <read_first>
    - src/analysis/_sql.py (file being modified)
    - src/db/metadata.py lines 945-996 and 345-368 (the two patterns being consolidated — copy the parse/row-extraction logic faithfully)
    - tests/unit/test_analysis_sql.py (the RED tests written in Task 1 — these are the contract)
  </read_first>
  <action>
    Add `class CatalogAwareReflector` to src/analysis/_sql.py. Constructor takes `connection` (a live SQLAlchemy Connection) and `dialect` (DialectStrategy). Provide two methods:
    (1) `reflect_columns(catalog, schema, table) -> list[dict]` returning [{"name": col_name, "data_type": data_type}, ...]: build `qualified = f"{qi(catalog)}.{qi(schema)}.{qi(table)}"` via dialect.quote_identifier per segment, run `self.connection.execute(text(f"DESCRIBE TABLE {qualified}"))`, parse rows with the metadata.py contract (col=row[0], type=row[1], STOP at blank or "#" prefix).
    (2) `list_tables(catalog, schema) -> list[str]` returning table names: run `self.connection.execute(text(f"SHOW TABLES IN {qi(catalog)}.{qi(schema)}"))`, take `row[1] if len(row) > 1 else row[0]`.
    Use `self.connection.execute` directly (the analysis classes own a live connection — do NOT open `engine.connect()`). NEVER emit USE CATALOG. Import `text` from sqlalchemy. Keep the TYPE_CHECKING import style already in the file.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_analysis_sql.py -x -q</automated>
  </verify>
  <done>All tests in tests/unit/test_analysis_sql.py pass (GREEN). `grep -v '^#' src/analysis/_sql.py | grep -c "USE CATALOG"` returns 0. CatalogAwareReflector exposes reflect_columns and list_tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool args -> raw SQL | `catalog`/`schema`/`table` originate as tool params and are concatenated into raw DESCRIBE TABLE / SHOW TABLES SQL (Databricks DESCRIBE does not accept bound params). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-01 | Tampering (SQLi) | CatalogAwareReflector.reflect_columns / list_tables | mitigate | Quote EVERY segment via dialect.quote_identifier (backticks) before concatenation — the existing T-11-04/T-mwr-02 control. Injection test in Task 1 asserts quoting. |
| T-15.1-02 | Tampering / Info Disclosure | pooled connection session catalog | mitigate | Stateless 3-part names only; reflector never emits USE CATALOG. grep gate in Task 2 done-criteria. |
| T-15.1-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all libraries are pre-existing pinned deps (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_analysis_sql.py -q` green.
- `uv run ruff check src/analysis/_sql.py` clean.
- `grep -v '^#' src/analysis/_sql.py | grep -c "USE CATALOG"` == 0.
</verification>

<success_criteria>
CatalogAwareReflector exists in src/analysis/_sql.py, is injection-safe (per-segment quoting),
stateless (no USE CATALOG), and operates over a live connection. Catalog-discriminating tests
pass. This is the shared contract Wave 2 plans consume.
</success_criteria>

<output>
Create `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-01-SUMMARY.md` when done.
</output>
