---
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
plan: 02
type: tdd
wave: 2
depends_on: ["15.1-01"]
files_modified:
  - src/analysis/pk_discovery.py
  - tests/unit/test_pk_discovery.py
autonomous: true
requirements: [IDENT-08]
must_haves:
  truths:
    - "PKDiscovery accepts an optional catalog arg; when set on Databricks, its column/constraint/uniqueness reads target that catalog, not the connection default."
    - "_qualified_table is 3-part [catalog].[schema].[table] (TSQL brackets) when catalog is set; sqlglot transpiles it to `cat`.`sch`.`tbl`."
    - "Default-catalog path (catalog=None) is unchanged — Inspector still used; existing tests still pass."
    - "No USE CATALOG is emitted on the cross-catalog path."
  artifacts:
    - path: "src/analysis/pk_discovery.py"
      provides: "Catalog-aware PK candidate discovery"
      contains: "catalog"
    - path: "tests/unit/test_pk_discovery.py"
      provides: "Cross-catalog + qualified-table + no-USE-CATALOG tests (catalog-discriminating fake)"
      contains: "cross_catalog"
  key_links:
    - from: "src/analysis/pk_discovery.py"
      to: "src/analysis/_sql.py CatalogAwareReflector"
      via: "catalog-aware column reflection on the cross-catalog branch"
      pattern: "CatalogAwareReflector"
    - from: "src/analysis/pk_discovery.py _qualified_table"
      to: "transpile_query"
      via: "3-part TSQL bracket name transpiled to Databricks backticks"
      pattern: "_qualified_table"
---

<objective>
Thread `resolved.catalog` into `PKDiscovery` so an explicit non-default catalog targets that
catalog's columns/constraints. Two halves (RESEARCH §Summary): (1) structural/uniqueness SQL
fixed by a 3-part `_qualified_table`; (2) Inspector-backed reads re-routed to the
CatalogAwareReflector raw-SQL path on the Databricks cross-catalog branch.

Purpose: Eliminate the CR-02 silent mis-targeting for find_pk_candidates.
Output: catalog-aware PKDiscovery + catalog-discriminating regression tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-PATTERNS.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-01-SUMMARY.md

<interfaces>
<!-- From src/analysis/_sql.py (Plan 15.1-01 output): -->
<!--   class CatalogAwareReflector(connection, dialect) -->
<!--     .reflect_columns(catalog, schema, table) -> list[{"name","data_type"}] -->
<!--     .list_tables(catalog, schema) -> list[str] -->
<!--   transpile_query(sql, dialect) -> str  (TSQL [brackets] -> dialect backticks) -->
<!-- Current PKDiscovery.__init__ (lines 39-52): -->
<!--   (connection, schema_name, table_name, dialect=None, inspector=None) -->
<!--   self._qualified_table = f"[{schema_name}].[{table_name}]"  (2-part TSQL) -->
<!-- Catalog-blind Inspector reads to re-route on cross-catalog branch: -->
<!--   _get_constraint_candidates_inspector lines 168-176 (get_columns, get_pk_constraint) -->
<!--   line 193 (get_unique_constraints) -->
<!--   _list_all_columns lines 265-272 (get_columns) -->
<!--   _column_is_unique lines 290-310 (already interpolates _qualified_table + transpile_query) -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — cross-catalog + qualified-table + no-USE-CATALOG tests</name>
  <read_first>
    - src/analysis/pk_discovery.py (full — constructor lines 39-52, the Inspector call sites lines 168-176/193/265-272, _column_is_unique lines 290-310)
    - tests/unit/test_pk_discovery.py lines 30-55 (existing catalog-BLIND side_effect-list mock — the pattern to UPGRADE, NOT reuse)
    - tests/conftest.py lines 49-69 (real `dialect` fixture)
    - tests/unit/test_metadata.py lines 595-669 (catalog-asserting call_args_list pattern)
    - src/analysis/_sql.py (CatalogAwareReflector contract from Plan 01)
  </read_first>
  <behavior>
    - test_cross_catalog_pk_targets_requested_catalog: construct PKDiscovery(..., catalog="cerner_src") with a catalog-discriminating fake connection that returns PK rows ONLY when the SQL carries `cerner_src`.`dbo`.`orders`; assert candidates non-empty (proves catalog reached the SQL). Without the catalog arg the same fake returns [] -> would be the CR-02 false negative.
    - test_qualified_table_three_part: assert PKDiscovery(..., catalog="cat")._qualified_table == "[cat].[sch].[tbl]" AND sqlglot.transpile(that, read="tsql", write="databricks")[0] == "... `cat`.`sch`.`tbl` ...".
    - test_no_use_catalog: spy on connection.execute; assert "USE CATALOG" not in any executed SQL on the cross-catalog branch.
    - test_default_path_unchanged: PKDiscovery(..., catalog=None) still routes through the Inspector (existing behavior) — assert inspector.get_columns/get_pk_constraint still called, reflector NOT used.
  </behavior>
  <action>
    Extend tests/unit/test_pk_discovery.py with a TestCrossCatalogPK class. Build a catalog-discriminating fake connection (function-style execute.side_effect inspecting str(stmt), asserting no "USE CATALOG", returning rows only for the expected 3-part name) — do NOT reuse the order-dependent list mock at lines 45-48. Use the real `dialect` fixture (@pytest.mark.dialects('databricks')). Tests MUST fail now (PKDiscovery has no catalog kwarg -> TypeError). Do not implement.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pk_discovery.py -k "cross_catalog or qualified_table or no_use_catalog" -x -q 2>&1 | grep -qE "(TypeError|unexpected keyword|FAILED|error)" &amp;&amp; echo RED-OK</automated>
  </verify>
  <done>New cross-catalog tests exist and fail because PKDiscovery does not accept `catalog` (RED).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — thread catalog through PKDiscovery</name>
  <read_first>
    - src/analysis/pk_discovery.py (file being modified)
    - tests/unit/test_pk_discovery.py (the RED tests — contract)
    - src/analysis/_sql.py CatalogAwareReflector (Plan 01)
    - src/db/metadata.py lines 945-996 (constraint reflection reference: PK/UNIQUE via information_schema qualified to catalog when Inspector is catalog-blind)
  </read_first>
  <action>
    Add `catalog: str | None = None` to PKDiscovery.__init__; store `self._catalog`. Build `self._qualified_table = f"[{catalog}].[{schema_name}].[{table_name}]"` when catalog is set, else the existing 2-part form (TSQL brackets — NOT backticks; Pitfall 4). Add a guard `self._cross_catalog = bool(catalog) and dialect is not None and dialect.name == "databricks"`. On the cross-catalog branch, replace the Inspector reads at lines 168-176/193/265-272 with CatalogAwareReflector(self.connection, self._dialect).reflect_columns(self._catalog, self.schema_name, self.table_name) for columns, and catalog-scoped information_schema.table_constraints / key_column_usage queries (qualified to self._catalog, every segment quoted) for PK/UNIQUE constraints. The default-catalog branch (catalog=None) keeps the existing Inspector calls verbatim — no behavior drift. The structural uniqueness path (_column_is_unique) is fixed automatically by the 3-part _qualified_table (it already runs through transpile_query).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_pk_discovery.py -x -q</automated>
  </verify>
  <done>All test_pk_discovery.py tests pass (new + pre-existing regression). `grep -v '^#' src/analysis/pk_discovery.py | grep -c "USE CATALOG"` == 0. Default-path tests unchanged and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP tool args -> raw SQL | catalog/schema/table flow into DESCRIBE TABLE + information_schema 3-part SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-03 | Tampering (SQLi) | PKDiscovery cross-catalog constraint/column reads | mitigate | All segments quoted via dialect.quote_identifier (delegated to CatalogAwareReflector + information_schema query); injection covered by Plan 01 + reuse. |
| T-15.1-04 | Tampering / Info Disclosure | pooled connection catalog state | mitigate | No USE CATALOG (stateless 3-part); test_no_use_catalog + grep gate. |
| T-15.1-05 | Information Disclosure | cross-catalog reach | accept | Unity Catalog ACLs enforce server-side; queries run as connected principal; no new exposure beyond list_schemas/get_sample_data (RESEARCH Security V4). |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_pk_discovery.py -q` green (new + regression).
- `uv run ruff check src/analysis/pk_discovery.py` clean.
- `grep -v '^#' src/analysis/pk_discovery.py | grep -c "USE CATALOG"` == 0.
</verification>

<success_criteria>
find_pk_candidates with a non-default Databricks catalog targets that catalog's
columns/constraints; default path unchanged; no USE CATALOG; MSSQL/generic gate
unchanged (resolver still rejects catalog upstream — untouched here).
</success_criteria>

<output>
Create `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-02-SUMMARY.md` when done.
</output>
