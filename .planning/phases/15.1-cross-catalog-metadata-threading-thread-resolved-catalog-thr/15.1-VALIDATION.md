---
phase: 15.1
slug: cross-catalog-metadata-threading-thread-resolved-catalog-thr
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-29
---

# Phase 15.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 8.x (uv-managed) |
| **Config file** | pyproject.toml / pytest.ini |
| **Quick run command** | `uv run pytest tests/unit/test_metadata.py tests/unit/test_analysis_tools.py -q` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~60–90 seconds (full); <10s (quick) |

---

## Sampling Rate

- **After every task commit:** Run quick command (targeted metadata/analysis tests)
- **After every plan wave:** Run full suite command
- **Before `/gsd:verify-work`:** Full suite must be green; coverage ≥ 85% floor
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

> Populated by gsd-planner during planning. One row per task.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | — | — | IDENT-08 (proposed) | — | — | unit | TBD | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Populated by gsd-planner. Key constraint from RESEARCH.md: unit fakes MUST be
> **catalog-discriminating** — a fake that returns the same constraints regardless
> of the catalog argument cannot detect the silent mis-targeting bug being fixed.

- [ ] Catalog-discriminating fake/fixture for Inspector + raw-SQL paths — TBD
- [ ] Regression test asserting cross-catalog request targets requested catalog, not default — TBD

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cross-catalog FK/PK/column-stats against a live Databricks warehouse with a SECOND accessible catalog | IDENT-08 (proposed) | Requires live Unity Catalog with ≥2 catalogs; SQLAlchemy reflection behavior against real metastore cannot be fully faked | Connect to live Databricks; call find_pk_candidates / find_fk_candidates / get_column_info with explicit catalog= differing from connection default; assert results come from the requested catalog. Live warehouse confirmed available (see memory: live-databricks-available). |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (catalog-discriminating fakes)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
