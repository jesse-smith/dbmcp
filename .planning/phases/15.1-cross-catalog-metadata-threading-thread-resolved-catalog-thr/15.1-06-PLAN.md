---
phase: 15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr
plan: 06
type: execute
wave: 4
depends_on: ["15.1-05"]
files_modified:
  - .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md
autonomous: false
requirements: [IDENT-08]
must_haves:
  truths:
    - "Against a live Databricks warehouse (default catalog bmtct, second catalog cerner_src), all three tools called with an explicit cerner_src override return results FROM cerner_src."
    - "Negative control: the same calls without the override return the bmtct/default result (or not-found) — proving the override is what changed the target."
    - "Results recorded in 15.1-UAT.md following the Phase 15 15-UAT.md precedent."
  artifacts:
    - path: ".planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md"
      provides: "Live cross-catalog UAT evidence for find_pk_candidates, find_fk_candidates, get_column_info"
      contains: "cerner_src"
  key_links:
    - from: "dbmcp-test MCP server (live)"
      to: "cerner_src catalog tables"
      via: "explicit catalog override on the three analysis tools"
      pattern: "cerner_src"
---

<objective>
Phase gate before /gsd:verify-work: prove the cross-catalog fix works against a REAL
Databricks warehouse, not just catalog-discriminating mocks. The unit tests prove the SQL
carries the catalog; this UAT proves real Unity Catalog metadata comes back from the
non-default catalog. Mirrors the Phase 15 15-UAT.md method via the `dbmcp-test` MCP server.

Purpose: Close RESEARCH assumption A2 (live multi-catalog) and produce the human-verified
evidence the phase requires.
Output: 15.1-UAT.md recording pass/fail for all three tools + the negative control.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md
@.planning/phases/15-unified-identifier-resolver-cross-dialect/15-UAT.md
@.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reload dbmcp-test and run the live cross-catalog probes</name>
  <read_first>
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-UAT.md (the precedent format — Tests 4-7 cross-catalog method, pass/fail recording)
    - .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-RESEARCH.md Validation Architecture "Live-Databricks confirmation" (the exact 6-step method)
  </read_first>
  <action>
    Ensure the `dbmcp-test` MCP server reflects the merged Wave 1-3 code (it is the dev mirror;
    reload if needed). Connect via dbmcp-test with default catalog bmtct. Identify a table that
    exists in cerner_src (the second readable catalog) — use list_schemas/list_tables on
    cerner_src to pick a concrete schema.table with a known PK column and non-trivial row count.
    Then for that cerner_src table run, with an explicit catalog override targeting cerner_src:
    (1) find_pk_candidates — assert PK candidate columns reflect the cerner_src table;
    (2) find_fk_candidates on a column of that table — assert candidates/targets come from cerner_src;
    (3) get_column_info — assert row/distinct counts reflect cerner_src data, not bmtct.
    Negative control: repeat each call WITHOUT the override and confirm it returns the
    bmtct/default result (or a not-found error) — proving the override changed the target.
    Capture the actual tool responses (TOON) for the record.
  </action>
  <verify>
    <human-check>Operator confirms each of the 3 tools returns cerner_src-sourced metadata under the override and the default/bmtct result (or not-found) without it.</human-check>
  </verify>
  <done>All three tools verified cross-catalog against cerner_src; negative control confirms the default path is unchanged; raw responses captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — confirm live cross-catalog evidence</name>
  <what-built>Cross-catalog targeting for find_pk_candidates, find_fk_candidates, and get_column_info verified live against the dbmcp-test MCP server (default catalog bmtct, override to cerner_src).</what-built>
  <how-to-verify>
    1. Review the captured TOON responses in the working notes from Task 1.
    2. Confirm: under the cerner_src override, each tool's output reflects the cerner_src table (PK column / row+distinct counts match cerner_src, not bmtct).
    3. Confirm: without the override, each call returns the bmtct/default result or a not-found error.
    4. Confirm no errors mentioning USE CATALOG or stale-catalog behavior.
  </how-to-verify>
  <action>Pause for the operator to review the Task 1 evidence per the how-to-verify steps and approve or report a discrepancy. No code changes in this task — it is a verification gate.</action>
  <verify><human-check>Operator types "approved" after confirming all four how-to-verify points.</human-check></verify>
  <done>Operator approves: all three tools cross-catalog-correct under the cerner_src override; negative control confirms the bmtct/default path; no USE CATALOG behavior.</done>
  <resume-signal>Type "approved" to record the UAT as passed, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record results in 15.1-UAT.md</name>
  <read_first>
    - .planning/phases/15-unified-identifier-resolver-cross-dialect/15-UAT.md (format to mirror)
  </read_first>
  <action>
    Write .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md mirroring the 15-UAT.md structure: a row per tool (find_pk_candidates, find_fk_candidates, get_column_info) with the cerner_src override result and the bmtct negative-control result, plus an overall PASS/FAIL verdict and the connection details (default bmtct, second cerner_src, dbmcp-test). Reference IDENT-08.
  </action>
  <verify>
    <automated>test -f .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md &amp;&amp; grep -q "cerner_src" .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md &amp;&amp; grep -q "bmtct" .planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-UAT.md</automated>
  </verify>
  <done>15.1-UAT.md exists, records all three tools + negative control, references bmtct + cerner_src + IDENT-08, with a clear verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live operator -> dbmcp-test -> Databricks | Manual UAT issues queries as the connected principal against a real Unity Catalog metastore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-15 | Information Disclosure | live cross-catalog read of cerner_src | accept | Read-only metadata/stats as the connected principal; Unity Catalog ACLs enforce; no write, no new credential. |
| T-15.1-16 | Tampering | pooled connection catalog state in live run | mitigate | UAT explicitly confirms no USE CATALOG / stale-catalog behavior across repeated calls (negative control on the same pooled connection). |
</threat_model>

<verification>
- 15.1-UAT.md exists and references cerner_src + bmtct + IDENT-08.
- Human checkpoint approved (all three tools cross-catalog-correct; negative control confirms default path).
</verification>

<success_criteria>
Live evidence that the explicit catalog override targets cerner_src for all three tools, and
that the default (bmtct) path is unchanged, recorded in 15.1-UAT.md — RESEARCH A2 closed.
</success_criteria>

<output>
Create `.planning/phases/15.1-cross-catalog-metadata-threading-thread-resolved-catalog-thr/15.1-06-SUMMARY.md` when done.
</output>
