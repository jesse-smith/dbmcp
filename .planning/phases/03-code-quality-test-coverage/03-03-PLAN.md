---
phase: 03-code-quality-test-coverage
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - pyproject.toml
  - codecov.yml
  - tests/unit/test_metadata.py
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "uv run pytest -m 'not integration' --cov reports 70%+ for every module under src/"
    - "pyproject.toml contains fail_under = 70 in coverage.report section"
    - "codecov.yml enforces absolute 70% target with 1% threshold"
    - "metadata.py coverage is at or above 70%"
  artifacts:
    - path: "pyproject.toml"
      provides: "Coverage fail_under = 70 configuration"
      contains: "fail_under = 70"
    - path: "codecov.yml"
      provides: "Absolute 70% coverage target for CI"
      contains: "target: 70%"
    - path: "tests/unit/test_metadata.py"
      provides: "New tests covering metadata.py gap lines"
  key_links:
    - from: "pyproject.toml"
      to: "codecov.yml"
      via: "Both enforce 70% floor"
      pattern: "fail_under|target: 70"
    - from: "tests/unit/test_metadata.py"
      to: "src/db/metadata.py"
      via: "Tests cover uncovered lines"
      pattern: "MetadataService|inspector"
---

<objective>
Configure coverage enforcement and fill the remaining test gap to achieve 70%+ on all modules.

Purpose: TEST-02 establishes CI-enforceable coverage baselines. TEST-01 ensures every module meets the 70% floor -- after Plan 01 removes metrics.py (0%), only metadata.py (67%) needs new tests.
Output: Coverage config in pyproject.toml and codecov.yml, metadata.py tests bringing it to 70%+.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-code-quality-test-coverage/03-RESEARCH.md
@.planning/phases/03-code-quality-test-coverage/03-01-SUMMARY.md
@.planning/phases/03-code-quality-test-coverage/03-02-SUMMARY.md

@pyproject.toml
@codecov.yml
@tests/unit/test_metadata.py
@src/db/metadata.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure coverage enforcement in pyproject.toml and codecov.yml</name>
  <files>pyproject.toml, codecov.yml</files>
  <action>
**pyproject.toml:**
Add `fail_under = 70` to the existing `[tool.coverage.report]` section. The section already exists with `exclude_lines` -- just add the fail_under line. Per user decision, this applies to unit-only test runs (`-m "not integration"`).

Per user decision, the 70% baseline applies to unit-only test runs. Integration tests are bonus coverage with no hard threshold.

**codecov.yml:**
Update the existing codecov.yml to enforce absolute 70% target. The current config has `range: "70...90"` and `auto` targets. Update to:
```yaml
coverage:
  range: "70...90"
  status:
    project:
      default:
        target: 70%
        threshold: 1%
    patch:
      default:
        target: auto
        threshold: 1%
```

Per user decision: absolute 70% target alongside 1% threshold for regression protection.

**Verify:** Run `uv run pytest -m "not integration" --cov=src --cov-report=term-missing` and confirm it does NOT fail (current total is ~80% after metrics.py removal, well above 70%). The `fail_under` should exit 0.
  </action>
  <verify>
    <automated>grep "fail_under" pyproject.toml && grep "target: 70" codecov.yml && uv run pytest -m "not integration" --cov=src -q</automated>
  </verify>
  <done>pyproject.toml has fail_under = 70, codecov.yml has absolute 70% target with 1% threshold, pytest --cov passes with the new floor</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill metadata.py test gap to reach 70%+ coverage</name>
  <files>tests/unit/test_metadata.py</files>
  <behavior>
    - Test: MetadataService handles SQLAlchemyError on get_schema_names gracefully (returns fallback)
    - Test: MetadataService handles SQLAlchemyError on get_table_comment gracefully (returns None)
    - Test: MetadataService handles SQLAlchemyError on table_exists gracefully (returns False)
    - Test: MetadataService handles SQLAlchemyError on get_indexes gracefully (returns empty list)
    - Test: MetadataService handles SQLAlchemyError on get_primary_key gracefully (returns empty list)
    - Test: MetadataService get_row_count returns None on SQLAlchemyError
    - Test: Coverage for the _list_schemas_mssql path or mark MSSQL-specific DMV queries with pragma: no cover (lines 89-120 execute raw SQL against DMVs that cannot be tested with SQLite mocks)
    - Test: Coverage for _list_tables_mssql path or mark with pragma: no cover (lines 389-508, same reason)
  </behavior>
  <action>
Add new test cases to the existing `tests/unit/test_metadata.py` to cover the uncovered error paths in metadata.py. The current coverage is 67% with these uncovered areas:

**Testable error paths (mock inspector to raise SQLAlchemyError):**
1. `get_schema_names` error fallback (line 130) -- mock inspector.get_schema_names to raise SQLAlchemyError, verify returns `[None]`
2. `get_table_comment` error fallback (line 290) -- mock inspector to raise, verify returns None/empty
3. `table_exists` error path (lines 709-713) -- mock to raise, verify returns False
4. `get_indexes` error paths (lines 579-592) -- mock to raise, verify returns empty list
5. `get_primary_key` error paths (lines 622-626) -- mock to raise, verify returns empty list
6. `_get_row_count` error path (line 372) -- mock connection.execute to raise, verify returns None

**MSSQL-specific code (lines 89-120, 389-508):**
These execute raw SQL against SQL Server DMVs and cannot be meaningfully unit tested with SQLite mocks. Two options:
- Option A: Mark with `# pragma: no cover` (cleaner -- these are integration-test territory)
- Option B: Mock the raw connection to return expected shapes

Use Option A (pragma: no cover) for the MSSQL-specific DMV query blocks IF needed to reach 70%. Try the error path tests first -- they may be sufficient to reach 70% without pragma: no cover.

**After adding tests, verify:**
`uv run pytest -m "not integration" --cov=src/db/metadata.py --cov-report=term-missing -q`
metadata.py must report >= 70% coverage.

**Final full suite validation:**
`uv run pytest -m "not integration" --cov=src --cov-report=term-missing`
Every module must be >= 70%. The `fail_under = 70` (total) should pass.

NOTE: Use SQLAlchemyError (not Exception) for mocking side effects since Plan 02 narrowed the handlers. Import from `sqlalchemy.exc`.
  </action>
  <verify>
    <automated>uv run pytest -m "not integration" --cov=src --cov-report=term-missing 2>&1 | tail -30</automated>
  </verify>
  <done>metadata.py at 70%+ coverage, all modules at 70%+, fail_under = 70 passes, full test suite green</done>
</task>

</tasks>

<verification>
- `uv run pytest -m "not integration" --cov=src --cov-report=term-missing` shows all modules >= 70%
- `uv run pytest -m "not integration" --cov=src` exits 0 (fail_under = 70 passes)
- `grep "fail_under = 70" pyproject.toml` returns a match
- `grep "target: 70" codecov.yml` returns a match
- `uv run ruff check src/ tests/` clean

Phase-level verification (all requirements):
- QUAL-01: `test -f src/metrics.py` returns false (from Plan 01)
- QUAL-02: `grep -rn "except Exception" src/ | grep -v mcp_server/` returns 0 lines (from Plan 02)
- QUAL-03: `grep "type: ignore" src/db/query.py` returns 0 lines (from Plan 01)
- TEST-01: All modules >= 70% coverage
- TEST-02: fail_under = 70 configured in pyproject.toml, codecov.yml has absolute target
</verification>

<success_criteria>
- Coverage config established with CI enforcement at 70% floor
- metadata.py reaches 70%+ coverage with new unit tests
- All modules pass the 70% threshold
- Full test suite green with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality-test-coverage/03-03-SUMMARY.md`
</output>
