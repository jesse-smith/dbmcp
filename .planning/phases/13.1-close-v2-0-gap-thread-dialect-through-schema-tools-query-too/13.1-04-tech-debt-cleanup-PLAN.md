---
phase: 13.1
plan: 04
title: Tech debt cleanup — TD-01 (ROADMAP), TD-02 (REQUIREMENTS), TD-03 (SUMMARY frontmatter), TD-04 (metrics ruff)
type: execute
wave: 2
depends_on:
  - 13.1-01
  - 13.1-02
  - 13.1-03
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/08-dialect-protocol-mssql-extraction/08-01-SUMMARY.md
  - .planning/phases/08-dialect-protocol-mssql-extraction/08-02-SUMMARY.md
  - .planning/phases/08-dialect-protocol-mssql-extraction/08-03-SUMMARY.md
  - .planning/phases/09-config-discrimination-validation-dialect/09-01-SUMMARY.md
  - .planning/phases/09-config-discrimination-validation-dialect/09-02-SUMMARY.md
  - .planning/phases/10-genericdialect-tool-interface/10-01-SUMMARY.md
  - .planning/phases/10-genericdialect-tool-interface/10-02-SUMMARY.md
  - .planning/phases/10-genericdialect-tool-interface/10-03-SUMMARY.md
  - .planning/phases/11-databricksdialect/11-01-SUMMARY.md
  - .planning/phases/11-databricksdialect/11-02-SUMMARY.md
  - .planning/phases/12-analysis-module-adaptation/12-01-SUMMARY.md
  - .planning/phases/12-analysis-module-adaptation/12-02-SUMMARY.md
  - .planning/phases/13-test-infrastructure-coverage/13-01-SUMMARY.md
  - .planning/phases/13-test-infrastructure-coverage/13-02-SUMMARY.md
  - .planning/phases/13-test-infrastructure-coverage/13-03-SUMMARY.md
  - .planning/phases/13-test-infrastructure-coverage/13-04-SUMMARY.md
autonomous: true
risk: low
requirements:
  - META-01
  - VALID-01

must_haves:
  truths:
    - "ROADMAP.md Phase 9 success-criterion #1 text matches implementation (dialect is required, not defaulted to mssql)."
    - "REQUIREMENTS.md TEST-03 wording reads '85%+' to match implementation floor."
    - "Every SUMMARY.md frontmatter `requirements-completed` list for phases 8-13 includes all REQ IDs marked SATISFIED in that phase's VERIFICATION.md table."
    - "ruff checks pass clean on src/ (TD-04 pre-existing metrics.py warning verified resolved or file absent)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 success-criterion #1 text aligned with D-02 (dialect required)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TEST-03 row reads '85%+ maintained'"
    - path: ".planning/phases/*/*-SUMMARY.md"
      provides: "requirements-completed frontmatter matches VERIFICATION.md SATISFIED rows"
  key_links:
    - from: "VERIFICATION.md SATISFIED rows"
      to: "SUMMARY.md requirements-completed frontmatter"
      via: "mechanical reconciliation per phase"
      pattern: "requirements-completed:"
---

<objective>
Clean up v2.0 tech debt captured in `.planning/v2.0-MILESTONE-AUDIT.md`:

- **TD-01:** Update `ROADMAP.md` Phase 9 success-criterion #1 text (currently says "omitting dialect defaults to mssql") to match D-02 (dialect is required).
- **TD-02:** Update `REQUIREMENTS.md` TEST-03 row wording from "70%+" to "85%+" (matches Phase 13 implementation floor).
- **TD-03:** For each phase (8, 9, 10, 11, 12, 13), reconcile every plan's `SUMMARY.md` `requirements-completed` frontmatter against that phase's `VERIFICATION.md` SATISFIED rows. Add any REQ IDs marked SATISFIED in VERIFICATION but absent from SUMMARY frontmatter. Audit flagged examples: DIAL-01, DIAL-05, CONF-01/02/03, VALID-01/02/03, TEST-02/03 — but reconcile every SUMMARY against its VERIFICATION, not only these.
- **TD-04:** Verify `uv run ruff check src/` passes clean. Per initial-context check the source file `src/metrics.py` referenced in the audit does not exist in the current tree; the flagged warning is already resolved (no ruff failures on src/). Confirm and record.

Runs in wave 2 after the three wiring plans land so reconciliation reflects the clean v2.0 state.

Purpose: Clear the v2.0-MILESTONE-AUDIT.md tech_debt block so future milestone audits (which rely on frontmatter) don't false-flag satisfied requirements.
Output: 2 small doc edits + 16 SUMMARY reconciliations + ruff verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.0-MILESTONE-AUDIT.md
@.planning/phases/13.1-close-v2-0-gap-thread-dialect-through-schema-tools-query-too/13.1-CONTEXT.md
@.planning/phases/09-config-discrimination-validation-dialect/09-CONTEXT.md
@.planning/phases/13-test-infrastructure-coverage/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TD-01, TD-02, TD-04 — ROADMAP/REQUIREMENTS text edits + ruff verification</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/ROADMAP.md (Phase 9 section starting with "### Phase 9: Config Discrimination & Validation Dialect"; success-criterion #1 to edit: "TOML config with `dialect` field correctly routes to dialect-specific config models; omitting `dialect` defaults to \"mssql\" (backward compatible)")
    - .planning/REQUIREMENTS.md (TEST-03 row in Testing section: "70%+ test coverage maintained across all modules"; also the Traceability table row for TEST-03 — note the existing file has a line break anomaly on TEST-02/TEST-03 rows that should be preserved or cleaned as part of the edit)
    - .planning/phases/09-config-discrimination-validation-dialect/09-CONTEXT.md (D-02 decision: dialect is required)
  </read_first>

  <action>
**TD-01** — edit `.planning/ROADMAP.md`:

Locate the Phase 9 success-criteria block:

```
### Phase 9: Config Discrimination & Validation Dialect
**Goal**: Users can configure non-MSSQL connections via TOML and execute validated queries against any supported dialect
**Depends on**: Phase 8
**Requirements**: CONF-01, CONF-02, VALID-01, VALID-02, VALID-03
**Success Criteria** (what must be TRUE):
  1. TOML config with `dialect` field correctly routes to dialect-specific config models; omitting `dialect` defaults to "mssql" (backward compatible)
```

Replace success-criterion #1 with the exact text:

```
  1. TOML config requires an explicit `dialect` field (per D-02 in 09-CONTEXT.md) and correctly routes to dialect-specific config models; omitting `dialect` is a validation error
```

Leave criteria 2–5 unchanged.

**TD-02** — edit `.planning/REQUIREMENTS.md`:

Locate the TEST-03 bullet (currently split across lines 53-54 in the file):

```
- [x] **TEST-03
**: 70%+ test coverage maintained across all modules
```

Replace with:

```
- [x] **TEST-03**: 85%+ test coverage maintained across all modules
```

(Collapse the stray line break; update "70%+" to "85%+".)

No other changes to REQUIREMENTS.md.

**TD-04** — verify ruff:

Run `uv run ruff check src/` and confirm exit code 0 and "All checks passed!" output. If `src/metrics.py` exists and has a `from typing import Generator` line, change it to `from collections.abc import Generator` and re-run ruff. Otherwise (file absent — which is the current state per initial context), record in the SUMMARY that TD-04 was verified resolved (no code change needed — the flagged file is not present; ruff passes clean on `src/`).
  </action>

  <verify>
    <automated>grep -q "omitting \`dialect\` is a validation error" .planning/ROADMAP.md && grep -q "85%+ test coverage maintained across all modules" .planning/REQUIREMENTS.md && ! grep -q "omitting \`dialect\` defaults to \"mssql\"" .planning/ROADMAP.md && ! grep -q "70%+ test coverage maintained" .planning/REQUIREMENTS.md && uv run ruff check src/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "omitting \`dialect\` is a validation error" .planning/ROADMAP.md` returns ≥1 match.
    - `grep -n "omitting \`dialect\` defaults to \"mssql\"" .planning/ROADMAP.md` returns 0 matches.
    - `grep -n "85%+ test coverage maintained across all modules" .planning/REQUIREMENTS.md` returns ≥1 match.
    - `grep -n "70%+ test coverage maintained" .planning/REQUIREMENTS.md` returns 0 matches.
    - `uv run ruff check src/` exits 0.
  </acceptance_criteria>

  <done>
    ROADMAP Phase 9 success-criterion #1 aligned with D-02; REQUIREMENTS TEST-03 text shows 85%+; ruff check on src/ passes clean (TD-04 resolution confirmed).
  </done>
</task>

<task type="auto">
  <name>Task 2: TD-03 — reconcile SUMMARY.md requirements-completed frontmatter against VERIFICATION.md per phase</name>
  <files>
    .planning/phases/08-dialect-protocol-mssql-extraction/08-01-SUMMARY.md,
    .planning/phases/08-dialect-protocol-mssql-extraction/08-02-SUMMARY.md,
    .planning/phases/08-dialect-protocol-mssql-extraction/08-03-SUMMARY.md,
    .planning/phases/09-config-discrimination-validation-dialect/09-01-SUMMARY.md,
    .planning/phases/09-config-discrimination-validation-dialect/09-02-SUMMARY.md,
    .planning/phases/10-genericdialect-tool-interface/10-01-SUMMARY.md,
    .planning/phases/10-genericdialect-tool-interface/10-02-SUMMARY.md,
    .planning/phases/10-genericdialect-tool-interface/10-03-SUMMARY.md,
    .planning/phases/11-databricksdialect/11-01-SUMMARY.md,
    .planning/phases/11-databricksdialect/11-02-SUMMARY.md,
    .planning/phases/12-analysis-module-adaptation/12-01-SUMMARY.md,
    .planning/phases/12-analysis-module-adaptation/12-02-SUMMARY.md,
    .planning/phases/13-test-infrastructure-coverage/13-01-SUMMARY.md,
    .planning/phases/13-test-infrastructure-coverage/13-02-SUMMARY.md,
    .planning/phases/13-test-infrastructure-coverage/13-03-SUMMARY.md,
    .planning/phases/13-test-infrastructure-coverage/13-04-SUMMARY.md
  </files>

  <read_first>
    For each phase in {08, 09, 10, 11, 12, 13}:
    - `.planning/phases/{phase-dir}/{phase}-VERIFICATION.md` — find the requirements table; identify every row where status is SATISFIED (or equivalent ✓ / complete / passed marker used in that file). Collect the set of REQ IDs (e.g., DIAL-01, CONF-01, VALID-01, META-05, TEST-02, etc.).
    - Every `{phase}-NN-SUMMARY.md` in that phase dir — read the frontmatter `requirements-completed:` list. Determine the union across all plans in the phase (many phases distribute REQs across plans; a REQ can appear in whichever plan actually implemented it).
    - Audit CONTEXT hint — per CONTEXT `<decisions>` TD-03: flagged likely-missing IDs are DIAL-01, DIAL-05, CONF-01/02/03, VALID-01/02/03, TEST-02/03 — but reconcile against VERIFICATION, not only this list.
  </read_first>

  <action>
For each of the 6 phases (08, 09, 10, 11, 12, 13), do the following mechanical reconciliation:

1. Read `{phase-dir}/{phase}-VERIFICATION.md`. Extract the set **V** = { REQ IDs marked SATISFIED / ✓ / complete / passed }.

2. Read every `{phase}-NN-SUMMARY.md` in the phase dir. For each SUMMARY, extract the existing `requirements-completed:` list from its YAML frontmatter. Call the union across all plans in the phase **S**.

3. Compute the gap **G = V − S**. For every REQ ID in **G**:
   - Decide which plan within the phase most plausibly implemented that REQ. Use the plan titles / brief descriptions in the SUMMARYs as the guide (e.g., in Phase 8: DIAL-01 → 08-01 which defined the protocol; DIAL-02 → 08-02 which extracted MssqlDialect; DIAL-05 → 08-01 which defined the registry; META-05 → 08-03 which wired dialect into ConnectionManager/MetadataService/QueryService).
   - If a REQ's implementation truly spans multiple plans (rare — prefer assigning to the plan where the primary artifact landed), pick the plan whose SUMMARY already lists its closest sibling REQs.
   - Add the REQ ID to that plan SUMMARY's `requirements-completed:` frontmatter list. Preserve YAML formatting (likely a list-of-strings under the key; match the existing formatting of other entries in that file).

4. Do NOT remove any existing frontmatter entries. Do NOT change any other frontmatter fields. Do NOT touch the SUMMARY body.

5. If VERIFICATION shows a REQ as PARTIAL (not fully SATISFIED), do NOT add it — the SUMMARY should list only fully satisfied REQs. For v2.0, VALID-01/VALID-02 are marked SATISFIED in Phase 9 VERIFICATION (the integration degradation is fixed by this phase 13.1's earlier plans), so they SHOULD appear in Phase 9 SUMMARY frontmatter.

6. After updates, produce a concise reconciliation report for the SUMMARY of this plan listing, per phase, the set G that was added and which plan received each ID.

Concretely, the frontmatter pattern to match/extend will look like this (example — actual key name may be `requirements-completed` or `requirements_completed` depending on how earlier phases wrote it; preserve whichever form each SUMMARY already uses — DO NOT normalize across files in this task):

```yaml
---
phase: 08
plan: 01
requirements-completed:
  - DIAL-01
  - DIAL-05
---
```

Add missing IDs as additional list entries under the same key.
  </action>

  <verify>
    <automated>
      for p in 08-dialect-protocol-mssql-extraction 09-config-discrimination-validation-dialect 10-genericdialect-tool-interface 11-databricksdialect 12-analysis-module-adaptation 13-test-infrastructure-coverage; do
        # Collect SATISFIED REQ IDs from VERIFICATION.md for the phase
        ver=".planning/phases/$p/$(echo $p | cut -c1-2)-VERIFICATION.md"
        [ -f "$ver" ] || { echo "missing $ver"; exit 1; }
        # Filter to SATISFIED-only lines before extracting REQ IDs so PARTIAL/NOT-SATISFIED
        # rows or narrative mentions don't produce false-positive "missing" flags.
        verified_ids=$(grep -E '(SATISFIED|\bcomplete\b|✓)' "$ver" | grep -oE '\b(DIAL|CONF|VALID|META|ANLYS|TEST)-[0-9]+\b' | sort -u)
        # Collect union of requirements-completed across all plan SUMMARYs in phase
        union_ids=$(grep -h -A20 'requirements[_-]completed:' .planning/phases/$p/*-SUMMARY.md 2>/dev/null | grep -oE '\b(DIAL|CONF|VALID|META|ANLYS|TEST)-[0-9]+\b' | sort -u)
        missing=$(comm -23 <(echo "$verified_ids") <(echo "$union_ids"))
        if [ -n "$missing" ]; then
          echo "Phase $p: SUMMARY frontmatter still missing: $missing"
          exit 1
        fi
      done
      echo "All 6 phases reconciled."
    </automated>
  </verify>

  <acceptance_criteria>
    - For each phase in {08, 09, 10, 11, 12, 13}: the union of `requirements-completed` REQ IDs across all plan SUMMARY frontmatters is a superset of the REQ IDs marked SATISFIED in that phase's VERIFICATION.md.
    - The automated grep-based reconciliation script above exits 0 for all 6 phases.
    - Specifically, the frontmatter union now includes (at minimum) the audit-flagged IDs in their home phases: Phase 8 includes DIAL-01 and DIAL-05; Phase 9 includes CONF-01, CONF-02, CONF-03 (if present in VERIFICATION), VALID-01, VALID-02, VALID-03; Phase 13 includes TEST-02 and TEST-03.
    - No existing frontmatter entries were removed; no SUMMARY body content was modified.
  </acceptance_criteria>

  <done>
    All 16 SUMMARY.md files (across phases 8–13) have `requirements-completed` frontmatter consistent with each phase's VERIFICATION.md SATISFIED truth. The reconciliation script passes. A per-phase report of added REQ IDs is captured in the plan's SUMMARY.
  </done>
</task>

</tasks>

<verification>
- TD-01: `grep "omitting \`dialect\` is a validation error" .planning/ROADMAP.md` ≥1; old text gone.
- TD-02: `grep "85%+ test coverage" .planning/REQUIREMENTS.md` ≥1; old "70%+" gone.
- TD-03: reconciliation script (see Task 2 verify block) exits 0 for all 6 phases.
- TD-04: `uv run ruff check src/` exits 0.
- Full suite: `uv run pytest -x -q` still passes (no code changed in Task 1; Task 2 is docs-only).
</verification>

<success_criteria>
- `.planning/v2.0-MILESTONE-AUDIT.md` `tech_debt` block can be cleared after this plan runs.
- Future milestone audits that read SUMMARY frontmatter will not false-flag satisfied REQs as missing.
- ROADMAP and REQUIREMENTS text match implementation reality (dialect required; 85% coverage floor).
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-close-v2-0-gap-thread-dialect-through-schema-tools-query-too/13.1-04-SUMMARY.md` including the per-phase reconciliation report from Task 2.
</output>
