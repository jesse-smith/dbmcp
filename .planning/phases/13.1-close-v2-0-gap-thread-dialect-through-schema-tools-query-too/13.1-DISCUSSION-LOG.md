# Phase 13.1: Discussion Log

**Session:** 2026-05-06
**Mode:** discuss (default)
**Outcome:** CONTEXT.md written — ready for planning

## Context

No prior CONTEXT.md, RESEARCH.md, or plans for this phase. Phase was inserted via `/gsd-phase --insert` after the v2.0 milestone audit found 3 wiring gaps. Audit (`.planning/v2.0-MILESTONE-AUDIT.md`) prescribed exact fix sites, line numbers, and replacement code — so discussion focused on scope and implementation-style gray areas, not discovery.

## Gray Areas Presented

### 1. WIRING-03 scope

**Options:**
- Fix now — thread caller dialect through `connect()` for consistency; removes latent coupling
- Defer — no behavioral impact; capture as tech debt

**Selected:** Fix now.

**Rationale:** Phase title already scopes in WIRING-03; closing it now keeps the milestone audit clean.

### 2. get_sample_data fix approach (WIRING-02)

**Options:**
- Reuse `_get_metadata_service` helper — DRY, single source of truth
- Inline `conn_manager.get_dialect(connection_id)` at call site — locally obvious, more verbose

**Selected:** Reuse helper.

**Rationale:** Helper already exists and threads dialect correctly. Avoids drift between the two paths.

### 3. Regression test strategy

**Options:**
- Assert-dialect unit tests per call site — fast, catches regression even if behavior-level misses
- Behavior-level tests only — closer to user outcome, slower, needs fixtures
- Both — defense in depth

**Selected:** Assert-dialect unit tests per call site.

**Rationale:** Construction asserts are deterministic and directly target the bug shape (forgot to pass dialect). Behavior tests can follow as low-cost additions at planner's discretion.

### 4. WIRING-01 test scope

**Options:**
- Test `schema_count` for generic/Databricks — proves user-visible consequence
- Skip — covered by construction assertion

**Selected:** Test schema_count behavior.

**Rationale:** Construction assert proves dialect is passed; schema_count test proves the response actually surfaces correct count for the generic-URL case where `engine.dialect.name != "generic"`.

## Deferred Ideas

- v2.0 tech debt (ROADMAP text, REQUIREMENTS TEST-03 wording, SUMMARY.md frontmatter, metrics.py ruff) — out of phase scope.
- Nyquist validation backfill for phases 8/9/10/12/13 — separate `/gsd-validate-phase` runs.
- Behavior-level `validate_query`-from-`get_sample_data` integration test — planner's discretion.

## Claude's Discretion (recorded in CONTEXT.md)

- Helper promotion decision (keep `_get_metadata_service` in schema_tools.py vs move to shared module).
- Exact test file placement.
- Whether to add an additional behavior test beyond unit assertions.
- Test depth for WIRING-03 beyond construction assert.
