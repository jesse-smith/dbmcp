---
phase: 14-connect-time-hardening-databricks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/metadata.py
autonomous: true
requirements:
  - IDENT-02
must_haves:
  truths:
    - "list_schemas on Databricks never calls SHOW CATALOGS (no fallback to catalog listing)"
    - "The _list_databricks_catalogs method no longer exists in src/db/metadata.py"
    - "_databricks_default_catalog is renamed to _engine_catalog and has no 'main' fallback"
  artifacts:
    - path: "src/db/metadata.py"
      provides: "Cleaned-up list_schemas Databricks branch with fallback code removed and method renamed"
      contains: "def _engine_catalog"
  key_links:
    - from: "src/db/metadata.py::list_schemas"
      to: "_engine_catalog"
      via: "catalog resolution when explicit catalog= is not supplied"
      pattern: "self\\._engine_catalog\\(\\)"
---

<objective>
Delete the silent catalog-listing fallback in `MetadataService.list_schemas` (D-09, D-10),
rename `_databricks_default_catalog` → `_engine_catalog` with no `"main"` fallback (D-11),
and update the `list_schemas` docstring to reflect the post-IDENT-01 invariant (D-12).

Purpose: Satisfies IDENT-02. After this plan, the "schemas returned are actually catalogs"
silent-data-corruption class is eliminated. `list_schemas` on Databricks has a single
deterministic path; if the engine's catalog invariant is violated (it should not be,
post-IDENT-01), a `KeyError` propagates loudly instead of a silent wrong-data result.

Output: Cleaned `src/db/metadata.py` with three deletions, one rename, and a docstring
update.
</objective>

<execution_context>
@/Users/jsmith79/.claude/plugins/cache/gsd-plugin/gsd/2.40.1/workflows/execute-plan.md
@/Users/jsmith79/.claude/plugins/cache/gsd-plugin/gsd/2.40.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-connect-time-hardening-databricks/14-CONTEXT.md
@.planning/phases/14-connect-time-hardening-databricks/14-RESEARCH.md
@src/db/metadata.py

<interfaces>
Relevant code regions in src/db/metadata.py (verify by re-reading before edits):

- Lines 79-94: `list_schemas` docstring. Contains "falls back to listing available catalogs"
  language that needs to change per D-12.
- Lines 107-115: the `try/except SQLAlchemyError` block that calls `_list_databricks_catalogs`
  on failure. This entire block is deleted (D-10). Replace with the single-path form:
  ```python
  if self._dialect and self._dialect.name == "databricks":
      effective_catalog = catalog or self._engine_catalog()
      result = self._list_schemas_databricks(connection_id, effective_catalog)
  ```
- Lines 164-190 (approx): `_list_databricks_catalogs` method — DELETED entirely (D-09).
- Lines 192-201 (approx): `_databricks_default_catalog` method — RENAMED to `_engine_catalog`,
  body replaced with `return self.engine.url.query["catalog"]` (D-11).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete _list_databricks_catalogs and the try/except fallback in list_schemas</name>
  <files>src/db/metadata.py</files>
  <read_first>
    - src/db/metadata.py (lines 79-201 — the full list_schemas method plus the two helper methods)
    - .planning/phases/14-connect-time-hardening-databricks/14-CONTEXT.md (D-09, D-10, D-12)
    - .planning/phases/14-connect-time-hardening-databricks/14-RESEARCH.md (§"Pre-IDENT-01 failure mode")
  </read_first>
  <behavior>
    - `grep "_list_databricks_catalogs" src/db/metadata.py` returns zero matches after this
      task (method and its sole caller are both gone).
    - The Databricks branch of `list_schemas` no longer has a `try/except SQLAlchemyError`
      block. SQLAlchemyError from `_list_schemas_databricks` now propagates naturally to the
      caller (same as MSSQL/generic branches).
    - Calling `list_schemas` on a Databricks connection where the bound catalog has valid
      schemas returns those schemas (behavior unchanged for the happy path).
  </behavior>
  <action>
Per D-09, D-10, D-12:

1. Delete the entire `_list_databricks_catalogs` method body (lines ~164-190). Confirm no
   other callsite exists before deleting:
   `grep -rn "_list_databricks_catalogs" src/ tests/` — should show only the callsite at
   line 115 and the method definition itself.

2. In `list_schemas`, replace the try/except block (lines ~107-115) with the single-path form.
   Current shape (approximate — verify by reading):
   ```python
   default_catalog = self._databricks_default_catalog()
   try:
       result = self._list_schemas_databricks(connection_id, default_catalog)
   except SQLAlchemyError as exc:
       logger.info(...)
       result = self._list_databricks_catalogs(connection_id)
   ```
   Replace with:
   ```python
   effective_catalog = catalog or self._engine_catalog()
   result = self._list_schemas_databricks(connection_id, effective_catalog)
   ```
   (Note: the rename `_databricks_default_catalog` → `_engine_catalog` happens in Task 2; for
   this task you may call it by the old name, and Task 2 does the rename. OR do both together
   — executor's choice. If done together, ensure the rename is consistent across file.)

   CRITICAL: keep the `catalog or ...` form so the optional `catalog=` parameter (D-13) still
   wins over the engine-bound catalog. Only fall through to `_engine_catalog()` when the
   caller did not pass `catalog=`.

3. Update the docstring at lines 79-94. Remove the "falls back to listing available catalogs"
   language. New docstring should state, approximately:
   ```
   """List schemas visible in the catalog.

   On Databricks, the catalog is always known post-connect (IDENT-01 invariant):
   if `catalog=` is supplied, that catalog is used; otherwise the engine-bound
   catalog (from the SQLAlchemy URL) is used. No fallback to catalog enumeration.

   Args:
       connection_id: Active connection id.
       catalog: Optional explicit catalog (Databricks cross-catalog discovery).

   Returns:
       list[Schema]
   """
   ```
   (Exact phrasing is discretionary per CONTEXT.md. Keep the IDENT-01 invariant named.)

4. If the `from sqlalchemy.exc import SQLAlchemyError` import at line 18 is now unused by the
   file, leave it (other methods may still use it). Confirm with a quick grep before deleting
   the import:
   `grep -n "SQLAlchemyError" src/db/metadata.py` — only remove the import if the file has
   zero remaining references after deletion.

5. If existing tests in `tests/unit/test_metadata.py` monkeypatch `_list_databricks_catalogs`
   or rely on the fallback behavior, either delete or update those tests. Document any
   test removals in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_metadata.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_list_databricks_catalogs" src/db/metadata.py` returns ZERO matches.
    - `grep -n "except SQLAlchemyError" src/db/metadata.py` — if any remain, they are NOT
      inside the `list_schemas` Databricks branch (review each match).
    - The string "falls back to listing available catalogs" no longer appears anywhere in
      `src/db/metadata.py`.
    - Existing tests in `tests/unit/test_metadata.py` continue to pass (or have been updated
      consistently with the new single-path contract).
  </acceptance_criteria>
  <done>
    Fallback code deleted. list_schemas Databricks branch is single-path. Docstring reflects
    IDENT-01 invariant. Existing tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename _databricks_default_catalog to _engine_catalog, strip "main" fallback</name>
  <files>src/db/metadata.py</files>
  <read_first>
    - src/db/metadata.py (current definition at ~line 192-201 and all callsites)
    - .planning/phases/14-connect-time-hardening-databricks/14-CONTEXT.md (D-11)
    - .planning/phases/14-connect-time-hardening-databricks/14-RESEARCH.md (§"Pitfall 4" — grep before rename)
  </read_first>
  <behavior>
    - `self._engine_catalog()` returns `self.engine.url.query["catalog"]` unconditionally.
    - No `"main"` string literal exists anywhere in `src/db/metadata.py` after this task.
    - If the engine's `url.query` dict has no `"catalog"` key (should never happen
      post-IDENT-01), a `KeyError` propagates — this is intentional (loud failure per D-11).
    - All previous callers of `_databricks_default_catalog` now call `_engine_catalog` and
      work identically for the valid-catalog happy path.
  </behavior>
  <action>
Per D-11 and Pitfall 4 in research:

1. Pre-check: `grep -rn "_databricks_default_catalog" src/ tests/` — identify ALL callsites
   before renaming. Expected: only `src/db/metadata.py` (method definition plus the one
   callsite inside `list_schemas` that Task 1 may have left as-is).

2. Rename the method. Replace the current body with the single-line form:
   ```python
   def _engine_catalog(self) -> str:
       """Return the catalog the engine was built with.

       Post-IDENT-01 invariant: a Databricks engine is always constructed with a
       non-empty `catalog` in the URL query. If this invariant is violated,
       KeyError propagates — a loud failure beats a silent "main" default.
       """
       return self.engine.url.query["catalog"]
   ```

3. Update the callsite(s) inside `list_schemas` (and any other caller flagged by the grep in
   step 1) from `self._databricks_default_catalog()` to `self._engine_catalog()`.

4. Update any test that monkeypatches or asserts on `_databricks_default_catalog` by name to
   use `_engine_catalog`.

5. Final verification sweep: `grep -rn '"main"' src/db/metadata.py tests/unit/test_metadata.py`
   should show no matches introduced by this plan (pre-existing unrelated matches, if any,
   are fine — inspect each).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_metadata.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_databricks_default_catalog" src/ tests/` returns ZERO matches.
    - `grep -n "def _engine_catalog" src/db/metadata.py` returns exactly 1 match.
    - The `_engine_catalog` body is a single return statement: `return self.engine.url.query["catalog"]` — no try/except, no `.get(...)` with default, no `"main"`.
    - `grep -n '"main"' src/db/metadata.py` returns ZERO matches.
    - `uv run pytest tests/unit/test_metadata.py -x` passes.
  </acceptance_criteria>
  <done>
    Method renamed. All callsites updated. Body stripped to single `url.query["catalog"]`
    lookup. No "main" default anywhere in metadata.py. Tests green.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `uv run pytest tests/unit/test_metadata.py -x -v` — all pass
- `grep -rn "_list_databricks_catalogs\|_databricks_default_catalog" src/ tests/` — ZERO matches
- `grep -n '"main"' src/db/metadata.py` — ZERO matches
</verification>

<success_criteria>
- `_list_databricks_catalogs` method deleted (D-09)
- `try/except SQLAlchemyError` fallback in `list_schemas` Databricks branch deleted (D-10)
- `_databricks_default_catalog` renamed to `_engine_catalog` with "main" fallback stripped (D-11)
- `list_schemas` docstring updated (D-12)
- All existing metadata tests still green
</success_criteria>

<output>
Create `.planning/phases/14-connect-time-hardening-databricks/14-02-SUMMARY.md` describing:
- Lines deleted and the exact line ranges before deletion
- Rename executed and all callsites updated
- Exact new docstring for `list_schemas`
- Any test updates required to align with the new single-path contract
</output>
