---
phase: 01-atomic-toon-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/mcp_server/schema_tools.py
  - src/mcp_server/query_tools.py
  - src/mcp_server/analysis_tools.py
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "All 9 tool docstrings document TOON response format (not JSON)"
    - "No docstring contains 'JSON string' in its Returns section"
    - "Docstrings use structural outline format: field names, types, conditional annotations"
    - "TOON is named explicitly in each Returns section"
  artifacts:
    - path: "src/mcp_server/schema_tools.py"
      provides: "4 tool docstrings with TOON format documentation"
      contains: "TOON-encoded string"
    - path: "src/mcp_server/query_tools.py"
      provides: "2 tool docstrings with TOON format documentation"
      contains: "TOON-encoded string"
    - path: "src/mcp_server/analysis_tools.py"
      provides: "3 tool docstrings with TOON format documentation"
      contains: "TOON-encoded string"
  key_links:
    - from: "docstring Returns sections"
      to: "actual encode_response() output"
      via: "field names match response dict keys"
      pattern: "TOON-encoded string with"
---

<objective>
Update all 9 MCP tool docstrings to document TOON response format instead of JSON.

Purpose: FastMCP sends docstrings verbatim to LLM clients. Clients currently see "JSON string with..." but receive TOON. Docstrings must match actual output format so LLM consumers know what they're parsing.

Output: All 9 tool docstrings say "TOON-encoded string with..." and show structural outlines using TOON conventions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-atomic-toon-migration/01-CONTEXT.md
@.planning/phases/01-atomic-toon-migration/01-RESEARCH.md
@.planning/phases/01-atomic-toon-migration/01-02-SUMMARY.md

@src/mcp_server/schema_tools.py
@src/mcp_server/query_tools.py
@src/mcp_server/analysis_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 9 tool docstrings from JSON to TOON format documentation</name>
  <files>src/mcp_server/schema_tools.py, src/mcp_server/query_tools.py, src/mcp_server/analysis_tools.py</files>
  <action>
    For each of the 9 MCP tool functions, update the Returns section of the docstring:

    **Format rules (from CONTEXT.md locked decisions):**
    - Name TOON explicitly: "TOON-encoded string with..."
    - Structural outline only: field names, types, and conditional annotations
    - No full sample data -- just the schema
    - Same structure as current JSON examples, different syntax

    **Docstring template per tool:**
    ```
    Returns:
        TOON-encoded string with {description}:

            status: "success" | "error"
            {field_name}: {type}               // {annotation if needed}
            {field_name}: {type}               // on success only
            error_message: string              // on error only
    ```

    **schema_tools.py -- 4 tools:**
    1. `connect_database` -- Returns connection details (connection_id, message, schema_count, has_cached_docs on success; error_message on error; auth_error/auth_method on auth failure)
    2. `list_schemas` -- Returns schema list (total_schemas, schemas array with schema_name/table_count/view_count)
    3. `list_tables` -- Returns table list (total_count, returned_count, tables array with schema_name/table_name/type/row_count/has_primary_key, pagination info)
    4. `get_table_schema` -- Returns table structure (table object with columns, indexes, relationships)

    **query_tools.py -- 2 tools:**
    5. `get_sample_data` -- Returns sample rows (row_count, columns, rows, sampling_method)
    6. `execute_query` -- Returns query results (row_count, columns, rows, execution_time_ms, truncated)

    **analysis_tools.py -- 3 tools:**
    7. `get_column_info` -- Returns column statistics (column_name, data_type, stats object with type-specific fields)
    8. `find_pk_candidates` -- Returns PK candidates (table, candidates array with columns/score/reasons)
    9. `find_fk_candidates` -- Returns FK candidates (column, candidates array with referenced_table/confidence/evidence)

    **Process:**
    1. Read each tool function's current docstring Returns section
    2. Read the actual response dict being passed to encode_response() to get accurate field names
    3. Rewrite Returns section using TOON structural outline format
    4. Keep all other docstring sections (Args, description) unchanged
    5. After all 9 are done, verify with ruff and grep

    **Verification grep:**
    - `grep -n "JSON string" src/mcp_server/*.py` -- should return ZERO matches
    - `grep -n "TOON-encoded string" src/mcp_server/*.py` -- should return 9 matches (one per tool)
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q && uv run ruff check src/mcp_server/</automated>
  </verify>
  <done>
    - All 9 tool docstrings say "TOON-encoded string with..." in Returns section
    - Zero occurrences of "JSON string" in tool module docstrings
    - Structural outlines list actual field names matching the response dicts
    - All tests still pass (docstring changes are behavior-neutral)
    - ruff clean
  </done>
</task>

</tasks>

<verification>
- `grep -rn "JSON string" src/mcp_server/schema_tools.py src/mcp_server/query_tools.py src/mcp_server/analysis_tools.py` -- zero matches
- `grep -c "TOON-encoded string" src/mcp_server/schema_tools.py src/mcp_server/query_tools.py src/mcp_server/analysis_tools.py` -- counts should be 4, 2, 3
- `uv run pytest tests/ -x -q` -- all tests pass
- `uv run ruff check src/` -- clean
</verification>

<success_criteria>
- All 9 tool docstrings document TOON format
- No JSON format references remain in Returns sections
- Tests still pass (no regressions from docstring edits)
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-toon-migration/01-03-SUMMARY.md`
</output>
