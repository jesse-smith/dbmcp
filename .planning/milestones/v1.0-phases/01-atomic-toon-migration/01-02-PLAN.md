---
phase: 01-atomic-toon-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/mcp_server/schema_tools.py
  - src/mcp_server/query_tools.py
  - src/mcp_server/analysis_tools.py
  - tests/integration/test_discovery.py
  - tests/integration/test_get_column_info.py
  - tests/integration/test_pk_discovery.py
  - tests/integration/test_query_execution.py
  - tests/integration/test_fk_candidates.py
  - tests/integration/test_sample_data.py
  - tests/utils.py
autonomous: true
requirements:
  - SRLZ-03
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "All 9 MCP tools return TOON-encoded strings (no json.dumps in tool modules)"
    - "All integration tests pass using parse_tool_response() (no json.loads on tool responses)"
    - "tests/utils.py assert helpers use parse_tool_response (no json.loads)"
    - "Non-primitive types in analysis_tools.py responses are properly pre-serialized (no default=str)"
  artifacts:
    - path: "src/mcp_server/schema_tools.py"
      provides: "4 tools returning TOON via encode_response"
      contains: "from src.serialization import encode_response"
    - path: "src/mcp_server/query_tools.py"
      provides: "2 tools returning TOON via encode_response"
      contains: "from src.serialization import encode_response"
    - path: "src/mcp_server/analysis_tools.py"
      provides: "3 tools returning TOON via encode_response"
      contains: "from src.serialization import encode_response"
    - path: "tests/utils.py"
      provides: "assert_json_contains and assert_json_has_keys updated to use parse_tool_response"
      contains: "from tests.helpers import parse_tool_response"
  key_links:
    - from: "src/mcp_server/schema_tools.py"
      to: "src/serialization.py"
      via: "import encode_response"
      pattern: "from src\\.serialization import encode_response"
    - from: "src/mcp_server/query_tools.py"
      to: "src/serialization.py"
      via: "import encode_response"
      pattern: "from src\\.serialization import encode_response"
    - from: "src/mcp_server/analysis_tools.py"
      to: "src/serialization.py"
      via: "import encode_response"
      pattern: "from src\\.serialization import encode_response"
    - from: "tests/integration/*"
      to: "tests/helpers.py"
      via: "import parse_tool_response"
      pattern: "from tests\\.helpers import parse_tool_response"
---

<objective>
Atomic swap: replace all json.dumps calls in 3 tool modules with encode_response() and all json.loads calls in 6 integration test files with parse_tool_response(), simultaneously. This is the core migration.

Purpose: This is the "atomic" part of "Atomic TOON Migration" -- tools and tests change together so there is never a mixed JSON/TOON state. Every tool response is TOON, every test deserializes TOON.

Output: All 9 tools return TOON, all integration tests pass against TOON output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-atomic-toon-migration/01-CONTEXT.md
@.planning/phases/01-atomic-toon-migration/01-RESEARCH.md
@.planning/phases/01-atomic-toon-migration/01-01-SUMMARY.md

@src/mcp_server/schema_tools.py
@src/mcp_server/query_tools.py
@src/mcp_server/analysis_tools.py
@tests/integration/test_discovery.py
@tests/integration/test_get_column_info.py
@tests/integration/test_pk_discovery.py
@tests/integration/test_query_execution.py
@tests/integration/test_fk_candidates.py
@tests/integration/test_sample_data.py
@tests/utils.py

<interfaces>
<!-- From Plan 01 outputs (src/serialization.py) -->
from src.serialization import encode_response
# encode_response(data: dict) -> str
# Pre-serializes datetime, date, StrEnum, Decimal, then TOON-encodes

<!-- From Plan 01 outputs (tests/helpers.py) -->
from tests.helpers import parse_tool_response
# parse_tool_response(response: str) -> dict
# Decodes TOON string, raises ValueError if result is not a dict
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all 3 tool modules from json.dumps to encode_response</name>
  <files>src/mcp_server/schema_tools.py, src/mcp_server/query_tools.py, src/mcp_server/analysis_tools.py</files>
  <action>
    For each of the 3 tool modules, perform these changes:

    **All 3 files:**
    1. Remove `import json` line
    2. Add `from src.serialization import encode_response`
    3. Replace every `json.dumps(...)` call with `encode_response(...)`
    4. For simple cases: `json.dumps({"status": "error", ...})` becomes `encode_response({"status": "error", ...})`
    5. Run `uv run ruff check` on each file after editing

    **schema_tools.py specific:**
    - 16 json.dumps calls across connect_database (5), list_schemas (3), list_tables (4), get_table_schema (4)
    - Straightforward replacement -- no default=str or special args

    **query_tools.py specific:**
    - 11 json.dumps calls across get_sample_data (6), execute_query (5)
    - In execute_query, there is `json.dumps(result)` where `result` is already a dict -- replace with `encode_response(result)`
    - Straightforward replacement -- no default=str

    **analysis_tools.py specific (MOST CARE NEEDED):**
    - 13 json.dumps calls across get_column_info (4), find_pk_candidates (4), find_fk_candidates (5)
    - CRITICAL: Line ~137 has `json.dumps(response, default=str)` -- replace with plain `encode_response(response)`. The pre-serializer in encode_response() handles datetime/Decimal/StrEnum that default=str was catching. Remove the `default=str` -- do NOT try to replicate it.
    - Remove `import json` at top

    After all 3 files are edited:
    6. Run `uv run ruff check src/mcp_server/` -- must be clean
    7. Verify no `json.dumps` remains: grep for json.dumps in all 3 files -- should return zero matches
    8. Do NOT run tests yet (they still use json.loads and will fail -- that's expected)
  </action>
  <verify>
    <automated>uv run ruff check src/mcp_server/schema_tools.py src/mcp_server/query_tools.py src/mcp_server/analysis_tools.py</automated>
  </verify>
  <done>
    - Zero json.dumps calls remain in schema_tools.py, query_tools.py, analysis_tools.py
    - All 3 files import encode_response from src.serialization
    - No `import json` remains in tool modules (unless used for non-response purposes -- check first)
    - ruff clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all integration tests and utils from json.loads to parse_tool_response</name>
  <files>tests/integration/test_discovery.py, tests/integration/test_get_column_info.py, tests/integration/test_pk_discovery.py, tests/integration/test_query_execution.py, tests/integration/test_fk_candidates.py, tests/integration/test_sample_data.py, tests/utils.py</files>
  <action>
    **tests/utils.py:**
    1. Update `assert_json_contains()`: replace `json_module.loads(json_str)` with `from tests.helpers import parse_tool_response` and `parse_tool_response(json_str)`. Rename the parameter from `json_str` to `response_str` for clarity (optional but recommended). Remove `import json as json_module` inside the function.
    2. Update `assert_json_has_keys()`: same pattern -- replace json.loads with parse_tool_response.
    3. Consider renaming functions from `assert_json_*` to `assert_response_*` (optional -- only if no external callers depend on old names; grep first).

    **All 6 integration test files:**
    For each file:
    4. Add `from tests.helpers import parse_tool_response` to imports
    5. Replace every `json.loads(result)` or `json.loads(response)` with `parse_tool_response(result)` or `parse_tool_response(response)`
    6. Remove `import json` if no other json usage remains in the file
    7. If any test does strict type checking on numeric values (comparing int vs float), relax the assertion to handle TOON's numeric representation (Research Pitfall 5). Use `pytest.approx()` for float comparisons or `== expected_value` without strict type check.

    **Occurrence counts to verify against:**
    - test_discovery.py: ~11 json.loads
    - test_get_column_info.py: ~14 json.loads
    - test_pk_discovery.py: ~9 json.loads
    - test_query_execution.py: ~13 json.loads
    - test_fk_candidates.py: ~16 json.loads
    - test_sample_data.py: ~1 json.loads

    After all files edited:
    8. Run full test suite: `uv run pytest tests/ -x -q`
    9. If any tests fail, diagnose:
       - Numeric type mismatch (int vs float) -> relax assertion
       - Unexpected None values -> pre-serialization gap in encode_response (should not happen if Plan 01 was correct)
       - Import errors -> fix import paths
    10. Run `uv run ruff check tests/` -- must be clean
    11. Verify compliance: grep for `json.loads` in tests/integration/ -- should find ZERO occurrences on tool response strings. Note: json.loads may still exist for non-response parsing (e.g., fixture data) -- that's acceptable.
  </action>
  <verify>
    <automated>uv run pytest tests/ -v && uv run ruff check tests/</automated>
  </verify>
  <done>
    - All 385+ tests pass (zero regressions)
    - Zero json.loads calls on tool responses in integration tests
    - tests/utils.py assert helpers use parse_tool_response
    - All integration tests exercise TOON decode path via parse_tool_response
    - ruff clean across tests/
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -v` -- full suite green
- `grep -rn "json\.dumps" src/mcp_server/schema_tools.py src/mcp_server/query_tools.py src/mcp_server/analysis_tools.py` -- zero matches
- `grep -rn "json\.loads" tests/integration/` -- zero matches on tool response lines (may have non-response uses)
- `uv run ruff check src/ tests/` -- clean
</verification>

<success_criteria>
- All 9 MCP tools return TOON-encoded strings
- All integration tests decode TOON via parse_tool_response
- Full test suite passes with zero regressions
- No json.dumps in tool modules, no json.loads on tool responses in tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-atomic-toon-migration/01-02-SUMMARY.md`
</output>
