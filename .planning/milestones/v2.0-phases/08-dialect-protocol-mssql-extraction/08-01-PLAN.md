---
phase: 08-dialect-protocol-mssql-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/dialects/__init__.py
  - src/db/dialects/protocol.py
  - src/db/dialects/registry.py
  - tests/unit/test_dialect_protocol.py
  - tests/unit/test_dialect_registry.py
autonomous: true
requirements:
  - DIAL-01
  - DIAL-05
must_haves:
  truths:
    - "DialectStrategy protocol exists with all required members per ROADMAP"
    - "Registry maps dialect name strings to strategy classes"
    - "Unknown dialect names raise ValueError with helpful message"
    - "Protocol is runtime_checkable for isinstance verification"
  artifacts:
    - path: "src/db/dialects/protocol.py"
      provides: "DialectStrategy Protocol definition"
      contains: "class DialectStrategy(Protocol)"
    - path: "src/db/dialects/registry.py"
      provides: "Dict registry with register/get functions"
      exports: ["register_dialect", "get_dialect"]
    - path: "src/db/dialects/__init__.py"
      provides: "Package exports"
    - path: "tests/unit/test_dialect_protocol.py"
      provides: "Protocol conformance tests"
    - path: "tests/unit/test_dialect_registry.py"
      provides: "Registry behavior tests"
  key_links:
    - from: "src/db/dialects/registry.py"
      to: "src/db/dialects/protocol.py"
      via: "import DialectStrategy for type annotation"
      pattern: "from src.db.dialects.protocol import DialectStrategy"
---

<objective>
Define the DialectStrategy protocol and dialect registry -- the foundational abstractions
that all subsequent plans depend on.

Purpose: Establish the protocol contract (DIAL-01) and registry mechanism (DIAL-05) as
new files with no modifications to existing code. This enables Plan 02 to implement
MssqlDialect and wire it into services.

Output: `src/db/dialects/` package with protocol.py, registry.py, __init__.py, plus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dialect-protocol-mssql-extraction/08-CONTEXT.md
@.planning/phases/08-dialect-protocol-mssql-extraction/08-RESEARCH.md

<interfaces>
<!-- No existing interfaces to extract -- this plan creates the first protocol in the codebase. -->
<!-- Downstream Plan 02 will implement MssqlDialect against this protocol. -->

From src/db/connection.py (used by Plan 02, shown here for context):
```python
@dataclass
class PoolConfig:
    pool_size: int = 5
    max_overflow: int = 10
    pool_timeout: int = 30
    pool_recycle: int = 3600
    pool_pre_ping: bool = True
    query_timeout: int = 30
    azure_ad_pool_recycle: int = 2700
```

From src/models/schema.py (AuthenticationMethod enum used by current connection code):
```python
class AuthenticationMethod(StrEnum):
    SQL = "sql"
    WINDOWS = "windows"
    AZURE_AD = "azure_ad"
    AZURE_AD_INTEGRATED = "azure_ad_integrated"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define DialectStrategy protocol and tests (DIAL-01)</name>
  <files>src/db/dialects/__init__.py, src/db/dialects/protocol.py, tests/unit/test_dialect_protocol.py</files>
  <read_first>
    - src/db/connection.py (understand create_engine signature and PoolConfig)
    - src/db/metadata.py (understand is_mssql pattern and DMV usage)
    - src/db/query.py (understand _sanitize_identifier bracket quoting)
    - .planning/phases/08-dialect-protocol-mssql-extraction/08-RESEARCH.md (protocol design section)
  </read_first>
  <behavior>
    - Test 1: A class implementing all protocol members is recognized as DialectStrategy via isinstance check (runtime_checkable)
    - Test 2: A class missing a required member is NOT recognized as DialectStrategy via isinstance
    - Test 3: Protocol has property `name` returning str
    - Test 4: Protocol has property `sqlglot_dialect` returning str
    - Test 5: Protocol has property `supports_indexes` returning bool
    - Test 6: Protocol has property `has_fast_row_counts` returning bool
    - Test 7: Protocol has method `create_engine(**kwargs) -> Engine`
    - Test 8: Protocol has method `fast_row_counts(engine, schema_name=None) -> dict[str, int]`
    - Test 9: Protocol has method `quote_identifier(identifier) -> str`
  </behavior>
  <action>
    Create `src/db/dialects/` package directory with `__init__.py`.

    Create `src/db/dialects/protocol.py` with:
    - `from typing import Protocol, runtime_checkable`
    - `from sqlalchemy.engine import Engine`
    - `@runtime_checkable` decorator on the class
    - `class DialectStrategy(Protocol):` with docstring per project conventions (Google-style)
    - Properties (all `@property` with `...` body):
      - `name -> str` (dialect identifier: 'mssql', 'databricks', 'generic')
      - `sqlglot_dialect -> str` (sqlglot dialect name: 'tsql', 'databricks')
      - `supports_indexes -> bool` (per D-02: bool properties for capability flags)
      - `has_fast_row_counts -> bool` (per D-02)
    - Methods (all with `...` body):
      - `create_engine(self, **kwargs) -> Engine`
      - `fast_row_counts(self, engine: Engine, schema_name: str | None = None) -> dict[str, int]`
      - `quote_identifier(self, identifier: str) -> str`
    - All members have Google-style docstrings with Args/Returns.
    - Per D-03: No additional methods beyond what ROADMAP specifies.
    - Per D-07: `fast_row_counts()` returns empty dict when unsupported (documented in docstring), guarded by `has_fast_row_counts` flag.

    Create `src/db/dialects/__init__.py` with:
    - `from src.db.dialects.protocol import DialectStrategy`
    - `__all__ = ["DialectStrategy"]` (registry exports added in Task 2)

    Create `tests/unit/test_dialect_protocol.py` with:
    - A conforming stub class `_StubDialect` implementing all members (returns dummy values, `create_engine` returns `MagicMock(spec=Engine)`)
    - A non-conforming stub class `_IncompleteDialect` missing `quote_identifier`
    - Test: `isinstance(_StubDialect(), DialectStrategy)` is True
    - Test: `isinstance(_IncompleteDialect(), DialectStrategy)` is False
    - Tests verifying each property and method exists on the protocol with correct return types via the stub
    - Use `from src.db.dialects.protocol import DialectStrategy`
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dialect_protocol.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/db/dialects/protocol.py contains `@runtime_checkable`
    - src/db/dialects/protocol.py contains `class DialectStrategy(Protocol):`
    - src/db/dialects/protocol.py contains `def name(self) -> str:`
    - src/db/dialects/protocol.py contains `def sqlglot_dialect(self) -> str:`
    - src/db/dialects/protocol.py contains `def supports_indexes(self) -> bool:`
    - src/db/dialects/protocol.py contains `def has_fast_row_counts(self) -> bool:`
    - src/db/dialects/protocol.py contains `def create_engine(self, **kwargs) -> Engine:`
    - src/db/dialects/protocol.py contains `def fast_row_counts(self, engine: Engine, schema_name: str | None = None) -> dict[str, int]:`
    - src/db/dialects/protocol.py contains `def quote_identifier(self, identifier: str) -> str:`
    - src/db/dialects/__init__.py contains `from src.db.dialects.protocol import DialectStrategy`
    - tests/unit/test_dialect_protocol.py exits 0
  </acceptance_criteria>
  <done>DialectStrategy protocol exists with all 4 properties and 3 methods per ROADMAP spec. Tests verify runtime_checkable conformance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create dialect registry with tests (DIAL-05)</name>
  <files>src/db/dialects/registry.py, tests/unit/test_dialect_registry.py, src/db/dialects/__init__.py</files>
  <read_first>
    - src/db/dialects/protocol.py (created in Task 1 -- the protocol it references)
    - src/db/dialects/__init__.py (to add registry exports)
    - .planning/phases/08-dialect-protocol-mssql-extraction/08-RESEARCH.md (registry pattern section)
  </read_first>
  <behavior>
    - Test 1: `register_dialect("test", StubDialect)` succeeds without error
    - Test 2: `get_dialect("test")` returns `StubDialect` class after registration
    - Test 3: `get_dialect("unknown")` raises `ValueError` with message containing "Unknown dialect 'unknown'"
    - Test 4: `get_dialect("unknown")` error message lists registered dialect names
    - Test 5: Registering same name twice overwrites (no error -- simple dict semantics)
    - Test 6: `get_dialect` with empty registry raises ValueError with "(none)" in message
  </behavior>
  <action>
    Create `src/db/dialects/registry.py` with:
    - Module docstring: "Dialect registry mapping dialect names to strategy implementations."
    - `from src.db.dialects.protocol import DialectStrategy`
    - Module-level `_REGISTRY: dict[str, type[DialectStrategy]] = {}`
    - `def register_dialect(name: str, dialect_class: type[DialectStrategy]) -> None:` 
      - Google-style docstring with Args
      - Body: `_REGISTRY[name] = dialect_class`
    - `def get_dialect(name: str) -> type[DialectStrategy]:`
      - Google-style docstring with Args/Returns/Raises
      - If `name not in _REGISTRY`: build registered list as `", ".join(sorted(_REGISTRY.keys())) or "(none)"`, raise `ValueError(f"Unknown dialect '{name}'. Registered dialects: {registered}")`
      - Return `_REGISTRY[name]`
    - Per D-06: No fallback -- unknown names raise ValueError in Phase 8.

    Update `src/db/dialects/__init__.py` to add:
    - `from src.db.dialects.registry import get_dialect, register_dialect`
    - Update `__all__` to `["DialectStrategy", "get_dialect", "register_dialect"]`

    Create `tests/unit/test_dialect_registry.py` with:
    - Import `register_dialect, get_dialect` from `src.db.dialects.registry`
    - Use a `_StubDialect` class (copy from test_dialect_protocol or define locally)
    - Use `monkeypatch` or a fixture that resets `_REGISTRY` between tests to avoid cross-test pollution. Approach: `@pytest.fixture(autouse=True)` that saves and restores `registry._REGISTRY`.
    - Tests per behavior list above.
    - Use `pytest.raises(ValueError, match=r"Unknown dialect")` for error cases.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dialect_registry.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/db/dialects/registry.py contains `_REGISTRY: dict[str, type[DialectStrategy]] = {}`
    - src/db/dialects/registry.py contains `def register_dialect(name: str, dialect_class: type[DialectStrategy]) -> None:`
    - src/db/dialects/registry.py contains `def get_dialect(name: str) -> type[DialectStrategy]:`
    - src/db/dialects/registry.py contains `raise ValueError`
    - src/db/dialects/__init__.py contains `register_dialect`
    - src/db/dialects/__init__.py contains `get_dialect`
    - tests/unit/test_dialect_registry.py exits 0
    - uv run ruff check src/db/dialects/ exits 0
  </acceptance_criteria>
  <done>Registry maps dialect names to classes. Unknown names raise ValueError with helpful message listing registered dialects. Tests pass with isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None in this plan | Protocol and registry are internal code with no user input crossing trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Elevation of Privilege | registry.py | accept | Registry is populated at import time from hardcoded code, not user input. No runtime registration from external sources. |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_dialect_protocol.py tests/unit/test_dialect_registry.py -v` -- all pass
- `uv run ruff check src/db/dialects/` -- no lint errors
- `uv run pytest tests/unit/ -x -q` -- existing tests unaffected (new files only, no changes to existing code)
</verification>

<success_criteria>
- DialectStrategy protocol with 4 properties + 3 methods exists in src/db/dialects/protocol.py
- Registry with register_dialect() and get_dialect() exists in src/db/dialects/registry.py
- Package __init__.py exports all public names
- All new tests pass, all existing tests unaffected
- Ruff clean on new files
</success_criteria>

<output>
After completion, create `.planning/phases/08-dialect-protocol-mssql-extraction/08-01-SUMMARY.md`
</output>
