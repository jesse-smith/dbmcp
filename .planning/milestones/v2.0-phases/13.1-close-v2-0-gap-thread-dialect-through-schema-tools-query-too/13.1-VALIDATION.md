---
phase: 13.1
slug: close-v2-0-gap-thread-dialect-through-schema-tools-query-too
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-06
---

# Phase 13.1 — Validation Strategy

> Reconstructed from SUMMARY artifacts after execution (State B). All phase requirements have automated verification in the existing pytest suite.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x (pytest-asyncio, pytest-cov) |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/unit -x -q` |
| **Full suite command** | `uv run pytest -x -q` |
| **Estimated runtime** | ~60 seconds (full), ~10 seconds (unit-only) |

---

## Sampling Rate

- **After every task commit:** `uv run pytest tests/unit -x -q`
- **After every plan wave:** `uv run pytest -x -q`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~60s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 13.1-01-01 | 01 | 1 | META-01 | unit (regression) | `uv run pytest tests/unit/test_connect_tool.py::TestConnectDatabaseThreadsDialectIntoMetadataService -x -q` | ✅ | ✅ green |
| 13.1-02-01 | 02 | 1 | VALID-01, VALID-02, META-05 | unit (regression) | `uv run pytest tests/unit/test_async_tools.py -x -q -k threads_dialect` | ✅ | ✅ green |
| 13.1-03-01 | 03 | 1 | META-05 | unit (regression) | `uv run pytest tests/unit/test_connection.py::TestConnectDialectInjection -x -q` | ✅ | ✅ green |
| 13.1-04-01 | 04 | 2 | TD-01, TD-02, TD-04 (docs) | shell grep + ruff | `grep -q "omitting \`dialect\` is a validation error" .planning/ROADMAP.md && grep -q "85%+ test coverage maintained across all modules" .planning/REQUIREMENTS.md && uv run ruff check src/` | ✅ | ✅ green |
| 13.1-04-02 | 04 | 2 | TD-03 (frontmatter reconciliation) | shell script (phase loop) | See Task 2 `<verify>` block in `13.1-04-tech-debt-cleanup-PLAN.md` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.* The pytest suite, ruff, and shell grep tooling were all in place before phase 13.1 began; no Wave 0 install was needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| End-to-end dialect threading against a live Databricks warehouse | VALID-01, VALID-02 (live-connection dimension) | Requires real Databricks credentials; unit tests cover the wiring, not network I/O | Connect via `connect_database` with a Databricks URL, run `get_sample_data`, confirm sqlglot dialect resolves to `databricks` (not `tsql`) in logs |

*All phase behaviors that can be automated in-process are automated. The manual check above is the integration dimension that cannot run in CI without external credentials.*

---

## Deferred (Out of Scope for 13.1)

- `tests/integration/test_sample_data.py::test_get_sample_data_mcp_tool` — skipped (see `deferred-items.md`). Architectural root cause: `src/db/query.py:183-188` uses `_dialect is None` as a SQLite proxy in `_build_top_query`. Moving sample-SQL generation into `DialectStrategy` (so `GenericDialect` emits `LIMIT`) is tracked for a future plan.

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (none required)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-06 (retroactive — reconstructed from SUMMARY artifacts after phase execution completed)
