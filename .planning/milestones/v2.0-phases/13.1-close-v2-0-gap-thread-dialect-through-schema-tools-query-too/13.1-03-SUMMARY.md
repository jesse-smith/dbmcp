---
phase: 13.1
plan: 03
subsystem: db/connection
tags: [wiring, dialect, backward-compatible, regression-test]
requires: []
provides:
  - "ConnectionManager.connect() dialect injection point (removes latent MSSQL coupling)"
affects:
  - src/db/connection.py
tech-stack:
  added: []
  patterns:
    - "Optional dependency injection with backward-compatible default"
key-files:
  created: []
  modified:
    - src/db/connection.py
    - tests/unit/test_connection.py
decisions:
  - "WIRING-03 fix style D (minimal): extend signature with optional dialect kwarg and single-line conditional; preserve MssqlDialect() default. No caller migration required."
metrics:
  duration: ~6min
  completed: 2026-05-06
  tasks: 1
  files: 2
requirements:
  - META-05
---

# Phase 13.1 Plan 03: WIRING-03 — thread optional dialect through ConnectionManager.connect() MSSQL path Summary

**One-liner:** Added optional `dialect: DialectStrategy | None = None` kwarg to `ConnectionManager.connect()` so a caller's dialect is honored instead of being silently overwritten by a fresh `MssqlDialect()`; default behavior preserved for existing callers.

## What Changed

- **`src/db/connection.py`** (2 edits):
  - `connect()` signature: appended `dialect: DialectStrategy | None = None` (line 161) and documented in Args docstring.
  - Line 198 (now 203): `dialect = MssqlDialect()` → `dialect = dialect if dialect is not None else MssqlDialect()`. The caller-supplied dialect now flows through unchanged to `dialect.create_engine(...)` and into `self._dialects[connection_id]`.
- **`tests/unit/test_connection.py`**: added `TestConnectDialectInjection` class with two regression tests — (1) caller-supplied dialect is used and stored; (2) omitting the kwarg still instantiates `MssqlDialect` exactly once.

## TDD Gate Compliance

- RED: commit `640c477` — `test(13.1-03): add failing regression for caller-supplied dialect in ConnectionManager.connect` (verified failing with `TypeError: ... got an unexpected keyword argument 'dialect'`).
- GREEN: commit `4857edb` — `feat(13.1-03): accept optional dialect in ConnectionManager.connect (WIRING-03)`; both new tests pass, full suite green.
- REFACTOR: not needed (two-line change).

## Verification

- `grep -n "dialect: DialectStrategy | None = None" src/db/connection.py` → 1 match (line 161).
- `grep -n "dialect if dialect is not None else MssqlDialect" src/db/connection.py` → 1 match (line 203).
- `grep -cE "^\s*dialect = MssqlDialect\(\)\s*$" src/db/connection.py` → 0 (unconditional instantiation gone).
- `uv run pytest tests/unit/test_connection.py -x -q` → 46 passed.
- `uv run pytest -x -q --no-cov` → 948 passed, 78 skipped (no regressions).

## Deviations from Plan

None — plan executed exactly as written. `DialectStrategy` was already imported in `src/db/connection.py` (line 21), so no import changes were needed. The test was placed inside a new `TestConnectDialectInjection` class rather than as module-level functions to match the file's existing class-based test organization (cosmetic — behavior identical to the plan's snippet).

## Commits

| Hash    | Type | Message                                                                                          |
| ------- | ---- | ------------------------------------------------------------------------------------------------ |
| 640c477 | test | test(13.1-03): add failing regression for caller-supplied dialect in ConnectionManager.connect   |
| 4857edb | feat | feat(13.1-03): accept optional dialect in ConnectionManager.connect (WIRING-03)                  |

## Self-Check: PASSED

- FOUND: src/db/connection.py (modified; signature + conditional verified by grep).
- FOUND: tests/unit/test_connection.py (modified; `TestConnectDialectInjection` appended).
- FOUND: commit 640c477 (RED test).
- FOUND: commit 4857edb (GREEN implementation).
